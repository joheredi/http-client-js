## 2026-02-24 — Test Infrastructure (Phase 0, Task 0.6)

### What was done
- Created `test/test-host.ts`: Tester instance using `createTester()` from `@typespec/compiler/testing`
  with `@typespec/http` and `@typespec/versioning` libraries pre-loaded.
- Created `test/utils.tsx`: `TestFile` wrapper component that wraps children in `<Output><SourceFile>`
  for rendering Alloy components in unit tests.
- Created `test/vitest.d.ts`: Type declarations for `toRenderTo` and `toRenderToAsync` custom matchers
  provided by `@alloy-js/core/testing`.
- Created `test/components/test-infrastructure.test.tsx`: 5 foundational tests validating the
  test infrastructure (compilation, rendering, code templates, TS declarations, matcher rejection).
- Updated `vitest.config.ts`: Added `test.include` to restrict test discovery to `test/` directory
  (prevents picking up tests from submodules).

### Pattern for writing tests
1. Import `"@alloy-js/core/testing"` (side-effect import registers matchers)
2. Use `Tester.createInstance()` then `runner.compile(t.code\`...\`)` to compile TypeSpec
3. Wrap components in `<TestFile program={program}>` for rendering
4. Assert with `expect(template).toRenderTo(\`expected output\`)`

### Notes for next person
- Task 0.1 (Add Dependencies) is also done — package.json already has all required deps.
- Next priorities: SDK Context Provider (0.3), Refkey Helpers (0.4), External Package Definitions (0.5),
  or Emitter Entry Point (0.2). These are all Phase 0 foundation tasks.
- The `TestFile` wrapper is intentionally minimal. It will need to be extended with context
  providers (e.g., `SdkContext`) once those components are created (Task 0.3).

## 2026-02-24 — SDK Context Provider (Phase 0, Task 0.3)

### What was done
- Created `src/context/sdk-context.tsx`: Named context (`SdkContext`), provider component
  (`SdkContextProvider`), and `useSdkContext()` hook for accessing TCGC SDK package data.
- Created `src/context/index.ts`: Barrel file exporting all context types and components.
- Updated `src/index.ts`: Re-exports context module.
- Updated `test/test-host.ts`:
  - Added `@azure-tools/typespec-client-generator-core` to Tester libraries.
  - Added `TesterWithService` pre-wrapped with `@service` namespace.
  - Added `createSdkContextForTest()` helper for creating TCGC contexts in tests.
- Updated `test/utils.tsx`: Added `SdkTestFile` wrapper that includes `SdkContextProvider`
  for testing components that depend on SDK context.
- Created `test/context/sdk-context.test.tsx`: 6 tests covering:
  - Providing sdkPackage data to child components
  - Exposing clients from SDK package
  - Exposing enums from SDK package
  - Throwing when used without provider
  - Consuming via raw `useContext()`
  - Exposing full sdkPackage object
- All 11 tests pass (5 infrastructure + 6 context).

### Architecture decisions
- Context value exposes `sdkPackage`, `clients`, `models`, `enums`, `unions` as typed accessors.
- `program` is NOT duplicated in our context — it's already available via `useTsp()` from
  `@typespec/emitter-framework`.
- TCGC `SdkContext` type is aliased as `TcgcSdkContext` to avoid name collision with our
  Alloy named context.

### Key learnings (see knowledge.md)
- TCGC prunes unreferenced models — always include operations that reference types in tests.
- Alloy JSX is lazy — use `renderToString()` to trigger throws in error-path tests.
- `EmitContext` requires a `perf` property — provide no-op mock in tests.

### Notes for next person
- Completed: 0.1 (Dependencies), 0.3 (SDK Context), 0.6 (Test Infrastructure).
- Next priorities: Refkey Helpers (0.4), External Package Definitions (0.5), Emitter Entry Point (0.2).
- The `SdkTestFile` wrapper and `createSdkContextForTest()` are ready for use in all future
  component tests that need TCGC data.
- The `TesterWithService` tester wraps TypeSpec in a `@service` namespace, which is needed for
  TCGC to produce clients. Use it instead of `Tester` for tests that call `createSdkContextForTest()`.

## 2026-02-24 — Refkey Helpers (Phase 0, Task 0.4)

### What was done
- Created `src/utils/refkeys.ts`: 14 refkey helper functions covering all artifact types
  needed by the emitter — types, serializers, deserializers, polymorphic types, known values,
  operation options, client artifacts (context, factory, classical class), XML serializers/
  deserializers, and utility helpers (serialization, paging, polling).
- Created `src/utils/index.ts`: Barrel file exporting all refkey helpers.
- Updated `src/index.ts`: Re-exports the utils module.
- Created `test/utils/refkeys.test.tsx`: 8 tests covering:
  - Identity/memoization (same entity → same refkey)
  - Uniqueness (different discriminators → different refkeys)
  - Entity isolation (different entities → different refkeys)
  - Helper name isolation (different names → different refkeys)
  - Cross-category isolation (same name, different category → different refkeys)
  - Base refkey vs discriminated refkeys
  - XML vs JSON serializer refkey distinction
  - Client-related refkey distinction (context, factory, classical)
- All 19 tests pass (8 refkey + 5 infrastructure + 6 context).

### Architecture decisions
- Entity-based helpers (typeRefkey, serializerRefkey, etc.) accept `unknown` entities — they
  wrap `refkey(entity, discriminator)` from `@alloy-js/core` with descriptive discriminator strings.
- `typeRefkey(entity)` uses no discriminator — it returns `refkey(entity)` directly, making it
  the "identity" refkey for any entity.
- String-based helpers (serializationHelperRefkey, pagingHelperRefkey, pollingHelperRefkey)
  accept a name string and use a category prefix as the first argument to prevent cross-category
  collisions (e.g., `refkey("serializationHelper", name)`).
- Discriminator strings follow the legacy emitter's naming convention for consistency.

### Notes for next person
- Completed: 0.1 (Dependencies), 0.3 (SDK Context), 0.4 (Refkey Helpers), 0.6 (Test Infrastructure).
- Next priorities: External Package Definitions (0.5), Emitter Entry Point (0.2).
- After 0.5, Phase 0 is complete and Phase 1 (Type Components) can begin.
- All refkey helpers are exported from `src/index.ts` — import them from the package root.
- When implementing components, use these helpers instead of calling `refkey()` directly with
  raw strings. This ensures consistent discriminator naming across the codebase.

## 2026-02-24 — External Package Definitions (Phase 0, Task 0.5)

### What was done
- Created `src/utils/external-packages.ts`: 8 external package definitions using Alloy's
  `createPackage()` for all runtime packages the emitter references.
- Updated `src/utils/index.ts`: Added exports for all 8 package definitions.
- Updated `test/utils.tsx`: Added optional `externals` prop to `TestFile` and `SdkTestFile`
  wrappers so tests can register external packages for refkey resolution.
- Created `test/utils/external-packages.test.tsx`: 11 tests covering:
  - httpRuntimeLib exposes all 16 core runtime symbols
  - httpRuntimeLib symbols generate correct imports in rendered output
  - Multiple symbols from same package grouped into single import
  - azureCoreClientLib exposes Azure-specific client symbols
  - azureCoreClientLib generates imports from @azure-rest/core-client
  - Azure supplementary packages expose expected symbols (pipeline, abort, util, auth)
  - Azure supplementary packages generate imports from correct packages
  - azureCoreLroLib exposes all 4 LRO symbols
  - azureCoreLroLib generates imports from @azure/core-lro
  - azureIdentityLib exposes DefaultAzureCredential
  - Multiple packages can be registered and used together
- All 30 tests pass (11 new + 19 existing).

### Packages defined
1. `httpRuntimeLib` — `@typespec/ts-http-runtime` (16 symbols: Client, ClientOptions, getClient,
   Pipeline, OperationOptions, operationOptionsToRequestParameters, StreamableMethod,
   PathUncheckedResponse, RestError, createRestError, ErrorModel, AbortSignalLike,
   uint8ArrayToString, stringToUint8Array, KeyCredential, isKeyCredential, TokenCredential)
2. `azureCoreClientLib` — `@azure-rest/core-client` (11 symbols)
3. `azureCorePipelineLib` — `@azure/core-rest-pipeline` (Pipeline)
4. `azureAbortControllerLib` — `@azure/abort-controller` (AbortSignalLike)
5. `azureCoreUtilLib` — `@azure/core-util` (uint8ArrayToString, stringToUint8Array)
6. `azureCoreAuthLib` — `@azure/core-auth` (KeyCredential, isKeyCredential, TokenCredential)
7. `azureCoreLroLib` — `@azure/core-lro` (PollerLike, OperationState, deserializeState,
   ResourceLocationConfig)
8. `azureIdentityLib` — `@azure/identity` (DefaultAzureCredential)

### Key learnings
- Alloy import ordering is determined by the order externals are registered and symbols
  are encountered during rendering, NOT alphabetical by package name. Tests must match
  the actual rendering order.
- The `TestFile` and `SdkTestFile` wrappers now accept an optional `externals` prop,
  allowing component tests to register external packages without creating custom Output
  wrappers.

### Notes for next person
- Completed: 0.1 (Dependencies), 0.3 (SDK Context), 0.4 (Refkey Helpers),
  0.5 (External Package Definitions), 0.6 (Test Infrastructure).
- Phase 0 is now complete except for 0.2 (Emitter Entry Point).
- 0.2 (Emitter Entry Point) can be implemented now, but Phase 1 (Type Components)
  can also proceed since all foundation infrastructure is in place.
- When generating code that references runtime types, use the package refkeys:
  ```tsx
  code`const client: ${httpRuntimeLib.Client} = ${httpRuntimeLib.getClient}(endpoint);`
  ```
  Alloy will auto-generate `import { Client, getClient } from "@typespec/ts-http-runtime";`
- For Azure-flavored SDKs, swap `httpRuntimeLib` for `azureCoreClientLib` and the
  supplementary Azure packages. The symbol names are the same logical concepts but
  import from different packages.

## 2026-02-24 — Type Expression Component (Phase 1, Task 1.1)

### What was done
- Created `src/components/type-expression.tsx`: `getTypeExpression()` function that maps
  TCGC `SdkType` to TypeScript type expressions (Alloy `Children`).
- Updated `src/components/index.ts`: Exports `getTypeExpression` from barrel file.
- Created `test/components/type-expression.test.tsx`: 13 tests covering:
  - Primitive scalars (string, int32, float32, boolean, bytes, utcDateTime)
  - All numeric types (int8, int16, int64, uint8-64, float64) → number
  - plainDate → Date
  - offsetDateTime → string
  - duration → string (default encoding)
  - Array types → (T)[] syntax
  - Dictionary types → Record<string, T>
  - Nullable types → T | null
  - Nested composite types (int32[][] → ((number)[])[] )
  - Model reference via refkey (renders as model name)
  - Enum reference via refkey (renders as enum name)
  - String constant literals → quoted strings
  - Numeric constant literals → unquoted numbers
- All 43 tests pass (13 new + 30 existing).

### Architecture decisions
- **Function, not JSX component**: `getTypeExpression` returns `Children` (string, refkey,
  or code template) rather than being a JSX component. This is because it's a type
  mapping utility consumed by other components, not a standalone renderable unit.
- **Legacy emitter parity**: All type mappings match the legacy emitter's behavior exactly.
  Notably, int64/uint64 → "number" (not bigint) for output parity with existing SDKs.
- **No context dependency**: The function is pure — it only needs the SdkType, not any
  Alloy context. This makes it easy to test and compose.
- **Refkeys for named types**: Model, enum, and union types return `typeRefkey(entity)`
  which enables Alloy's automatic cross-file import resolution.

### Notes for next person
- Completed Phase 0: 0.1, 0.3, 0.4, 0.5, 0.6. Phase 0 remaining: 0.2 (Emitter Entry Point).
- Completed Phase 1.1 (Type Expression).
- Next priorities: 1.2 (Model Interface), 1.3 (Enum Declaration), 1.4 (Union Declaration).
  All three depend on 1.1 (Type Expression) which is now available.
- `getTypeExpression` is exported from `src/index.ts` — import from the package root.
- The function handles `credential` type as a placeholder (returns "string"). Full
  credential type expression will be implemented in Phase 4 (Client Infrastructure).
- For testing type expressions with reference types (model, enum), you need to provide
  a declaration with the matching refkey in the same render tree so the refkey resolves.

## 2026-02-24 — Model Interface Component (Phase 1, Task 1.2)

### What was done
- Created `src/components/model-interface.tsx`: `ModelInterface` JSX component that generates
  `<InterfaceDeclaration>` from TCGC `SdkModelType`.
- Updated `src/components/index.ts`: Added `ModelInterface` export.
- Created `test/components/model-interface.test.tsx`: 9 tests covering:
  - Basic model with string/number/boolean properties
  - Optional properties rendered with `?` modifier
  - Readonly properties (visibility=[Read]) rendered with `readonly`
  - Base model inheritance via `extends` clause using refkey
  - Discriminator property on subtype renders literal `discriminatorValue` type
  - additionalProperties render as index signature `[key: string]: T`
  - JSDoc documentation on interface from model.doc
  - JSDoc documentation on members from property.doc
  - Cross-model reference types resolved via refkey
- All 52 tests pass (9 new + 43 existing).

### Architecture decisions
- **JSX component, not function**: Unlike `getTypeExpression()` (a function), `ModelInterface`
  is a proper JSX component because it renders structural output (`<InterfaceDeclaration>`)
  that composes child components. It's a middle-tier component in the component hierarchy.
- **Helper functions**: Internal logic split into small, documented helper functions:
  - `getExtendsClause()` — builds `extends` clause from `model.baseModel` via refkey
  - `getModelDoc()` — extracts doc string from `model.doc` or `model.summary`
  - `getExpandedProperties()` — collects properties with flatten expansion
  - `getPropertyTypeExpression()` — resolves property type, handling discriminator literals
  - `isReadOnly()` — checks visibility for read-only detection (reimplements TCGC's internal utility)
- **Flattened properties**: When `property.flatten === true` and the property type is a model,
  the nested model's properties are expanded inline. Children inherit the wrapper's optionality.
- **Discriminator handling**: On subtypes (models with `discriminatorValue`), the discriminator
  property type becomes the string literal `"value"` instead of the general type.
- **additionalProperties**: Rendered as a separate `<InterfaceMember indexer=...>` after the
  `<For>` block for regular properties, with a newline separator.

### Key learnings (see knowledge.md)
- Alloy JSDoc always renders multi-line format (`/**\n * text\n */`), not single-line.
- TCGC subtypes only contain their own properties, not inherited ones from baseModel.
- Index signature members outside `<For>` don't get automatic trailing semicolons.

### Notes for next person
- Completed: 0.1, 0.3, 0.4, 0.5, 0.6 (Phase 0 complete except 0.2), 1.1, 1.2 (Phase 1).
- Next priorities: 1.3 (Enum Declaration), 1.4 (Union Type), 1.5 (Polymorphic Type), 1.6 (ModelFiles Orchestrator).
- The `ModelInterface` component is exported from `src/index.ts` — import from package root.
- When rendering a model with base model in a different file, Alloy auto-generates imports
  because the extends clause uses a refkey reference.
- The `isReadOnly()` helper in model-interface.tsx reimplements TCGC's internal function
  since it's not publicly exported. If TCGC exports it in the future, switch to their version.
- The component does NOT handle polymorphic union types (Task 1.5) — that's a separate component
  that generates the `XxxUnion = SubType1 | SubType2 | BaseType` type alias.

## 2026-02-24 — Enum Declaration Component (Phase 1, Task 1.3)

### What was done
- Created `src/components/enum-declaration.tsx`: `EnumDeclaration` JSX component that generates
  two TypeScript declarations from TCGC `SdkEnumType`:
  1. Type alias (`type Name = "val1" | "val2" | ...`) for fixed enums, or
     `type Name = string` for extensible enums (isFixed=false).
  2. Known-values enum (`enum KnownName { Val1 = "val1", ... }`) documenting
     all values the service currently accepts.
- Updated `src/components/index.ts`: Added `EnumDeclaration` export.
- Created `test/components/enum-declaration.test.tsx`: 7 tests covering:
  - Fixed string enum (type alias with union literals + KnownXXX enum)
  - Fixed numeric enum (type alias with number literals + KnownXXX enum)
  - Extensible enum (type alias = base type + KnownXXX enum)
  - JSDoc documentation from enum.doc on both declarations
  - Member-level JSDoc from member.doc
  - Cross-reference via refkey from model properties
  - Single-value enum (edge case for join logic)
- Updated PRD: FR3 (Enum Type Generation) marked as implemented.
- All 59 tests pass (7 new + 52 existing).

### Architecture decisions
- **Two declarations per enum**: Every enum produces both a type alias and a Known enum,
  following the legacy emitter's pattern for output parity. The type alias is the primary
  type (referenced via `typeRefkey(enum)`), while the Known enum documents known values
  (referenced via `knownValuesRefkey(enum)`).
- **Refkey usage**: `typeRefkey(enum)` is on the type alias, `knownValuesRefkey(enum)` on
  the Known enum. When model properties reference an enum, they resolve to the type alias.
- **Extensibility via isFixed**: Fixed enums (`isFixed=true`) produce a union of literals.
  Extensible enums (`isFixed=false`) produce the base type (e.g., `string`) to allow
  arbitrary values. This follows the TCGC type model directly.
- **Documentation pattern**: Both declarations get doc from `enum.doc` when available.
  Without doc, the type alias gets "Type of {Name}" and the Known enum gets a description
  linking to the type alias via `{@link}`. Members get `member.doc` or fall back to the
  literal value string.

### Key learnings (see knowledge.md)
- Native TypeSpec enums are always `isFixed=true` in TCGC. Extensible enums require
  Azure-specific union-as-enum patterns. Tests simulate extensibility by overriding isFixed.
- Alloy's `<EnumMember jsValue={...}>` correctly renders string values in quotes and
  numeric values bare, matching TypeScript enum syntax.

### Notes for next person
- Completed: 0.1, 0.3, 0.4, 0.5, 0.6 (Phase 0 complete except 0.2), 1.1, 1.2, 1.3 (Phase 1).
- Next priorities: 1.4 (Union Declaration), 1.5 (Polymorphic Type), 1.6 (Model Files Orchestrator).
- After 1.4, Phase 1 type components are complete and Phase 2 (Serialization) can begin.
- The `EnumDeclaration` component is exported from `src/index.ts` — import from package root.
- The `<For>` component with `comma hardline enderPunctuation` works correctly inside
  `<EnumDeclaration>` for rendering enum members with trailing commas.
- For testing extensible enums without Azure libraries, spread the enum type and override
  `isFixed: false` — see the test for the pattern.

## 2026-02-24 — Union Type Declaration (Phase 1, Task 1.4)

### What was done
- Created `src/components/union-declaration.tsx`: `UnionDeclaration` JSX component that generates
  a `<TypeDeclaration>` type alias from TCGC `SdkUnionType`.
- Updated `src/components/index.ts`: Added `UnionDeclaration` export.
- Created `test/components/union-declaration.test.tsx`: 7 tests covering:
  - Union of string and integer types (string | number)
  - Union of three mixed types (string | number | boolean)
  - JSDoc documentation from union.doc
  - Fallback "Alias for {Name}" when no doc
  - Single-variant union (edge case for joiner)
  - Union with model type variants resolved via refkey
  - Cross-reference via refkey from model properties
- Updated PRD: FR4 (Union Type Generation) marked as implemented.
- All 66 tests pass (7 new + 59 existing).

### Architecture decisions
- **Single declaration**: Unlike enums (which produce two declarations), unions produce
  a single type alias: `export type Name = A | B | C`. This matches the legacy emitter.
- **`<For>` with `joiner=" | "`**: Uses Alloy's `<For>` component with custom joiner
  for pipe-separated variant types. This correctly handles both plain string types
  and refkey references (for model/enum variants).
- **`getTypeExpression()` for variants**: Each variant type is resolved through the
  shared type expression mapper, reusing the same logic as model properties, function
  parameters, etc. This ensures consistent type rendering across the emitter.
- **Documentation pattern**: `type.doc ?? "Alias for ${type.name}"` follows the legacy
  emitter's convention.

### Key learnings (see knowledge.md)
- TCGC converts string-literal unions (e.g., `"a" | "b"`) into `SdkEnumType`,
  NOT `SdkUnionType`. Only mixed-type unions remain as `SdkUnionType`.
- `sdkPackage.unions` has type `(SdkNullableType | SdkUnionType)[]` — cast needed
  in tests when accessing individual elements.

### Notes for next person
- Completed: 0.1, 0.3, 0.4, 0.5, 0.6 (Phase 0 complete except 0.2), 1.1, 1.2, 1.3, 1.4 (Phase 1).
- Next priorities: 1.5 (Polymorphic Type), 1.6 (Model Files Orchestrator).
- After 1.5 and 1.6, Phase 1 is complete and Phase 2 (Serialization) can begin.
- The `UnionDeclaration` component is exported from `src/index.ts` — import from package root.
- When testing union declarations, use mixed-type unions (string + int32 + boolean, or
  model variants). String-literal unions are handled by `EnumDeclaration` since TCGC
  promotes them to enums.
- The `ModelInterface` component uses prop `model` (not `type`). If you render
  `<ModelInterface type={...} />`, the model prop will be undefined and it will crash
  silently at runtime. Use `<ModelInterface model={...} />`.

## 2026-02-24 — Polymorphic Type Component (Phase 1, Task 1.5)

### What was done
- Created `src/components/polymorphic-type.tsx`: `PolymorphicType` JSX component that generates
  a `<TypeDeclaration>` union type alias from TCGC `SdkModelType` with discriminated subtypes.
- Exported `getDirectSubtypes()` utility function for use by other components (e.g., ModelFiles orchestrator).
- Updated `src/components/index.ts`: Added `PolymorphicType` and `getDirectSubtypes` exports.
- Created `test/components/polymorphic-type.test.tsx`: 7 tests covering:
  - Basic discriminated model produces union of subtypes + base model
  - Only direct subtypes are included (multi-level hierarchy filtering)
  - JSDoc documentation from model.doc on the type alias
  - Returns undefined when model has no discriminated subtypes
  - Polymorphic type is referenceable from other expressions via refkey
  - Single subtype renders correctly
  - getDirectSubtypes returns empty array for non-discriminated model
- Updated PRD: FR2 (Model Type Generation) marked as implemented.
- All 73 tests pass (7 new + 66 existing).

### Architecture decisions
- **Naming convention**: Union type alias uses `{ModelName}Union` suffix (e.g., `PetUnion`).
  The base model interface keeps its original name via `ModelInterface`. This follows the
  implementation plan's convention.
- **Direct subtypes only**: Uses `getDirectSubtypes()` which filters `discriminatedSubtypes`
  by `subtype.baseModel === model`. This prevents including transitive subtypes in multi-level
  hierarchies (each level generates its own polymorphic union).
- **Base model at end**: The union includes all direct subtypes followed by the base model
  at the end: `type PetUnion = Cat | Dog | Pet`. This serves as a fallback for unknown
  discriminator values.
- **Refkey**: Uses `polymorphicTypeRefkey(model)` from the shared refkey helpers, distinct
  from `typeRefkey(model)` which identifies the base model interface.
- **Exported utility**: `getDirectSubtypes()` is exported because the ModelFiles orchestrator
  (Task 1.6) needs it to determine which models require polymorphic type aliases.

### Key learnings (see knowledge.md)
- `discriminatedSubtypes` is a `Record<string, SdkModelType>`, not an array. Use
  `Object.values()` to iterate. Keys are discriminator string values.
- Multi-level hierarchies may include transitive subtypes in the map — always filter
  by `baseModel === model` for direct subtypes only.
- Test assertions must match actual TCGC output, not assumed TypeSpec properties.

### Notes for next person
- Completed: 0.1, 0.3, 0.4, 0.5, 0.6 (Phase 0 complete except 0.2), 1.1, 1.2, 1.3, 1.4, 1.5 (Phase 1).
- Next priorities: 1.6 (Model Files Orchestrator), then Phase 2 (Serialization).
- After 1.6, Phase 1 is complete. Phase 2 (FR5 JSON Serialization) is the most critical
  remaining P0 feature.
- The `PolymorphicType` component is exported from `src/index.ts` — import from package root.
- When building the ModelFiles orchestrator (1.6), use `getDirectSubtypes(model).length > 0`
  to determine if a model needs a polymorphic type alias alongside its interface.
- The component returns `undefined` for non-discriminated models, so it's safe to
  render `<PolymorphicType model={anyModel} />` without conditional checks.

## 2026-02-24 — ModelFiles Orchestrator (Phase 1, Task 1.6)

### What was done
- Created `src/components/model-files.tsx`: `ModelFiles` JSX component that orchestrates
  all type declarations into a `models/models.ts` source file inside a `models/` directory.
- Helper components: `ModelDeclarations`, `EnumDeclarations`, `UnionDeclarations`,
  `ModelWithPolymorphic` — each responsible for rendering its category of types.
- Updated `src/components/index.ts`: Added `ModelFiles` export.
- Created `test/components/model-files.test.tsx`: 8 tests covering:
  - Basic model renders into models/models.ts
  - Enums-only service renders enum declarations
  - Models render before enums (ordering)
  - Polymorphic types rendered after their base model
  - Empty service produces no output
  - Union declarations rendered correctly
  - Multiple models separated by blank lines
  - Cross-model references resolved in same file
- All 81 tests pass (8 new + 73 existing).

### Architecture decisions
- **Single file for now**: All types go into `models/models.ts`. Namespace grouping
  is deferred to a future task since it requires deeper TCGC namespace analysis.
- **Same SourceFile for serializers**: The component is structured so that serializers
  (Phase 2) will be added to the same `<SourceFile>`, preventing the self-import bug
  that is a well-known Alloy pitfall.
- **Union filtering**: `sdkPackage.unions` contains `SdkNullableType | SdkUnionType`.
  The component filters to only `SdkUnionType` (kind === "union") for rendering.
- **Declaration order**: Models (with polymorphic types) → Enums → Unions, matching
  the legacy emitter's convention.
- **Empty skip**: Returns undefined when no types exist, so no empty files/dirs.

### Key learnings (see knowledge.md)
- `t.model()` markers only work for TypeSpec `model` entities; use plain strings for
  enums and unions in test templates.
- TCGC may not include return-type-only unions in `sdkPackage.unions` — use them as
  input parameters in tests.
- Multiple operations with same HTTP route cause compilation errors — use `@route()`
  decorator to differentiate.
- Multi-file output testing: use `toRenderTo({ "path/file.ts": content })` with a
  custom wrapper that doesn't include its own SourceFile.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3, 0.4, 0.5, 0.6), Phase 1 (1.1–1.6). Phase 1 is complete.
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- **Next priority: Phase 2 (Serialization)** — FR5 (JSON Serialization) is the highest
  priority P0 feature remaining. Phase 2 depends on Phase 1 which is now complete.
- The `ModelFiles` component is exported from `src/index.ts` — import from package root.
- When implementing serializers (Phase 2), add them inside the `<SourceFile>` in
  `ModelFiles` AFTER the type declarations. The structure is ready for this.
- The `ModelFilesTestWrapper` in the test file shows how to test components that create
  their own SourceDirectory/SourceFile — reuse this pattern for future orchestrator tests.

## 2026-02-24 — JSON Serialization (Phase 2, Tasks 2.1 & 2.2)

### What was done
- Created `src/components/serialization/json-serializer.tsx`: `JsonSerializer` JSX component
  that generates `export function {name}Serializer(item: {Type}): any { return { ... }; }`
  functions from TCGC `SdkModelType`.
- Created `src/components/serialization/json-deserializer.tsx`: `JsonDeserializer` JSX component
  that generates `export function {name}Deserializer(item: any): {Type} { return { ... }; }`
  functions from TCGC `SdkModelType`.
- Created `src/components/serialization/index.ts`: Barrel file exporting all serialization components
  and utilities.
- Updated `src/components/index.ts`: Added exports for serialization components.
- Updated `src/components/model-files.tsx`: Integrated serializers and deserializers into the
  same `<SourceFile>` as type declarations, with Usage flag filtering:
  - Serializers generated for models with `UsageFlags.Input`
  - Deserializers generated for models with `UsageFlags.Output` or `UsageFlags.Exception`
- Updated `test/components/model-files.test.tsx`: Three existing tests updated to include
  expected deserializer output (since test models have Output usage from return types).
- Created `test/components/serialization/json-serializer.test.tsx`: 8 tests covering:
  - Basic model with simple properties
  - Client name to wire name mapping (@encodedName)
  - Optional nested model properties with null-check guard
  - Required nested model calling child serializer directly
  - Array of models with .map() and child serializer
  - Simple array passthrough without transformation
  - Serializer referenceable via serializerRefkey
  - Optional simple properties without null check
- Created `test/components/serialization/json-deserializer.test.tsx`: 7 tests covering:
  - Basic model with simple properties
  - Wire name to client name mapping (@encodedName)
  - Optional nested model properties with null-check guard
  - Required nested model calling child deserializer directly
  - Array of models with .map() and child deserializer
  - Deserializer referenceable via deserializerRefkey
  - Return type referencing model interface
- Updated PRD: FR5 (JSON Serialization) marked as implemented.
- All 96 tests pass (15 new + 81 existing).

### Architecture decisions
- **Alloy ObjectExpression/ObjectProperty**: Used Alloy's structural components for the
  return object literal instead of raw `code` templates. This ensures proper indentation,
  property name quoting (via `PropertyName`), and consistent formatting with Alloy's
  rendering engine.
- **Same SourceFile**: Serializers and deserializers are rendered in the same `models.ts`
  file as type declarations. This prevents the well-known Alloy self-import bug where
  splitting declarations and their serializers causes circular imports.
- **Usage flag filtering**: Models are filtered by `UsageFlags.Input` for serializers
  and `UsageFlags.Output | UsageFlags.Exception` for deserializers, matching the legacy
  emitter's behavior.
- **Null-check pattern**: Optional/nullable properties with complex types (models, dates,
  bytes) get wrapped with `!value ? value : serialize(value)`. Simple passthrough types
  (string, number, boolean) skip the guard since undefined passes through safely.
- **Exported utilities**: `getSerializationExpression()`, `getDeserializationExpression()`,
  and `needsTransformation()` are exported for reuse by future components (e.g.,
  polymorphic serializers, operation request builders).

### Key learnings (see knowledge.md)
- Alloy's ObjectExpression with ObjectProperty handles proper indentation and property
  name quoting automatically — prefer this over raw `code` templates for object literals.
- UsageFlags is a bitmask — use `(model.usage & UsageFlags.Input) !== 0` not `===`.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3, 0.4, 0.5, 0.6), Phase 1 (1.1–1.6), Phase 2 (2.1, 2.2).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.3 (Polymorphic Serialization), 2.4 (XML Serializers), 2.5 (Static Helpers).
- **Next priorities**: 2.3 (Polymorphic Serialization) or 0.2 (Emitter Entry Point).
  - 2.3 builds directly on the serializers and is the last P0 serialization piece.
  - 0.2 is needed to wire everything into a working emitter.
- The serialization components use Alloy's `ObjectExpression` and `ObjectProperty` — if you
  need to add new object literal generation elsewhere, use these instead of raw code templates.
- `getSerializationExpression()` and `getDeserializationExpression()` are generic type-based
  expression generators. They can be extended for new type kinds (e.g., XML types, LRO types)
  by adding cases to the switch statement.
- Date serialization currently uses `.toISOString()` for all datetime types. Unix timestamp
  encoding is handled but not yet tested. Bytes use `uint8ArrayToString`/`stringToUint8Array`
  from the HTTP runtime library.
- Dictionary (Record) serialization generates a `serializeRecord()` call pattern but the
  static helper function itself is not yet implemented (Task 2.5).

## 2026-02-24 — Polymorphic Serialization (Phase 2, Task 2.3)

### What was done
- Created `src/components/serialization/json-polymorphic-serializer.tsx`: `JsonPolymorphicSerializer`
  JSX component that generates switch-based serializer functions for discriminated models.
  Also exports `buildPolymorphicSwitchBody()` utility shared by both serializer and deserializer.
- Created `src/components/serialization/json-polymorphic-deserializer.tsx`: `JsonPolymorphicDeserializer`
  JSX component that generates switch-based deserializer functions for discriminated models.
- Updated `src/components/serialization/index.ts`: Exported new components and shared utility.
- Updated `src/components/model-files.tsx`: Integrated polymorphic serializers/deserializers.
  Discriminated models are now separated from regular models — discriminated parents get
  polymorphic switch-based serializers/deserializers while non-discriminated models keep
  regular property-based serializers/deserializers.
- Created `test/components/serialization/json-polymorphic-serializer.test.tsx`: 5 tests covering:
  - Basic discriminated model switch with cases for each subtype
  - Client property name used for discriminator access (not wire name)
  - Parameter type uses polymorphic union type (PetUnion not Pet)
  - Serializer referenceable via serializerRefkey
  - Default case returns item as-is for unknown discriminators
- Created `test/components/serialization/json-polymorphic-deserializer.test.tsx`: 5 tests covering:
  - Basic discriminated model switch with cases for each subtype
  - Wire property name (serializedName) used for discriminator access
  - Return type uses polymorphic union type
  - Deserializer referenceable via deserializerRefkey
  - Default case returns item as-is for unknown discriminators
- Updated PRD: FR6 (Polymorphic Serialization) marked as implemented. Task 2.3 marked done.
- All 106 tests pass (10 new + 96 existing).

### Architecture decisions
- **Switch-based routing**: Polymorphic serializers use a `switch` statement on the
  discriminator property, with each case routing to the subtype's serializer/deserializer
  via refkey. This matches the legacy emitter's pattern.
- **Discriminator access**: Serializers use client property name (`item["kind"]`) since the
  input is a typed SDK object. Deserializers use wire property name (`item["shape_type"]`)
  since the input is raw JSON from the service.
- **Replaces regular serializer**: For discriminated parent models, the polymorphic
  serializer gets `serializerRefkey(model)`, so external code that references the
  serializer automatically gets the switch-based version. Regular property serializers
  are NOT generated for discriminated parents.
- **Default case**: Returns `item` as-is for unknown discriminator values, providing
  forward compatibility when the service adds new subtypes.
- **Shared utility**: `buildPolymorphicSwitchBody()` is exported and shared between
  serializer and deserializer, parameterized by the refkey function (serializerRefkey
  vs deserializerRefkey).
- **All subtypes in switch**: The switch includes ALL entries from `discriminatedSubtypes`
  (including transitive subtypes in multi-level hierarchies), not just direct subtypes.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3, 0.4, 0.5, 0.6), Phase 1 (1.1–1.6), Phase 2 (2.1, 2.2, 2.3).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers, P1), 2.5 (Static Helpers).
- **Next priorities**: 0.2 (Emitter Entry Point) or Phase 3 (Operations).
  - 0.2 is needed to wire everything into a working emitter.
  - Phase 3 (Operation Generation) is the next P0 set of features after serialization.
  - 2.4 (XML) is P1 and can be deferred.
  - 2.5 (Static Helpers) is needed before Phase 3 can be fully functional.
- The polymorphic serializers are integrated into `model-files.tsx` — discriminated models
  are automatically routed to the polymorphic components based on `discriminatorProperty`
  and `getDirectSubtypes()`.
- Subtypes (e.g., Cat, Dog) still get regular `JsonSerializer`/`JsonDeserializer` because
  they serialize their own properties. Only the discriminated parent (Pet) gets the switch.
- The existing serializer's inheritance handling (subtypes only serialize their own properties,
  not inherited ones) is a separate concern not addressed by FR6.

## 2026-02-24 — Operation Options Component (Phase 3, Task 3.1)

### What was done
- Created `src/components/operation-options.tsx`: `OperationOptionsDeclaration` component
  that generates `XXXOptionalParams` interfaces for each operation.
- Updated `src/components/index.ts`: Exports `OperationOptionsDeclaration`.
- Created `test/components/operation-options.test.tsx`: 8 tests covering:
  - Basic operation with optional query parameters
  - Interface extends OperationOptions from runtime package
  - Refkey referenceable via operationOptionsRefkey
  - JSDoc documentation from parameter descriptions
  - Required parameters excluded
  - Empty interface for no optional parameters
  - Optional header parameters included
  - Optional body parameter included (with model reference)
- Also updated PRD to mark tasks 1.2-1.5, 2.1-2.2 as done (they were already
  implemented but not tracked).
- All 114 tests pass (8 new + 106 existing).

### Component behavior
- **Naming**: `{OperationName}OptionalParams` (e.g., `GetUserOptionalParams`)
- **Extends**: `OperationOptions` from `@typespec/ts-http-runtime` via refkey
- **Included params**: Method-level params where `onClient=false`, `optional=true` (or
  has `clientDefaultValue`), not auto-generated contentType/accept, not API version
- **LRO ops**: Adds `updateIntervalInMs?: number` for `kind === "lro"` or `"lropaging"`
- **Dual-format body**: Adds `contentType?: string` when body supports both JSON and XML
- **Refkey**: Registered via `operationOptionsRefkey(method)`

### Key learnings (see knowledge.md)
- Alloy renders `import type` when symbols are only in type positions
- Empty interfaces render as `{}` on one line
- Component names must not collide with external package symbol names
- `@patch` decorator needs `#{implicitOptionality: true}` for optional bodies

### Notes for next person
- Completed: 0.1, 0.3, 0.4, 0.5, 0.6 (Phase 0), 1.1-1.6 (Phase 1), 2.1-2.3 (Phase 2), 3.1 (Phase 3).
- Next priorities: Send Function (3.2), Deserialize Function (3.3), Public Operation Function (3.4),
  Operations Orchestrator (3.5). These form the core operations pipeline.
- 3.2 (Send Function) is the logical next step — it generates `_sendXXX` functions that
  build HTTP requests from the operation's parameters and options.
- The `OperationOptionsDeclaration` uses `getTypeExpression()` for parameter types, so model
  types referenced in options need a corresponding `ModelInterface` declaration in the same
  output tree for refkey resolution to work.
- Component is named `OperationOptionsDeclaration` (not `OperationOptions`) to avoid name
  collision with `httpRuntimeLib.OperationOptions`.

## 2026-02-24 — Send Function Component (Phase 3, Task 3.2)

### What was done
- Created `src/components/send-operation.tsx`: `SendOperation` JSX component that generates
  `_xxxSend` functions for each operation. These functions build HTTP requests by:
  - Expanding URL templates via `expandUrlTemplate` for path/query parameters
  - Setting Content-Type and Accept headers based on operation metadata
  - Including custom header parameters in the headers object
  - Serializing request bodies via model serializer refkeys
  - Dispatching via `context.path(path).verb({...options})`
- Added `expandUrlTemplate` to `httpRuntimeLib` external package definition.
- Added `sendOperationRefkey` to `src/utils/refkeys.ts` for cross-component references.
- Updated `src/components/index.ts`: Exports `SendOperation`.
- Created `test/components/send-operation.test.tsx`: 8 tests covering:
  - Basic GET operation with accept header
  - Path parameters in URL template expansion
  - Query parameters from options bag
  - POST with model body serialization
  - Refkey referenceable via sendOperationRefkey
  - Custom header parameters included in headers object
  - Combined path and query parameters
  - Optional body with null-check guard
- Updated PRD: Task 3.2 marked as done.
- All 122 tests pass (8 new + 114 existing).

### Architecture decisions
- **Single-line return statement**: The return statement with `context.path(path).verb({...})`
  is rendered as a single line to avoid Alloy indentation issues with multi-line code templates.
  Alloy's raw `\n` characters in code templates don't receive context indentation, so multi-line
  formats caused inconsistent spacing.
- **expandUrlTemplate from runtime**: Added `expandUrlTemplate` to `httpRuntimeLib` instead of
  creating a static helper. The legacy emitter uses a static helper file, but for the new emitter
  treating it as a runtime import is cleaner. Task 2.5 can adjust this if needed.
- **Accept header always present**: The accept header is derived from response content types
  and is always included when the operation has typed responses. This matches the legacy emitter.
- **Parameter mapping via correspondingMethodParams**: HTTP parameters (path, query, header)
  are mapped back to method parameters via `correspondingMethodParams[0]` to determine the
  correct accessor expression (direct arg vs options bag).
- **Body serialization via refkey**: Body serialization uses `getSerializationExpression()` which
  returns Alloy Children containing refkeys. This is embedded in a `code` template so Alloy
  auto-resolves the serializer function reference and generates imports.

### Key learnings (see knowledge.md)
- Alloy `code` templates with raw `\n` produce inconsistent indentation — avoid them.
- Alloy combines imports from the same package into a single statement with inline `type`.
- Alloy `FunctionDeclaration` breaks parameters onto separate lines with 2+ params.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3), Phase 3 (3.1-3.2).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers, P1), 2.5 (Static Helpers).
- Phase 3 remaining: 3.3 (Deserialize Function), 3.4 (Public Operation Function),
  3.5 (Operations Orchestrator).
- **Next priorities**: 3.3 (Deserialize Function) is the logical next step — it generates
  `_xxxDeserialize` functions that process responses. Together with 3.2, it completes the
  request/response pipeline that 3.4 (Public Operation Function) wraps.
- The `SendOperation` component uses `correspondingMethodParams[0]` to map HTTP parameters
  back to method parameters. This works for simple cases but may need enhancement for
  multi-segment parameter paths (e.g., spread models).
- Collection format handling (csv, ssv, pipes) for query array parameters is not yet
  implemented. The basic array parameter handling uses the URL template expansion which
  handles the `{?param*}` explode pattern. Static helpers for other formats will come in 2.5.
- The body serialization uses `getSerializationExpression()` from the serialization module.
  For the body to resolve correctly, the model's serializer must be declared in the same
  output tree (via `JsonSerializer` or `JsonPolymorphicSerializer`).

## 2026-02-24 — Deserialize Operation Component (Phase 3, Task 3.3)

### What was done
- Created `src/components/deserialize-operation.tsx`: `DeserializeOperation` JSX component that generates
  `_xxxDeserialize` functions for each operation. These functions process HTTP responses by:
  - Checking response status code against expected status codes
  - Throwing `createRestError(result)` for unexpected status codes
  - Deserializing response body using model deserializer refkeys for model types
  - Mapping array elements through element deserializers for array-of-model types
  - Returning `result.body` directly for primitive types
  - Returning `void` for operations with no response body
- Added `deserializeOperationRefkey` to `src/utils/refkeys.ts` for cross-component references.
- Updated `src/components/index.ts`: Exports `DeserializeOperation`.
- Created `test/components/deserialize-operation.test.tsx`: 8 tests covering:
  - Void response (DELETE 204) with empty return
  - Model response with deserializer function call
  - Primitive response returning result.body directly
  - Multiple status codes (200 + 201) in expectedStatuses array
  - Refkey referenceable via deserializeOperationRefkey
  - Array of model response with .map() deserialization
  - POST returning a model response
  - Combined 200 + 204 status codes (optional body)
- Updated PRD: FR8 marked as in_progress (send + deserialize done), FR9 marked as implemented.
- All 130 tests pass (8 new + 122 existing).

### Architecture decisions
- **async prop handles Promise wrapping**: Alloy's `FunctionDeclaration` with `async` prop
  automatically wraps the `returnType` in `Promise<>`. Do NOT double-wrap with
  `returnType={code\`Promise<${type}>\`}` — just provide the raw inner type.
- **Status codes as strings**: Expected status codes are formatted as quoted strings (e.g.,
  `"200"`) because `PathUncheckedResponse.status` is a string type in the HTTP runtime.
- **LRO status code expansion**: For LRO operations (non-GET), additional polling status codes
  (200, 201, 202) are added to match the legacy emitter's behavior.
- **Reuse of deserialization helpers**: Uses `getDeserializationExpression()` and
  `needsTransformation()` from the serialization module, which handle models, arrays,
  dicts, dates, bytes, and nullable types.
- **Single return path**: The function has a single return statement after the status check,
  similar to the legacy emitter's pattern. Complex multi-return paths (like dual-format
  JSON/XML responses) will be added when XML deserialization is implemented (FR7).

### Key learnings (see knowledge.md)
- Alloy `async` FunctionDeclaration wraps returnType in Promise automatically — do not double-wrap.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3), Phase 3 (3.1-3.3).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers, P1), 2.5 (Static Helpers).
- Phase 3 remaining: 3.4 (Public Operation Function), 3.5 (Operations Orchestrator).
- **Next priorities**: 3.4 (Public Operation Function) is the logical next step — it generates
  public `xxx` functions that compose `_xxxSend` and `_xxxDeserialize` together. This is the
  final piece of the operations pipeline that consumers actually call.
- After 3.4, 3.5 (Operations Orchestrator) groups operations into files and wires everything.
- The `DeserializeOperation` component does not yet handle:
  - Error body deserialization (custom error types from TCGC exceptions)
  - Response header extraction
  - Binary/stream responses (bytes/Uint8Array)
  - Dual-format (JSON/XML) response deserialization
  These will be added as part of later tasks (XML support, error deserialization).

## 2026-02-24 — Public Operation Function Component (Phase 3, Task 3.4)

### What was done
- Created `src/components/public-operation.tsx`: `PublicOperation` JSX component that generates
  the public operation functions that consumers call directly. The component dispatches based
  on method kind (basic, lro, paging, lropaging):
  - **Basic**: Async function that awaits `_xxxSend()` then returns `_xxxDeserialize(result)`.
  - **LRO**: Non-async function returning `PollerLike<OperationState<T>, T>` via
    `getLongRunningPoller()` with options for polling interval, abort signal, and
    resource location config.
  - **Paging**: Non-async function returning `PagedAsyncIterableIterator<T>` via
    `buildPagedAsyncIterator()` with item name and next link metadata.
  - **LRO+Paging**: Combined pattern creating an initial poller then wrapping with
    paged iterator.
- Added `publicOperationRefkey` to `src/utils/refkeys.ts` for cross-component references.
- Exported `isRequiredSignatureParameter` and `isAutoGeneratedHeader` from
  `src/components/send-operation.tsx` for reuse (avoids code duplication).
- Updated `src/components/index.ts`: Exports `PublicOperation` and shared helpers.
- Created `test/components/public-operation.test.tsx`: 8 tests covering:
  - Basic GET operation with async send + deserialize composition
  - POST with model body forwarding all arguments correctly
  - Void return type (DELETE 204) with status validation
  - JSDoc documentation from TypeSpec operation description
  - Refkey referenceable via publicOperationRefkey
  - Multiple path parameters as required function arguments
  - Combined path + body parameter forwarding
  - Optional-only parameters (context + options signature)
- Updated PRD: Task 3.4 marked as done.
- All 138 tests pass (8 new + 130 existing).

### Architecture decisions
- **Same parameter signature as send function**: The public function has identical parameters
  to the send function (context, required params, options). This enables simple argument
  forwarding by building a call argument string.
- **Shared parameter helpers**: `isRequiredSignatureParameter()` and `isAutoGeneratedHeader()`
  are exported from `send-operation.tsx` and reused in `public-operation.tsx` to maintain
  consistency between the send and public function signatures.
- **LRO/Paging via refkeys**: LRO helpers (`getLongRunningPoller`) and paging helpers
  (`buildPagedAsyncIterator`, `PagedAsyncIterableIterator`) are referenced via
  `pollingHelperRefkey` and `pagingHelperRefkey`. These will resolve when static helper
  declarations are created (task 2.5).
- **Azure LRO types**: `PollerLike` and `OperationState` come from `@azure/core-lro`
  (`azureCoreLroLib`), matching the legacy emitter's behavior.
- **Paging metadata extraction**: Item name comes from `pageItemsSegments` last segment's
  `serializedName`. Next link name comes from `continuationTokenResponseSegments` last
  segment's `serializedName`.

### Key learnings (see knowledge.md)
- Alloy `FunctionDeclaration` with `doc` prop auto-generates `@param {Type}` JSDoc tags.
- When `doc` generates `@param` tags, referenced types lose the `type` import prefix.
- Array type expressions render as `(string)[]` instead of `string[]`.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.4).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers, P1), 2.5 (Static Helpers).
- Phase 3 remaining: 3.5 (Operations Orchestrator).
- **Next priorities**: 3.5 (Operations Orchestrator) groups operations into files under
  `src/api/`. This is the final piece that wires all operation components together into
  the output directory structure.
- After 3.5, Phase 4 (Client Infrastructure) becomes the next focus:
  - 4.1 (Client Context) generates the context interface + factory function.
  - 4.2 (Logger) generates Azure-specific logger module.
- The LRO and paging patterns in `PublicOperation` are implemented but untested with
  real TCGC paging/LRO metadata (requires Azure-specific TypeSpec decorators). The
  patterns match the legacy emitter's output and use correct refkeys. They will be
  validated when scenario tests (task 9) are ported.
- The `PublicOperation` component uses `method.doc` for JSDoc, which Alloy renders as
  multi-line format with auto-generated `@param` tags.

## 2026-02-24 — Operations Orchestrator (Phase 3, Task 3.5)

### What was done
- Created `src/components/operation-files.tsx`: `OperationFiles` JSX orchestrator component that
  groups and organizes operations by operation group path. The component:
  - Gets clients from the SDK context via `useSdkContext()`
  - Traverses the client hierarchy using BFS to collect operations grouped by path prefix
  - Renders each group as an `api/{group}/operations.ts` source file
  - Root-level operations go to `api/operations.ts`
  - Nested operation groups go to `api/{groupName}/operations.ts`
  - Each operation renders four declarations: OperationOptionsDeclaration, SendOperation,
    DeserializeOperation, PublicOperation
- Updated `src/components/index.ts`: Exports `OperationFiles`.
- Created `test/components/operation-files.test.tsx`: 8 tests covering:
  - Root-level operations rendering into api/operations.ts with all four declarations
  - Multiple operations in the same file separated by blank lines
  - Empty service producing no output
  - Void-returning operations with correct deserialize behavior
  - Model response operations with deserializer refkey cross-file references
  - Model body operations with serializer refkey cross-file references
  - Operation group operations rendering into subdirectories
  - Operations with path parameters generating expandUrlTemplate calls
- Updated PRD: Task 3.5 marked as done.
- All 146 tests pass (8 new + 138 existing).

### Architecture decisions
- **BFS traversal of client hierarchy**: The component uses a BFS queue (similar to the
  legacy emitter's `getMethodHierarchiesMap()`) to traverse `client.methods` and
  `client.children` recursively. Child clients extend the prefix path with their
  normalized name.
- **Single SourceFile per group**: Each operation group gets a single `operations.ts` file
  containing all four declarations per operation. This matches the legacy emitter's pattern
  and avoids Alloy self-import bugs.
- **Name normalization**: Operation group names are normalized to camelCase (first char
  lowercase) for directory paths, matching the legacy emitter's `normalizeName` convention.
- **Composable with ModelFiles**: The orchestrator can be rendered alongside `<ModelFiles />`
  in the same output tree, and Alloy auto-generates cross-file imports for serializer/
  deserializer references.

### Key learnings (see knowledge.md)
- `toRenderTo` with `expect.stringContaining` requires ALL output files to be specified.
- Empty `@service` namespace produces 0 clients in TCGC (not 1 empty client).
- `export async function` is not matched by `expect.stringContaining("export function")`.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.5).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers, P1), 2.5 (Static Helpers).
- **Next priorities**: 
  - 4.1 (Client Context) is the logical next step — it generates the context interface
    + factory function that operations consume via the `context: Client` parameter.
  - 0.2 (Emitter Entry Point) ties everything together with `$onEmit` hook.
  - After 4.1, Phase 5 (Classical Client) builds the class-based client wrapper.
- The `OperationFiles` component does not yet handle:
  - Nested operation groups deeper than 1 level (multi-level paths like `users/profiles`)
    are supported by the traversal logic but not yet tested with real TypeSpec input.
  - Operation groups created via `@operationGroup` decorator (depends on TCGC behavior).
- The test for operation groups uses a TypeSpec `interface Widgets {}` pattern, which
  TCGC may or may not flatten into the root client depending on version. The test is
  flexible — it checks both the root and subdirectory cases.

## 2026-02-24 — Client Context Component (Phase 4, Task 4.1)

### What was done
- Created `src/components/client-context.tsx`: Four components that generate the client
  context infrastructure:
  - `ClientContextDeclaration` — Interface `{Name}Context extends Client` with optional
    custom properties (API version, custom client params).
  - `ClientContextOptionsDeclaration` — Interface `{Name}ClientOptionalParams extends ClientOptions`
    with all optional client-initialization parameters.
  - `ClientContextFactory` — Factory function `create{Name}(endpoint, credential?, options?)`
    that constructs the endpoint URL, calls `getClient()`, and returns the typed context.
  - `ClientContextFile` — Orchestrator rendering all three into `api/{name}Context.ts`.
- Added `clientOptionsRefkey` to `src/utils/refkeys.ts` for the options interface.
- Updated `src/utils/index.ts`: Added exports for `clientOptionsRefkey` and other missing
  refkeys (`sendOperationRefkey`, `deserializeOperationRefkey`, `publicOperationRefkey`).
- Updated `src/components/index.ts`: Exports all new client context components and
  `getClientName` helper.
- Created `test/components/client-context.test.tsx`: 8 tests covering:
  - Context interface extending Client from HTTP runtime
  - Options interface extending ClientOptions from HTTP runtime
  - Factory function calling getClient with endpoint URL
  - Factory function refkey referenceable via createClientRefkey
  - Context interface refkey referenceable via clientContextRefkey
  - Options interface refkey referenceable via clientOptionsRefkey
  - Complete context file with all three declarations
  - API version included when operations use api-version parameter
- Updated PRD: Tasks 3.3 and 4.1 marked as done.
- All 154 tests pass (8 new + 146 existing).

### Architecture decisions
- **Naming convention matches legacy emitter**: Context interface is `{ClientName}Context`,
  options is `{TCGCClientName}OptionalParams`, factory is `create{ClientName}` where
  `ClientName` = TCGC name stripped of "Client" suffix.
- **Endpoint URL template handling**: Supports simple endpoints (single `{endpoint}` param),
  template endpoints with multiple parameters, and endpoints with default values.
  Optional template args are extracted from options with nullish coalescing defaults.
- **Credential type mapping**: Maps TCGC auth scheme types to TypeScript credential types:
  apiKey/http → KeyCredential, oauth2/openIdConnect → TokenCredential. Supports union
  types for multiple auth schemes.
- **Context members vs options members**: Context interface contains non-endpoint,
  non-credential params that need to be on the client object (e.g., apiVersion). Options
  interface contains all optional params consumers can configure at creation time.
- **getClient() call pattern**: Without credentials: `getClient(endpointUrl, options)`.
  With credentials: `getClient(endpointUrl, credential, options)`.

### Key learnings (see knowledge.md)
- TCGC only adds `apiVersion` to `clientInitialization.parameters` when operations
  explicitly use an api-version query parameter. The `@versioned()` decorator alone
  only populates `client.apiVersions` array.
- Alloy's `toRenderTo` matcher calls `dedent()` internally which expects a string —
  `expect.stringContaining()` doesn't work for single-file renders. Use `renderToString()`
  + `toContain()` instead.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.5), Phase 4 (4.1).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers), 2.5 (Static Helpers).
- Phase 4 remaining: 4.2 (Logger Component — Azure-only).
- **Next priorities**:
  - 0.2 (Emitter Entry Point) — ties everything together with `$onEmit` hook. Now that
    we have ModelFiles, OperationFiles, and ClientContextFile, the emitter can produce
    a functional output tree.
  - 5.1 (Classical Client) — generates the class-based client that wraps the context.
    Depends on 4.1 (done) and 3.5 (done).
  - 7.1/7.2 (Index Files) — generates barrel files for the output.
- The `ClientContextFactory` does NOT yet handle:
  - User-agent prefix configuration (will be added when emitter options are available)
  - Custom pipeline policies (e.g., custom API key auth headers)
  - Logger integration (depends on 4.2 Logger Component)
  - ARM/Azure-specific endpoint resolution (depends on 8.1 Azure Extensions)
  These will be enhanced in later phases when the corresponding infrastructure exists.

## 2026-02-24 — Classical Client Component (Phase 5, Task 5.1)

### What was done
- Created `src/components/classical-client.tsx`: Two components that generate the
  class-based client wrapper:
  - `ClassicalClientFile` — Orchestrator rendering the client class into
    `{camelCaseClientName}.ts` (e.g., `testServiceClient.ts`).
  - `ClassicalClientDeclaration` — The class declaration with:
    - Private `_client` field typed as the context interface (via `clientContextRefkey`)
    - Public readonly `pipeline: Pipeline` property
    - Constructor that calls the factory function (via `createClientRefkey`)
      and assigns pipeline from context
    - Operation methods that delegate to public API functions (via `publicOperationRefkey`)
      passing `this._client` as the context argument
- Updated `src/components/index.ts`: Exports `ClassicalClientFile` and
  `ClassicalClientDeclaration`.
- Created `test/components/classical-client.test.tsx`: 8 tests covering:
  - Basic class structure with _client field, pipeline property, and constructor
  - Constructor generating import for factory function via cross-file refkey
  - Operation method delegating to public API function
  - Void-returning operation with correct Promise<void> return type
  - Multiple operations rendered as separate class methods
  - Class referenceable via classicalClientRefkey
  - Path parameters included in method signature and forwarded correctly
  - ClassicalClientFile orchestrator with correct filename
- Updated PRD: Task 5.1 marked as done.
- All 162 tests pass (8 new + 154 existing).

### Architecture decisions
- **Not async methods**: Class methods are NOT async — they return the promise
  from the API function directly (`return foo(this._client, options)`). This
  avoids an unnecessary async wrapper and matches the legacy emitter's output.
- **Same constructor signature as factory**: The constructor has the same
  parameter signature as `createXxx()` (endpoint, credential, options) and
  simply forwards all arguments to the factory function.
- **Readonly pipeline via code template**: Alloy's `ClassField` component doesn't
  support `readonly`. The pipeline field is rendered using a `code` template:
  `code\`public readonly pipeline: \${httpRuntimeLib.Pipeline};\``
- **Credential helpers duplicated**: The credential type expression logic is
  duplicated from `client-context.tsx` to avoid tight coupling. A shared
  utility could be extracted in a future refactoring if needed.
- **Root-level methods only**: The classical client currently renders only
  root-level methods (from `client.methods`). Operation group getters and
  child client support will be added in task 5.2.

### Notes for next person
- Completed: Phase 0 (0.1, 0.3-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.5), Phase 4 (4.1), Phase 5 (5.1).
- Phase 0 remaining: 0.2 (Emitter Entry Point).
- Phase 2 remaining: 2.4 (XML Serializers), 2.5 (Static Helpers).
- Phase 4 remaining: 4.2 (Logger Component — Azure-only).
- Phase 5 remaining: 5.2 (Classical Operation Groups — interfaces, factory
  functions, and getters for grouped operations).
- **Next priorities**:
  - 5.2 (Classical Operation Groups) — extends the classical client with
    operation group getters that return typed operation group objects.
  - 0.2 (Emitter Entry Point) — ties everything together with `$onEmit`.
    Now we have ModelFiles, OperationFiles, ClientContextFile, and
    ClassicalClientFile — all components needed for a functional output.
  - 7.1/7.2 (Index Files) — generates barrel files for the output.
- The classical client does NOT yet handle:
  - Constructor overloads for subscriptionId (ARM scenarios) — will be added
    when Azure extension support (8.1) is implemented.
  - Operation group getters (depends on 5.2).
  - User-agent prefix configuration.
  - LRO/Paging operation methods may need special return type handling
    (not tested with real LRO/paging TCGC data yet).

## 2026-02-24 — Emitter Entry Point (Phase 0, Task 0.2)

### What was done
- Created `src/emitter.tsx`: The `$onEmit` async function that serves as the TypeSpec
  compiler hook. It:
  - Initializes TCGC via `createSdkContext(context)` to extract `sdkPackage`
  - Builds a declarative JSX component tree composing all existing orchestrators:
    `<Output>` → `<SdkContextProvider>` → `<SourceDirectory path="src">` →
    `<ModelFiles>`, `<OperationFiles>`, `<ClientContextFile>`, `<ClassicalClientFile>`
  - Configures `Output` with `createTSNamePolicy()`, `tsNameConflictResolver`, and
    `httpRuntimeLib` as the external package
  - Iterates over `sdkPackage.clients` to render per-client context and classical files
  - Calls `writeOutput()` from `@typespec/emitter-framework` to render and emit files
- Updated `src/index.ts`: Exports `$onEmit` from the package entry point.
- Created `test/components/emitter.test.tsx`: 6 tests covering:
  - Full integration with models, operations, context, and classical client files
  - Scalar-only operations producing operations and client files
  - Cross-file refkey references between models and operations
  - Empty/minimal service handling without errors
  - Enum declarations flowing through the full emitter tree
  - HTTP runtime symbol imports working end-to-end
- Updated PRD: Task 0.2 marked as done.
- All 168 tests pass (6 new + 162 existing).

### Architecture decisions
- **Uses emitter-framework's Output**: Instead of using Alloy's raw `Output` directly,
  the emitter uses `@typespec/emitter-framework`'s `Output` which wraps the Alloy
  `Output` with a `TspContext.Provider` for the TypeSpec program. This enables
  downstream components to use `useTsp()` if needed.
- **Per-client iteration**: Uses `<For each={sdkPackage.clients}>` to render
  `ClientContextFile` and `ClassicalClientFile` for each client, supporting
  multi-client services.
- **SourceDirectory wrapping**: All output is wrapped in `<SourceDirectory path="src">`
  to produce the `src/` prefix in the output directory structure.
- **Single externals registration**: Only `httpRuntimeLib` is registered as an external.
  Azure-specific packages will be added in Phase 8 (Azure Extension).

### Notes for next person
- **Phase 0 is now COMPLETE**: All tasks (0.1-0.6) are done.
- Phase 1 (complete), Phase 2 (2.1-2.3 done), Phase 3 (complete), Phase 4 (4.1 done),
  Phase 5 (5.1 done).
- **Remaining tasks by priority**:
  1. 5.2 (Classical Operation Groups) — extends the classical client with operation
     group getters for grouped operations
  2. 7.1/7.2 (Index Files) — generates barrel files for `index.ts` exports
  3. 2.4/2.5 (XML Serializers, Static Helpers) — advanced serialization
  4. 4.2 (Logger Component) — Azure-only logging
  5. 6.x (Advanced Features) — LRO, multipart, samples
  6. 8.x (Azure Extension) — Azure-specific composition
- The emitter can now be invoked by the TypeSpec compiler on any `.tsp` file.
  To test manually: configure the emitter in `tspconfig.yaml` and run `tsp compile`.
- The `$onEmit` function does NOT yet handle:
  - Emitter options (e.g., `package-name`, `flavor`) — will be added when emitter
    options schema is defined
  - Azure external packages (only `httpRuntimeLib` is registered)
  - Index/barrel files (Phase 7)
  - Operation group classes (Phase 5.2)

## 2026-02-24 — Classical Operation Groups (Phase 5, Task 5.2)

### What was done
- Created `src/components/classical-operation-groups.tsx`: Four components that generate
  the classical operation group infrastructure:
  - `ClassicalOperationGroupFiles` — Orchestrator that traverses the client hierarchy
    via BFS and generates `classic/{path}/index.ts` files for each child client.
  - `ClassicalOperationGroupFile` — Renders a single operation group file containing
    the interface and factory function.
  - `OperationGroupInterface` — Renders `{GroupName}Operations` interface with method
    signatures for operations and property references for nested child groups.
  - `OperationGroupFactory` — Renders `_get{GroupName}Operations(context)` factory
    function that returns an object implementing the interface, delegating each method
    to the corresponding public API function with context bound.
- Added `operationGroupInterfaceRefkey` and `operationGroupFactoryRefkey` to
  `src/utils/refkeys.ts` for cross-file reference resolution.
- Updated `src/components/classical-client.tsx`:
  - Added readonly properties for each child client (operation group) on the class.
  - Added JSDoc comments for each operation group property.
  - Constructor now initializes each group property via the factory function.
- Updated `src/emitter.tsx`: Added `ClassicalOperationGroupFiles` to the emitter
  component tree so operation group files are generated alongside other output.
- Updated `src/components/index.ts`: Exports all new operation group components.
- Updated `src/utils/index.ts`: Exports new refkey helpers.
- Created `test/components/classical-operation-groups.test.tsx`: 11 tests covering:
  - Interface with method signatures for leaf operations
  - Factory function with method delegates to public API functions
  - Nested group reference in interface (parent → child)
  - Nested factory composing child factory calls
  - Classical client class with readonly group properties initialized in constructor
  - File orchestrator producing correct `classic/{group}/index.ts` paths
  - Required parameters included in interface method signatures
  - Multiple operation groups at same level rendered correctly
  - Interface referenceable via operationGroupInterfaceRefkey
  - Factory referenceable via operationGroupFactoryRefkey
  - Returns undefined when client has no operation groups
- Updated PRD: Task 5.2 marked as done.
- All 179 tests pass (11 new + 168 existing).

### Architecture decisions
- **Child clients = operation groups**: TCGC represents operation groups as child clients
  via `client.children`. Each child client has its own `methods` and potentially its own
  `children` for nested groups. The `SdkClientAccessor` pattern is deprecated in favor
  of the `parent`/`children` hierarchy.
- **Naming convention matches legacy emitter**: Interface is `{ChildName}Operations`,
  factory is `_get{ChildName}Operations`, property is `camelCase(childName)`. The
  child client's name from TCGC is used directly (e.g., `Widgets` → `WidgetsOperations`).
- **Factory function delegates via refkeys**: Each method in the factory's return object
  references the public operation function via `publicOperationRefkey(method)`, enabling
  Alloy to auto-generate cross-file imports.
- **Context flows through factory chain**: All factory functions take the root client
  context as a parameter and pass it to both operation delegates and nested factory
  calls. This mirrors the legacy emitter's pattern where the RLC client flows through
  all operation group layers.
- **BFS traversal for file generation**: Same BFS queue pattern used in `operation-files.tsx`
  for collecting operation groups. Each child client is visited with its accumulated
  prefix path, producing the correct directory nesting.

### Notes for next person
- Completed: Phase 0 (0.1-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.5), Phase 4 (4.1), Phase 5 (5.1-5.2).
- **Phase 5 is now COMPLETE**: Both classical client (5.1) and operation groups (5.2) done.
- **Remaining tasks by priority**:
  1. 7.1/7.2 (Index Files) — generates barrel files for `index.ts` exports
  2. 2.4/2.5 (XML Serializers, Static Helpers) — advanced serialization
  3. 4.2 (Logger Component) — Azure-only logging
  4. 6.x (Advanced Features) — LRO, multipart, samples
  5. 8.x (Azure Extension) — Azure-specific composition
  6. 9.x (Test Porting & Validation) — scenario testing
- The operation group components do NOT yet handle:
  - LRO helper methods (`begin_*` and `begin_*_andWait`) — these are a deprecated
    pattern from the legacy emitter. May not be needed in the rewrite.
  - Reserved word escaping for operation group names — will be handled by Alloy's
    name policy if needed.

## 2026-02-24 — Index & Barrel Files (Phase 7, Tasks 7.1 & 7.2)

### What was done
- Created `src/components/index-file.tsx`: Five components that generate barrel/index files
  for the SDK output:
  - `RootIndexFile` — Generates `src/index.ts` with selective exports of model types,
    client classes, client context/options/factory, operation options, and public operation
    functions. Excludes serializers, deserializers, and `_prefixed` internal symbols.
  - `ModelsIndexFile` — Generates `models/index.ts` that re-exports type declarations
    (interfaces, enums, unions, polymorphic aliases) from `models.ts`, excluding
    serializer/deserializer functions.
  - `ApiIndexFile` — Generates `api/index.ts` that re-exports public operation functions,
    options interfaces, and client context symbols from operation and context files.
    Excludes `_send` and `_deserialize` private functions.
  - `ClassicIndexFile` — Generates `classic/index.ts` using Alloy's `BarrelFile`
    for automatic re-export discovery of operation group interfaces.
  - `IndexFiles` — Orchestrator that composes all index files (root, models, api, classic)
    based on the SDK package contents.
- Updated `src/components/index.ts`: Exports all new index components.
- Updated `src/emitter.tsx`: Added `IndexFiles` to the emitter component tree so
  index files are generated alongside all other output.
- Created `test/components/index-file.test.tsx`: 16 tests covering:
  - Root index exports model types from models/index.js
  - Root index exports enum type alias and KnownXxx enum
  - Root index exports classical client class
  - Root index exports client context, options, and factory
  - Root index exports operation options interfaces and public functions
  - Root index does NOT export serializer/deserializer functions
  - Root index does NOT export _prefixed internal symbols
  - Root index returns undefined when no clients exist
  - Root index exports operation group interfaces for clients with children
  - Models index exports types but not serializers
  - Models index returns undefined when no models exist
  - API index exports public operations and context symbols
  - API index does NOT export _prefixed internal functions
  - IndexFiles orchestrator produces all index files
  - IndexFiles returns undefined when no clients exist
  - IndexFiles exports polymorphic union type aliases
- Updated PRD: Tasks 7.1 and 7.2 marked as done.
- All 195 tests pass (16 new + 179 existing).

### Architecture decisions
- **Explicit named exports over BarrelFile**: The root index and subpath indexes use
  explicit `export { X } from "Y"` statements built from TCGC SDK data rather than
  Alloy's `BarrelFile` component. This is necessary because `BarrelFile` does `export *`
  which would expose internal symbols (serializers, deserializers, _prefixed functions).
- **Export names derived from TCGC data**: The export lists are built by iterating
  over `sdkPackage.models`, `sdkPackage.enums`, `sdkPackage.unions`, and client methods.
  Name derivation matches the conventions used in model-interface, enum-declaration,
  union-declaration, and operation-options components.
- **ClassicIndexFile uses BarrelFile**: Unlike other indexes, the classic directory
  contains only operation group interfaces (all public), so `BarrelFile` is appropriate.
- **No paging/LRO type exports yet**: Paging types (PagedAsyncIterableIterator, etc.)
  and LRO types (SimplePollerLike) are not yet exported because the paging/LRO helper
  components (Phase 6) have not been implemented. The index will be extended when those
  features are added.

### Notes for next person
- Completed: Phase 0 (0.1-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3),
  Phase 3 (3.1-3.5), Phase 4 (4.1), Phase 5 (5.1-5.2), Phase 7 (7.1-7.2).
- **Phase 7 is now COMPLETE**: Both root index (7.1) and subpath index (7.2) done.
- **Remaining tasks by priority**:
  1. 2.4/2.5 (XML Serializers, Static Helpers) — advanced serialization
  2. 4.2 (Logger Component) — Azure-only logging
  3. 6.x (Advanced Features) — LRO, multipart, samples
  4. 8.x (Azure Extension) — Azure-specific composition
  5. 9.x (Test Porting & Validation) — scenario testing
- The index components will need to be extended when:
  - Paging support (6.x) is implemented: add PagedAsyncIterableIterator, PageSettings,
    ContinuablePage exports conditionally
  - LRO support (6.x) is implemented: add SimplePollerLike export conditionally
  - Azure flavor (8.x) is implemented: add Azure cloud type exports for ARM scenarios
  - Multipart support (6.2) is implemented: add FileContents export conditionally

## 2026-02-24 — Static Serialization Helpers (Phase 2, Task 2.5)

### What was done
- Created `src/components/static-helpers/` directory with four files:
  - `serialization-helpers.tsx`: 14 helper functions — `serializeRecord`, `deserializeRecord`,
    6 collection builders (csv, multi, pipe, ssv, tsv, newline), 4 collection parsers
    (csv, pipe, ssv, newline), and `areAllPropsUndefined`. Each function is registered
    with a `serializationHelperRefkey` for cross-file reference resolution.
  - `paging-helpers.tsx`: `PageSettings` interface, `PagedAsyncIterableIterator` interface,
    `BuildPagedAsyncIteratorOptions` interface, and `buildPagedAsyncIterator` function.
    Each registered with `pagingHelperRefkey` for cross-file imports.
  - `polling-helpers.tsx`: `OperationState` interface, `PollerLike` interface,
    `GetLongRunningPollerOptions` interface, and `getLongRunningPoller` async function.
    Each registered with `pollingHelperRefkey` for cross-file imports.
  - `index.tsx`: `StaticHelpers` orchestrator component that renders all three helper files.
- Fixed `src/components/serialization/json-serializer.tsx`: Changed plain string
  `serializeRecord(...)` to `${serializationHelperRefkey("serializeRecord")}(...)` refkey
  reference. This enables Alloy's auto-import resolution for dict-typed properties.
- Fixed `src/components/serialization/json-deserializer.tsx`: Same fix for `deserializeRecord`.
- Updated `src/emitter.tsx`: Added `<StaticHelpers />` to the emitter component tree.
- Updated `src/components/index.ts`: Exports all new static helper components.
- Created 4 test files with 22 total tests:
  - `test/components/static-helpers/serialization-helpers.test.tsx`: 8 tests
  - `test/components/static-helpers/paging-helpers.test.tsx`: 6 tests
  - `test/components/static-helpers/polling-helpers.test.tsx`: 6 tests
  - `test/components/static-helpers/static-helpers.test.tsx`: 2 tests
- Updated PRD: Task 2.5 marked as done.
- All 217 tests pass (22 new + 195 existing).

### Architecture decisions
- **Alloy FunctionDeclaration with code template bodies**: Each helper function is
  rendered as a proper Alloy `FunctionDeclaration` with a refkey, not as raw static
  file content. This enables Alloy's import resolution and ensures helpers integrate
  with the rest of the component tree.
- **Refkey-based references**: The json-serializer and json-deserializer now use
  `serializationHelperRefkey("serializeRecord")` and `serializationHelperRefkey("deserializeRecord")`
  instead of plain string references. This is a critical fix — without it, the generated
  code would reference `serializeRecord` without an import statement.
- **Simplified paging types**: The paging helpers use a single `TElement` type parameter
  instead of the legacy emitter's multi-parameter generic system. This simplifies the
  generated code while maintaining the same consumer-facing API.
- **Non-Azure runtime**: All helpers import from `@typespec/ts-http-runtime` via
  `httpRuntimeLib` refkeys. Azure-specific imports will be handled by the Azure
  Extension (Phase 8) via package swapping.
- **Emitted to helpers/ directory**: Helper files are placed in `helpers/` within
  the `src/` directory, matching common SDK conventions.

### Key learnings (see knowledge.md)
- Alloy name policy transforms type parameter names to camelCase (TResult → tResult)
- TypeParameterDescriptor uses `extends` not `constraint` for type constraints
- Mixed TypeParameterDescriptor arrays require all-object form
- Symbol.asyncIterator gets transformed by name policy to symbolAsyncIterator
- External packages must be registered in Output for refkey resolution

### Notes for next person
- Completed: Phase 0 (0.1-0.6), Phase 1 (1.1-1.6), Phase 2 (2.1-2.3, 2.5),
  Phase 3 (3.1-3.5), Phase 4 (4.1), Phase 5 (5.1-5.2), Phase 7 (7.1-7.2).
- **Remaining tasks by priority**:
  1. 2.4 (XML Serializers) — XML serialization/deserialization functions
  2. 4.2 (Logger Component) — Azure-only logging
  3. 6.x (Advanced Features) — LRO, multipart, samples
  4. 8.x (Azure Extension) — Azure-specific composition
  5. 9.x (Test Porting & Validation) — scenario testing
- The static helpers emit to `helpers/` directory. The index files (Phase 7) may need
  to be updated to re-export from `helpers/` if consumers need direct access.
- The paging and polling helpers are simplified implementations. When Phase 6 (LRO/paging)
  is implemented, these may need to be enhanced with more features (e.g., proper polling
  intervals, operation location headers, cancellation support).
- The `[Symbol.asyncIterator]` member in PagedAsyncIterableIterator gets transformed
  to `symbolAsyncIterator` by Alloy's name policy. This may need attention if the
  generated TypeScript needs the actual Symbol property. Consider using raw code
  templates or name policy escape for this member.
