{
  "document": {
    "title": "Product Requirements Document: Alloy Framework TypeSpec Emitter",
    "version": "1.0.0",
    "status": "complete",
    "created": "2026-02-24",
    "authors": [
      "@joheredi"
    ]
  },
  "product_overview": {
    "name": "@microsoft/http-client-js",
    "description": "A TypeSpec-to-TypeScript code emitter built on the Alloy Framework that generates modular TypeScript client SDKs for REST services. This is a ground-up rewrite of the legacy autorest.typescript/typespec-ts emitter, replacing ts-morph imperative AST manipulation with declarative Alloy JSX components.",
    "problem_statement": "The legacy emitter (autorest.typescript/packages/typespec-ts) suffers from: (1) overly complex code using raw string concatenation and manual import management, (2) fragile string-based reference resolution causing self-import bugs, (3) monolithic helper files (2700+ lines) that are hard to maintain, (4) no framework for symbol tracking or cross-file reference management. These issues increase maintenance cost, slow feature development, and introduce subtle bugs.",
    "solution": "Rewrite the emitter using Alloy Framework JSX components with automatic symbol resolution, refkey-based cross-file references, and composable component architecture. The rewrite must produce output-identical TypeScript SDKs to prevent breaking changes for existing Azure SDK consumers.",
    "target_users": [
      "TypeSpec SDK authors generating TypeScript client libraries",
      "Azure SDK team generating Azure service client SDKs",
      "Internal Microsoft teams generating REST client code from TypeSpec definitions"
    ]
  },
  "goals": [
    {
      "id": "G1",
      "title": "Output Parity",
      "description": "Generated TypeScript SDK code must be functionally identical to the legacy emitter output. The public API surface (exported types, function signatures, class shapes) must not change.",
      "priority": "P0",
      "success_metric": "All 101 scenario tests pass with matching output (modulo whitespace and import ordering)"
    },
    {
      "id": "G2",
      "title": "Pure Alloy Architecture",
      "description": "The entire emitter must use idiomatic Alloy patterns: JSX components, refkeys for all declarations/references, code tagged templates, context providers. No legacy bridges, no string concatenation for imports, no manual path calculation.",
      "priority": "P0",
      "success_metric": "Zero instances of manual import string construction or string-based reference resolution in codebase"
    },
    {
      "id": "G3",
      "title": "Composable Azure Extension",
      "description": "Core emitter works standalone for non-Azure scenarios. Azure-specific features (auth, logging, polling, cloud settings) are added via JSX composition, not conditional flags.",
      "priority": "P1",
      "success_metric": "Core emitter generates valid non-Azure SDK; Azure wrapper adds Azure-specific features without modifying core components"
    },
    {
      "id": "G4",
      "title": "Maintainability",
      "description": "Dramatically reduce code complexity compared to legacy emitter. Each component has single responsibility, is independently testable, and is under 300 lines.",
      "priority": "P1",
      "success_metric": "No component file exceeds 300 lines; each component has at least one test"
    },
    {
      "id": "G5",
      "title": "Developer Experience",
      "description": "New components are easy to add and test. The component architecture makes it straightforward to extend the emitter for new features.",
      "priority": "P2",
      "success_metric": "Adding a new feature (e.g., new serialization format) requires creating a new component, not modifying existing ones"
    }
  ],
  "non_goals": [
    {
      "id": "NG1",
      "title": "RLC (Rest Level Client) Rewrite",
      "description": "The RLC code path will NOT be ported. Only the modular path is being rewritten."
    },
    {
      "id": "NG2",
      "title": "typespec-test Package",
      "description": "The integration test package (typespec-test) will not be ported in this phase."
    },
    {
      "id": "NG3",
      "title": "Behavioral Changes",
      "description": "This rewrite must NOT introduce behavioral changes to generated code. Output parity is the primary constraint."
    },
    {
      "id": "NG4",
      "title": "New Features",
      "description": "No new code generation features will be added during the rewrite. Feature parity only."
    },
    {
      "id": "NG5",
      "title": "Runtime Library Changes",
      "description": "No changes to @typespec/ts-http-runtime or Azure runtime packages."
    }
  ],
  "requirements": {
    "functional": [],
    "non_functional": [
      {
        "id": "NFR1",
        "title": "Output Parity",
        "description": "Generated TypeScript code must match legacy emitter output. All 101 scenario tests must pass.",
        "priority": "P0"
      },
      {
        "id": "NFR2",
        "title": "Performance",
        "description": "Emitter performance should be comparable to or better than legacy emitter for typical service specs.",
        "priority": "P2"
      },
      {
        "id": "NFR3",
        "title": "Code Quality",
        "description": "All components follow Alloy idiomatic patterns. No anti-patterns (string-based refs, manual imports, monolithic code blocks).",
        "priority": "P0"
      },
      {
        "id": "NFR4",
        "title": "Testability",
        "description": "Every component is independently testable with the Alloy testing utilities (toRenderTo matcher, TypeSpec test host).",
        "priority": "P1"
      },
      {
        "id": "NFR5",
        "title": "Extensibility",
        "description": "Azure-specific features addable via JSX composition without modifying core components.",
        "priority": "P1"
      }
    ]
  },
  "technical_architecture": {
    "framework": {
      "name": "Alloy Framework",
      "packages": [
        "@alloy-js/core@0.22.0",
        "@alloy-js/typescript@0.22.0"
      ],
      "description": "JSX-based code generation framework. NOT React. Uses JSX purely as structural description language for code output. Provides automatic symbol resolution, cross-file import management, and naming policies."
    },
    "sdk_model": {
      "name": "TCGC (TypeSpec Client Generator Core)",
      "package": "@azure-tools/typespec-client-generator-core",
      "description": "Provides SdkPackage object model that pre-computes data from TypeSpec definitions. Language-agnostic SDK type system with clients, operations, models, enums, and type metadata."
    },
    "data_flow": "TypeSpec Input \u2192 Compiler \u2192 TCGC SdkPackage \u2192 Alloy JSX Components \u2192 Symbol Tree \u2192 TypeScript Files",
    "key_concepts": {
      "refkeys": "Symbolic reference keys created via refkey(entity, discriminator). Each declaration gets a refkey; references use the same refkey. Alloy resolves cross-file references and auto-generates imports.",
      "code_templates": "Tagged template literals (code`...`) that treat interpolated refkeys as live symbolic references. Alloy tracks them and resolves imports automatically.",
      "context_providers": "Named contexts via createNamedContext() for dependency injection. Eliminates prop drilling across deeply nested component trees.",
      "external_packages": "NPM packages defined via createPackage() with typed symbol accessors. Used in code templates for auto-import from runtime libraries."
    },
    "component_hierarchy": {
      "top_level": [
        "Output",
        "SdkContextProvider"
      ],
      "orchestrators": [
        "ModelFiles",
        "Operations",
        "ClassicalClient",
        "ClassicalOperationGroups"
      ],
      "source_files": [
        "ClientContext",
        "OperationOptions",
        "RestorePoller",
        "LoggerFile",
        "Samples"
      ],
      "declarations": [
        "ModelDeclaration",
        "EnumDeclaration",
        "UnionDeclaration",
        "PolymorphicType",
        "SendFunction",
        "DeserializeFunction",
        "OperationFunction",
        "JsonSerializer",
        "JsonDeserializer",
        "XmlSerializer"
      ],
      "utilities": [
        "TypeExpression",
        "RootIndex",
        "SubpathIndex",
        "StaticHelpers"
      ]
    },
    "external_dependencies": {
      "runtime": {
        "core": "@typespec/ts-http-runtime",
        "azure_client": "@azure-rest/core-client",
        "azure_pipeline": "@azure/core-rest-pipeline",
        "azure_auth": "@azure/core-auth",
        "azure_lro": "@azure/core-lro",
        "azure_identity": "@azure/identity",
        "azure_logger": "@azure/logger"
      },
      "build_time": {
        "compiler": "@typespec/compiler",
        "http": "@typespec/http",
        "rest": "@typespec/rest",
        "versioning": "@typespec/versioning",
        "tcgc": "@azure-tools/typespec-client-generator-core",
        "emitter_framework": "@typespec/emitter-framework"
      }
    }
  },
  "testing_strategy": {
    "framework": "vitest",
    "test_categories": [
      {
        "category": "Scenario Tests",
        "count": 101,
        "description": "Markdown-based tests ported from legacy emitter. Each .md file contains TypeSpec input and expected TypeScript output blocks. Tests validate output parity between legacy and new emitter.",
        "source": "autorest.typescript/packages/typespec-ts/test/modularUnit/scenarios/",
        "format": "H1 = test name, ```tsp = TypeSpec input, ```typescript <type> = expected output"
      },
      {
        "category": "Component Render Tests",
        "count": "TBD",
        "description": "JSX component tests using Alloy's toRenderTo matcher. Compile TypeSpec snippets, render components, assert output text.",
        "pattern": "src/components/*.test.tsx"
      },
      {
        "category": "Unit Tests",
        "count": "TBD",
        "description": "Pure function tests for utilities (refkey helpers, type mapping, etc.).",
        "pattern": "src/utils/*.test.ts"
      },
      {
        "category": "Spector Integration Tests (E2E)",
        "count": 48,
        "description": "End-to-end tests ported from legacy emitter's modularIntegration suite. Each test generates a client library from a Spector TypeSpec spec, then exercises the generated client against the Spector mock server to validate runtime behavior. Uses Vitest with globalSetup for server lifecycle.",
        "source": "submodules/autorest.typescript/packages/typespec-ts/test/modularIntegration/",
        "pattern": "test/e2e/**/*.test.ts",
        "infrastructure": {
          "generation_script": "eng/scripts/emit-e2e.ts",
          "vitest_config": "vitest.config.e2e.ts",
          "global_setup": "test/e2e/setup/global-setup.ts",
          "generated_code_dir": "test/e2e/generated/",
          "test_files_dir": "test/e2e/http/",
          "mock_server": "tsp-spector on port 3002",
          "coverage_file": "temp/spector-coverage.json"
        }
      }
    ],
    "test_infrastructure": {
      "test_host": "test/test-host.ts \u2014 Tester instance with TypeSpec library imports",
      "test_utils": "test/utils.tsx \u2014 TestFile, DeclarationTestFile wrappers, testHelper()",
      "custom_matchers": "test/vitest.d.ts \u2014 toRenderTo, toRenderToAsync from @alloy-js/core/testing",
      "scenario_harness": "test/scenarios/scenarios.spec.ts \u2014 MD file parser + assertion engine"
    },
    "acceptable_differences": [
      "Import statement ordering",
      "Trailing whitespace",
      "Trailing commas",
      "Empty line variations"
    ],
    "unacceptable_differences": [
      "Public API surface changes (exported types, function signatures)",
      "Type changes (different TypeScript types)",
      "Missing exports",
      "Missing or extra function parameters",
      "Different runtime behavior"
    ]
  },
  "scenario_test_inventory": {
    "total": 101,
    "categories": {
      "models_core": {
        "count": 3,
        "tests": [
          "models/models.md",
          "models/azureCoreErrorModels.md",
          "models/missingErrorResponseModel.md"
        ]
      },
      "models_serialization": {
        "count": 8,
        "tests": [
          "models/serialization/propertyType.md",
          "models/serialization/additionalProperties.md",
          "models/serialization/anonymousModel.md",
          "models/serialization/encodeIntAsString.md",
          "models/serialization/enumKeyNorm.md",
          "models/serialization/errorModels.md",
          "models/serialization/modelPropertyArrayEncoding.md",
          "models/serialization/readonlyFlattenModel.md"
        ]
      },
      "models_deserialization": {
        "count": 4,
        "tests": [
          "models/deserialization/propertyType.md",
          "models/deserialization/additionalProperties.md",
          "models/deserialization/anonymousModel.md",
          "models/deserialization/extends.md"
        ]
      },
      "models_nullable": {
        "count": 2,
        "tests": [
          "models/nullable/nullableOptional.md",
          "models/nullable/nullableUnion.md"
        ]
      },
      "models_property_flatten": {
        "count": 3,
        "tests": [
          "models/propertyFlatten/singleLayer.md",
          "models/propertyFlatten/mulipleLayers.md",
          "models/propertyFlatten/nameCollision.md"
        ]
      },
      "models_api_version": {
        "count": 2,
        "tests": [
          "models/apiVersion/apiVersionAsFixedEnum.md",
          "models/apiVersion/apiVersionAsKnownVersions.md"
        ]
      },
      "models_nested_enum": {
        "count": 3,
        "tests": [
          "models/nestedEnum/flatten/experimentalExtensibleEnumsTrue.md",
          "models/nestedEnum/notFlatten/experimentalExtensibleEnumsFalse.md",
          "models/nestedEnum/notFlatten/experimentalExtensibleEnumsUndefined.md"
        ]
      },
      "models_response": {
        "count": 4,
        "tests": [
          "models/response/headerAndModelInResponse.md",
          "models/response/headerAndModelSpread.md",
          "models/response/headerInResponse.md",
          "models/response/headerInXmlResponse.md"
        ]
      },
      "models_template": {
        "count": 1,
        "tests": [
          "models/template/template.md"
        ]
      },
      "operations_core": {
        "count": 6,
        "tests": [
          "operations/operations.md",
          "operations/override.md",
          "operations/overrideReservedkeywords.md",
          "operations/clientDefaultValue.md",
          "operations/armPatchWithUnionResponse.md",
          "operations/lroPaging.md"
        ]
      },
      "operations_path_param": {
        "count": 8,
        "tests": [
          "operations/pathParam/allowReservedFalseInAnnotation.md",
          "operations/pathParam/allowReservedTrueInAnnotation.md",
          "operations/pathParam/allowReservedTrueWithUriTemplate.md",
          "operations/pathParam/allowReservedWithUriTemplate.md",
          "operations/pathParam/optionalPath.md",
          "operations/pathParam/pathParamUrlTemplate.md",
          "operations/pathParam/pathParameterNamedPath.md",
          "operations/pathParam/requiredPathWithDefault.md"
        ]
      },
      "operations_query_param": {
        "count": 5,
        "tests": [
          "operations/queryParam/explodeTrueWithAnnotation.md",
          "operations/queryParam/explodeTrueWithUriTemplate.md",
          "operations/queryParam/queryParamCollectionFormat.md",
          "operations/queryParam/queryParamUrlTemplate.md",
          "operations/queryParam/queryParamWithClientInitialization.md"
        ]
      },
      "operations_header_param": {
        "count": 1,
        "tests": [
          "operations/headerParam/headerParamWithClientInitialization.md"
        ]
      },
      "operations_cookie_param": {
        "count": 1,
        "tests": [
          "operations/cookieParam/ignoreCookieParam.md"
        ]
      },
      "operations_error_deserialization": {
        "count": 2,
        "tests": [
          "operations/errorDeserialization/errorHeaderDeserialization.md",
          "operations/errorDeserialization/xmlErrorDeserialization.md"
        ]
      },
      "operations_pagination": {
        "count": 1,
        "tests": [
          "operations/pagination/disablePagination.md"
        ]
      },
      "api_operations": {
        "count": 3,
        "tests": [
          "apiOperations/apiOperations.md",
          "apiOperations/azureCoreOperations.md",
          "apiOperations/reservedWordOperations.md"
        ]
      },
      "client_context": {
        "count": 2,
        "tests": [
          "clientContext/clientContext.md",
          "clientContext/optionalApiVersion.md"
        ]
      },
      "classic_client": {
        "count": 3,
        "tests": [
          "classicClient/classicClient.md",
          "classicClient/clientConstructorOverloads.md",
          "classicClient/reservedWordOperations.md"
        ]
      },
      "enum_union": {
        "count": 1,
        "tests": [
          "enumUnion/enumUnion.md"
        ]
      },
      "multipart": {
        "count": 4,
        "tests": [
          "multipart/file.md",
          "multipart/json.md",
          "multipart/text.md",
          "multipart/renamewithWireNameAndClientName.md"
        ]
      },
      "payload_xml": {
        "count": 3,
        "tests": [
          "payload/xml/xmlArrayItemTypes.md",
          "payload/xml/xmlArrayItemsNameWrapping.md",
          "payload/xml/xmlName.md"
        ]
      },
      "samples_client": {
        "count": 8,
        "tests": [
          "samples/client/apiKeyCredentialClient.md",
          "samples/client/multipleClient.md",
          "samples/client/parameterizedHost.md",
          "samples/client/renameClientName.md",
          "samples/client/subscriptionIdHandling.md",
          "samples/client/disableHierarchy/disableOperationGroup.md",
          "samples/client/disableHierarchy/enableOperationGroup.md",
          "samples/client/enableHierarchy/disableOperationGroup.md",
          "samples/client/enableHierarchy/enableOperationGroup.md"
        ]
      },
      "samples_operations": {
        "count": 4,
        "tests": [
          "samples/operations/armCurdOperations.md",
          "samples/operations/disableHierarchyArmCurdOperations.md",
          "samples/operations/dpgCurdOperations.md",
          "samples/operations/multipleExamplesInOneFile.md"
        ]
      },
      "samples_parameters": {
        "count": 12,
        "tests": [
          "samples/parameters/bodyOptionalCheck.md",
          "samples/parameters/bodyOptionalParameterName.md",
          "samples/parameters/bodyReadonlyOptionalCheck.md",
          "samples/parameters/bodyRequiredCheck.md",
          "samples/parameters/clientConstructorArgs.md",
          "samples/parameters/inheritedPropertyMapping.md",
          "samples/parameters/optionalParameter.md",
          "samples/parameters/paramaterSpreadCheck.md",
          "samples/parameters/parameterName.md",
          "samples/parameters/parameterNormalization.md",
          "samples/parameters/parameterOrdering.md",
          "samples/parameters/parameterTypesCheck.md"
        ]
      },
      "samples_property_flatten": {
        "count": 3,
        "tests": [
          "samples/propertyFlatten/complexFlattenCases.md",
          "samples/propertyFlatten/mulipleFlattenCases.md",
          "samples/propertyFlatten/simpleFlattenCases.md"
        ]
      },
      "other": {
        "count": 4,
        "tests": [
          "anonymous/anonymous.md",
          "emptyProject/emptyProject.md",
          "example/example.md",
          "modelsGenerator/modelsGenerator.md"
        ]
      }
    }
  },
  "tasks": [
    {
      "id": "SPECTOR-7",
      "title": "Port parameters, payload, and routes Spector tests",
      "category": "testing/spector-integration",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [
        "SPECTOR-3"
      ],
      "design_review": null,
      "source": "design",
      "description": "Port parameters, payload, and routing Spector integration tests (9 test files).\n\n## Tests to port\n\n### Parameters\n| Legacy test file | Spector spec | New test path |\n|---|---|---|\n| `basicParameters.spec.ts` | `parameters/basic` | `test/e2e/http/parameters/basic/main.test.ts` |\n| `bodyOptionality.spec.ts` | `parameters/body-optionality` | `test/e2e/http/parameters/body-optionality/main.test.ts` |\n| `collectionFormat.spec.ts` | `parameters/collection-format` | `test/e2e/http/parameters/collection-format/main.test.ts` |\n| `spreadParameters.spec.ts` | `parameters/spread` | `test/e2e/http/parameters/spread/main.test.ts` |\n\n### Payload\n| Legacy test file | Spector spec | New test path |\n|---|---|---|\n| `payloadContentNegotiation.spec.ts` | `payload/content-negotiation` | `test/e2e/http/payload/content-negotiation/main.test.ts` |\n| `payloadMediaType.spec.ts` | `payload/media-type` | `test/e2e/http/payload/media-type/main.test.ts` |\n| `payloadMultipart.spec.ts` | `payload/multipart` | `test/e2e/http/payload/multipart/main.test.ts` |\n| `payloadPageable.spec.ts` | `payload/pageable` | `test/e2e/http/payload/pageable/main.test.ts` |\n\n### Routes\n| Legacy test file | Spector spec | New test path |\n|---|---|---|\n| `routes.spec.ts` | `routes` | `test/e2e/http/routes/main.test.ts` |\n\n## Notes\n- `payloadMultipart` is complex \u2014 uses `fs.createReadStream()` for file uploads. Tests need access to test assets. Create `test/e2e/assets/` with image.jpg and image.png files, or reference them from the existing spector spec assets.\n- `payloadPageable` tests pagination. Verify the generated client has paging helpers.\n- `routes` is a comprehensive test with many sub-tests for path/query expansion modes.\n- `spreadParameters` tests TypeSpec spread operator for parameter destructuring.\n\n## Reference files\n- Legacy tests: `submodules/autorest.typescript/packages/typespec-ts/test/modularIntegration/`\n- Spector specs: `submodules/typespec/packages/http-specs/specs/`\n- Reference tests: `submodules/typespec/packages/http-client-js/test/e2e/http/`\n\n## Validation\nRun `pnpm build && pnpm test:e2e` \u2014 all parameter/payload/routes tests should pass."
    },
    {
      "id": "SPECTOR-8",
      "title": "Port versioning and special Spector tests",
      "category": "testing/spector-integration",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [
        "SPECTOR-3"
      ],
      "design_review": null,
      "source": "design",
      "description": "Port versioning, special-words, and special-headers Spector integration tests (10 test files).\n\n## Tests to port\n\n### Versioning\n| Legacy test file | Spector spec | New test path |\n|---|---|---|\n| `versioningAdded.spec.ts` | `versioning/added` | `test/e2e/http/versioning/added/main.test.ts` |\n| `versioningMadeOptional.spec.ts` | `versioning/madeOptional` | `test/e2e/http/versioning/madeOptional/main.test.ts` |\n| `versioningRemovedV1.spec.ts` | `versioning/removed` (v1 config) | `test/e2e/http/versioning/removed/v1/main.test.ts` |\n| `versioningRemovedV2.spec.ts` | `versioning/removed` (v2 config) | `test/e2e/http/versioning/removed/v2/main.test.ts` |\n| `versioningRemovedV2Preview.spec.ts` | `versioning/removed` (v2preview config) | `test/e2e/http/versioning/removed/v2preview/main.test.ts` |\n| `versioningRenamedFrom.spec.ts` | `versioning/renamedFrom` | `test/e2e/http/versioning/renamedFrom/main.test.ts` |\n| `versioningReturnTypeChangedFrom.spec.ts` | `versioning/returnTypeChangedFrom` | `test/e2e/http/versioning/returnTypeChangedFrom/main.test.ts` |\n| `versioningTypeChangedFrom.spec.ts` | `versioning/typeChangedFrom` | `test/e2e/http/versioning/typeChangedFrom/main.test.ts` |\n\n### Special\n| Legacy test file | Spector spec | New test path |\n|---|---|---|\n| `specialWords.spec.ts` | `special-words` | `test/e2e/http/special-words/main.test.ts` |\n| `headersRepeatability.spec.ts` | `special-headers/repeatability` | `test/e2e/http/special-headers/repeatability/main.test.ts` |\n\n## Notes on versioning tests\nThe `versioning/removed` spec generates 3 different client variants based on version configuration:\n- v1: Only the operations/types from version 1\n- v2: Operations/types up to version 2 (v1 removed items are gone)\n- v2preview: Preview version with additional preview operations\n\nIn the legacy emitter, these are 3 separate entries in cadl-ranch-list.js with different outputPaths but the same inputPath. In the new emitter, the generation script needs to compile the same spec 3 times with different version options. This may require special handling in the emit-e2e script or version-specific tspconfig.yaml files.\n\n## Reference files\n- Legacy tests: `submodules/autorest.typescript/packages/typespec-ts/test/modularIntegration/versioning*.spec.ts`, `specialWords.spec.ts`, `headersRepeatability.spec.ts`\n- Legacy cadl-ranch-list.js: Shows how versioning/removed is split into 3 entries with different outputPaths\n- Spector specs: `submodules/typespec/packages/http-specs/specs/versioning/`, `special-words/`, `special-headers/`\n\n## Validation\nRun `pnpm build && pnpm test:e2e` \u2014 all versioning and special tests should pass."
    },
    {
      "id": "SPECTOR-9",
      "title": "Add e2e test CI integration and coverage reporting",
      "category": "testing/spector-infrastructure",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [
        "SPECTOR-3"
      ],
      "design_review": null,
      "source": "design",
      "description": "Integrate the Spector e2e tests into the CI pipeline and set up coverage reporting.\n\n## What to do\n1. Add a CI step that runs `pnpm build && pnpm test:e2e` in the existing CI workflow\n2. Ensure the Spector coverage file (`temp/spector-coverage.json`) is captured as a CI artifact\n3. Add a script to parse and summarize coverage: which Spector scenarios are covered vs not\n\n## Coverage reporting\nAfter tests complete, the Spector server writes coverage data to `temp/spector-coverage.json`. This file tracks which mock API scenarios were hit during testing.\n\nCreate `eng/scripts/calculate-coverage.ts` that:\n1. Reads `temp/spector-coverage.json`\n2. Prints a summary: N/M scenarios covered\n3. Lists uncovered scenarios\n\n## Reference\n- `submodules/typespec/packages/http-client-js/eng/scripts/calculate-coverage.js` \u2014 reference implementation\n- Spector coverage format: `CoverageReport[]` from `@typespec/spector`\n\n## Package.json script\nAdd: `\"test:e2e:ci\": \"pnpm build && pnpm test:e2e\"`\n\n## Validation\n- CI runs e2e tests successfully\n- Coverage artifact is captured"
    },
    {
      "id": "E2E-FIX-CONTENT-TYPE-BARE-SCALAR",
      "title": "Fix Content-Type for bare scalar body parameters (text/plain vs application/json)",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 6 skipped e2e tests caused by incorrect Content-Type header for PUT operations on bare scalar types.\n\n## Problem\n\nWhen a TypeSpec operation accepts a bare scalar body (string, enum, unknown \u2014 not wrapped in a model), the Spector mock server expects `Content-Type: text/plain`. The emitter always generates `contentType: \"application/json\"` regardless of body type.\n\n## Affected Tests (6)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `test/e2e/http/type/enum/fixed/main.test.ts` | should put a known enum value | 32 |\n| `test/e2e/http/type/enum/fixed/main.test.ts` | should put an unknown enum value and receive 500 | 37 |\n| `test/e2e/http/type/enum/extensible/main.test.ts` | should put a known enum value | 34 |\n| `test/e2e/http/type/enum/extensible/main.test.ts` | should put an unknown enum value | 39 |\n| `test/e2e/http/type/scalar/main.test.ts` | should put a string value | 29 |\n| `test/e2e/http/type/scalar/main.test.ts` | should put an unknown value | 52 |\n\n## Mock Server Expectations (from mockapi.ts)\n\n- `type/enum/fixed` mockapi.ts: PUT expects `\"Content-Type\": \"application/json\"` with `json(\"Monday\")`\n- `type/enum/extensible` mockapi.ts: PUT expects `\"Content-Type\": \"text/plain\"` with `json(\"Monday\")`\n- `type/scalar/string` mockapi.ts: PUT expects `\"Content-Type\": \"text/plain\"` with `json(\"test\")`\n- `type/scalar/unknown` mockapi.ts: PUT expects `\"Content-Type\": \"text/plain\"` with `json(\"test\")`\n\n## Current Generated Code (broken)\n\nAll PUT operations generate: `contentType: \"application/json\", body: body`\n\nFor example, in `test/e2e/generated/type/enum/extensible/src/api/operations.ts`:\n```typescript\nreturn context.path(\"/type/enum/extensible/string/known-value\").put({\n  contentType: \"application/json\",\n  body: body,\n});\n```\n\n## Root Cause\n\nThe emitter operation generation component always emits `contentType: \"application/json\"` without checking whether the body is a bare scalar type. When the body is a bare scalar (not a model/object), the content-type should be `text/plain` per the TypeSpec HTTP specification.\n\n## Recommended Fix\n\nIn the operation _send function generation code (likely in `src/components/` \u2014 look for where `contentType` is set in the request options), detect when the request body parameter type is a bare scalar:\n\n1. Check the TCGC `SdkHttpOperation`'s body parameter type via `operation.bodyParam.type.kind`\n2. If the body type kind is `\"string\"`, `\"enum\"`, `\"unknown\"`, or another scalar type (not `\"model\"`, `\"array\"`, `\"dict\"`), emit `contentType: \"text/plain\"`\n3. Otherwise, keep `contentType: \"application/json\"`\n\nThe TCGC body parameter has a `type` field with a `kind` property that distinguishes scalars from complex types.\n\n## After Fix\n\nRemove `it.skip` from all 6 tests and verify they pass against the Spector mock server.\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Type.Enum.Fixed|Type.Enum.Extensible|Type.Scalar\"\n```"
    },
    {
      "id": "E2E-FIX-QUERY-PARAM-ENCODING",
      "title": "Fix query parameter type coercion for Date, duration, and bytes types",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 13 skipped e2e tests caused by raw typed objects (Date, number, Uint8Array) being passed directly to `expandUrlTemplate()` without encoding conversion.\n\n## Problem\n\nThe emitter passes raw JavaScript objects to URL template expansion without converting them to the wire-format string. `Date` objects serialize to `[object Object]`, `Uint8Array` objects to `[object Uint8Array]`, etc.\n\n## Affected Tests (13)\n\n### Datetime Query Params (5)\n| Test file | Test name | Line | Expected encoding |\n|-----------|-----------|------|-------------------|\n| `encode/datetime/main.test.ts` | default (rfc3339) query param | 21 | `value.toISOString()` \u2192 `2022-08-26T18:38:00.000Z` |\n| `encode/datetime/main.test.ts` | rfc3339 query param | 25 | `value.toISOString()` \u2192 `2022-08-26T18:38:00.000Z` |\n| `encode/datetime/main.test.ts` | rfc7231 query param | 29 | `value.toUTCString()` \u2192 `Fri, 26 Aug 2022 14:38:00 GMT` |\n| `encode/datetime/main.test.ts` | unixTimestamp query param | 33 | `Math.floor(value.getTime()/1000)` \u2192 `1686566864` |\n| `encode/datetime/main.test.ts` | unixTimestamp array query param | 37 | Each element unix-converted then CSV-joined |\n\n### Duration Query Params (4)\n| Test file | Test name | Line | Expected encoding |\n|-----------|-----------|------|-------------------|\n| `encode/duration/main.test.ts` | int32 seconds query param | 24 | `input=36` (numeric to string) |\n| `encode/duration/main.test.ts` | float seconds query param | 28 | `input=35.625` |\n| `encode/duration/main.test.ts` | float64 seconds query param | 32 | `input=35.625` |\n| `encode/duration/main.test.ts` | int32 seconds array query param | 36 | `input=36,47` (CSV joined) |\n\n### Bytes Query Params (4)\n| Test file | Test name | Line | Expected encoding |\n|-----------|-----------|------|-------------------|\n| `encode/bytes/main.test.ts` | default (base64) query param | 51 | `uint8ArrayToString(value, \"base64\")` \u2192 `dGVzdA==` |\n| `encode/bytes/main.test.ts` | base64 query param | 55 | `uint8ArrayToString(value, \"base64\")` \u2192 `dGVzdA==` |\n| `encode/bytes/main.test.ts` | base64url query param | 59 | `uint8ArrayToString(value, \"base64url\")` \u2192 `dGVzdA` |\n| `encode/bytes/main.test.ts` | base64url array query param | 63 | Each element base64url-encoded then CSV-joined |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/encode/datetime/src/api/query/operations.ts`:\n```typescript\nconst path = expandUrlTemplate(\"/encode/datetime/query/default{?value}\", { \"value\": value });\n// value is a Date object \u2014 serializes to garbage in URL\n```\n\nIn `test/e2e/generated/encode/bytes/src/api/query/operations.ts`:\n```typescript\nconst path = expandUrlTemplate(\"/encode/bytes/query/default{?value}\", { \"value\": value });\n// value is a Uint8Array \u2014 serializes to \"[object Uint8Array]\"\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/encode/datetime/mockapi.ts`:\n- `Encode_Datetime_Query_default`: expects query `value=2022-08-26T18:38:00.000Z` (rfc3339)\n- `Encode_Datetime_Query_rfc7231`: expects `value=Fri, 26 Aug 2022 14:38:00 GMT`\n- `Encode_Datetime_Query_unixTimestamp`: expects `value=1686566864` (unix seconds, numeric)\n\nFrom `node_modules/@typespec/http-specs/specs/encode/bytes/mockapi.ts`:\n- `Encode_Bytes_Query_default`: expects `value=dGVzdA==`\n- `Encode_Bytes_Query_base64url`: expects `value=dGVzdA`\n\nFrom `node_modules/@typespec/http-specs/specs/encode/duration/mockapi.ts`:\n- `Encode_Duration_Query_int32Seconds`: expects `input=36`\n- `Encode_Duration_Query_floatSeconds`: expects `input=35.625`\n\n## Root Cause\n\nThe emitter's operation `_send` function generation passes raw parameter values into `expandUrlTemplate()` without applying the encoding transformation. TCGC provides `encode` info on each parameter's type (e.g., `type.encode === \"rfc3339\"`, `type.encode === \"unixTimestamp\"`, `type.encode === \"base64url\"`), but the operation generation code doesn't use it for query parameters.\n\n## Recommended Fix\n\nIn the emitter's operation generation code that builds the URL template variables object:\n\n1. For each query parameter, check `param.type.kind` and `param.type.encode`:\n   - `utcDateTime` + `rfc3339` (or default): Generate `value.toISOString()`\n   - `utcDateTime` + `rfc7231`: Generate `value.toUTCString()`\n   - `utcDateTime` + `unixTimestamp`: Generate `Math.floor(value.getTime() / 1000)`\n   - `duration` + numeric encoding: Generate `value` (number auto-stringified by URL)\n   - `bytes` + `base64`: Generate `uint8ArrayToString(value, \"base64\")` (import from runtime)\n   - `bytes` + `base64url`: Generate `uint8ArrayToString(value, \"base64url\")`\n2. For array params, apply element-level encoding before joining with `buildCsvCollection`\n\nLook at how the emitter handles these encodings for JSON body properties in `src/components/serialization/json-serializer.tsx` \u2014 apply the same pattern.\n\n## Key Files to Investigate\n\n- `src/components/` \u2014 find where `expandUrlTemplate` call is generated with query params\n- `src/components/serialization/json-serializer.tsx` \u2014 existing encoding logic to reuse\n- `test/e2e/generated/encode/datetime/src/api/query/operations.ts` \u2014 see current broken output\n\n## After Fix\n\nRemove `it.skip` from all 13 tests. Regenerate test clients if needed.\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Datetime|Encode.Duration|Encode.Bytes\"\n```"
    },
    {
      "id": "E2E-FIX-HEADER-PARAM-ENCODING",
      "title": "Fix header parameter type coercion for Date, duration, and bytes types",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 9 skipped e2e tests caused by raw typed objects (Date arrays, numbers, Uint8Array) being passed directly into HTTP headers without string conversion.\n\n## Problem\n\nThe emitter generates header assignments that pass raw typed objects instead of string-encoded values. HTTP headers require string values.\n\n## Affected Tests (9)\n\n### Datetime Headers (2)\n| Test file | Test name | Line | Expected |\n|-----------|-----------|------|----------|\n| `encode/datetime/main.test.ts` | unixTimestamp header | 94 | `Math.floor(value.getTime()/1000).toString()` \u2192 `\"1686566864\"` |\n| `encode/datetime/main.test.ts` | unixTimestamp array header | 98 | Each element unix-converted, then CSV-joined \u2192 `\"1686566864,1686734256\"` |\n\n### Duration Headers (3)\n| Test file | Test name | Line | Expected |\n|-----------|-----------|------|----------|\n| `encode/duration/main.test.ts` | int32 seconds header | 104 | `duration.toString()` \u2192 `\"36\"` |\n| `encode/duration/main.test.ts` | float seconds header | 108 | `duration.toString()` \u2192 `\"35.625\"` |\n| `encode/duration/main.test.ts` | float64 seconds header | 112 | `duration.toString()` \u2192 `\"35.625\"` |\n\n### Bytes Headers (4)\n| Test file | Test name | Line | Expected |\n|-----------|-----------|------|----------|\n| `encode/bytes/main.test.ts` | default (base64) header | 103 | `uint8ArrayToString(value, \"base64\")` \u2192 `\"dGVzdA==\"` |\n| `encode/bytes/main.test.ts` | base64 header | 107 | `uint8ArrayToString(value, \"base64\")` \u2192 `\"dGVzdA==\"` |\n| `encode/bytes/main.test.ts` | base64url header | 111 | `uint8ArrayToString(value, \"base64url\")` \u2192 `\"dGVzdA\"` |\n| `encode/bytes/main.test.ts` | base64url array header | 115 | Each element base64url-encoded, CSV-joined \u2192 `\"dGVzdA,dGVzdA\"` |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/encode/duration/src/api/header/operations.ts`:\n```typescript\nheaders: { \"duration\": duration, ...options.requestOptions?.headers }\n// duration is a number \u2014 HTTP headers require strings\n```\n\nIn `test/e2e/generated/encode/bytes/src/api/header/operations.ts`:\n```typescript\nheaders: { \"value\": value, ...options.requestOptions?.headers }\n// value is a Uint8Array \u2014 headers need base64 string\n```\n\nIn `test/e2e/generated/encode/datetime/src/api/header/operations.ts`:\n```typescript\nheaders: { \"value\": buildCsvCollection(value), ...options.requestOptions?.headers }\n// value is Date[] \u2014 buildCsvCollection needs string/number elements, not Date objects\n```\n\n## Mock Server Expectations\n\nFrom duration mockapi.ts:\n- `Encode_Duration_Header_int32Seconds`: expects `duration: \"36\"`\n- `Encode_Duration_Header_floatSeconds`: expects `duration: \"35.625\"`\n\nFrom bytes mockapi.ts:\n- `Encode_Bytes_Header_default`: expects `value: \"dGVzdA==\"`\n- `Encode_Bytes_Header_base64urlArray`: expects `value: \"dGVzdA,dGVzdA\"`\n\nFrom datetime mockapi.ts:\n- `Encode_Datetime_Header_unixTimestamp`: expects `value: \"1686566864\"`\n- `Encode_Datetime_Header_unixTimestampArray`: expects `value: \"1686566864,1686734256\"`\n\n## Root Cause\n\nSame underlying issue as E2E-FIX-QUERY-PARAM-ENCODING \u2014 the emitter doesn't apply `@encode` transformations when generating header parameter assignments. Headers require string values but the emitter passes raw SDK types (number, Date, Uint8Array) directly.\n\n## Recommended Fix\n\nIn the emitter's operation generation code that builds the headers object, apply encoding based on param type:\n- `utcDateTime` + `unixTimestamp`: `String(Math.floor(value.getTime() / 1000))`\n- `duration` + numeric encoding: `value.toString()`\n- `bytes` + `base64`/`base64url`: `uint8ArrayToString(value, encoding)`\n- For array headers, encode each element before passing to `buildCsvCollection`\n\nCoordinate with E2E-FIX-QUERY-PARAM-ENCODING since encoding logic is identical \u2014 consider extracting a shared helper.\n\n## Key Files\n\n- Same emitter operation generation code as query params\n- `test/e2e/generated/encode/*/src/api/header/operations.ts` \u2014 current broken output\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Datetime|Encode.Duration|Encode.Bytes\"\n```"
    },
    {
      "id": "E2E-FIX-RESPONSE-HEADER-DESER",
      "title": "Generate response header deserialization for operations with typed response headers",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 4 skipped e2e tests caused by missing response header extraction and deserialization.\n\n## Problem\n\nOperations that return typed response headers generate `void` return types and no code to extract/parse header values.\n\n## Affected Tests (4)\n\n| Test file | Test name | Line | Expected header |\n|-----------|-----------|------|----------------|\n| `encode/datetime/main.test.ts` | default (rfc7231) response header | 119 | `value: \"Fri, 26 Aug 2022 14:38:00 GMT\"` \u2192 `Date` |\n| `encode/datetime/main.test.ts` | rfc3339 response header | 131 | `value: \"2022-08-26T18:38:00.000Z\"` \u2192 `Date` |\n| `encode/datetime/main.test.ts` | rfc7231 response header | 143 | `value: \"Fri, 26 Aug 2022 14:38:00 GMT\"` \u2192 `Date` |\n| `encode/datetime/main.test.ts` | unixTimestamp response header | 155 | `value: \"1686566864\"` \u2192 `Date` (via `new Date(parseInt(v)*1000)`) |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/encode/datetime/src/api/responseHeader/operations.ts`:\n```typescript\nexport async function $default(\n  context: DatetimeContext,\n  options: DefaultOptionalParams = { requestOptions: {} },\n): Promise<void> {  // Returns void \u2014 should return { value: Date }\n  const result = await _defaultSend(context, options);\n  return _defaultDeserialize(result);\n}\n\nexport async function _defaultDeserialize(\n  result: PathUncheckedResponse,\n): Promise<void> {\n  const expectedStatuses = [\"204\"];\n  if (!expectedStatuses.includes(result.status)) {\n    throw createRestError(result);\n  }\n  // No code to extract headers!\n}\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/encode/datetime/mockapi.ts`:\n- `Encode_Datetime_ResponseHeader_default`: Returns response header `value: \"Fri, 26 Aug 2022 14:38:00 GMT\"` with status 204\n- `Encode_Datetime_ResponseHeader_rfc3339`: Returns header `value: \"2022-08-26T18:38:00.000Z\"`\n- `Encode_Datetime_ResponseHeader_unixTimestamp`: Returns header `value: \"1686566864\"` (unix seconds as string)\n\n## Root Cause\n\nThe emitter's deserialize function generation doesn't handle response headers. When an operation's response includes `@header` properties, the emitter should generate a response type with header fields, extract values from `result.headers[\"name\"]`, and apply type-appropriate deserialization.\n\n## Recommended Fix\n\n1. In operation deserialize function generation, check TCGC's `operation.responses[].headers` for response header definitions\n2. If headers exist, generate a response model type with the header fields\n3. In `_deserialize`, extract headers from `result.headers[\"headerName\"]`\n4. Apply encoding-aware deserialization:\n   - `utcDateTime` + default/rfc7231: `new Date(result.headers[\"value\"])`\n   - `utcDateTime` + rfc3339: `new Date(result.headers[\"value\"])`\n   - `utcDateTime` + unixTimestamp: `new Date(parseInt(result.headers[\"value\"]) * 1000)`\n5. Change return type from `void` to response header model\n\n## Key Files\n\n- Look for where `_deserialize` functions are generated in the emitter\n- Check how TCGC exposes response headers: `operation.responses[].headers`\n- Reference: `submodules/flight-instructor/` for how response headers are handled\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Datetime\"\n```"
    },
    {
      "id": "E2E-FIX-NUMERIC-STRING-ENCODE",
      "title": "Handle @encode(string) on integer types in serialization/deserialization",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 3 skipped e2e tests caused by missing support for `@encode(string)` on integer types.\n\n## Problem\n\nWhen integer types have `@encode(string)` in TypeSpec, the wire format is a string (e.g., `\"10000000000\"`) but the SDK should expose the value as a `number`. The emitter generates no-op passthrough serializers.\n\n## Affected Tests (3)\n\n| Test file | Test name | Line | Wire value | SDK value |\n|-----------|-----------|------|-----------|----------|\n| `encode/numeric/main.test.ts` | safeint as string | 15 | `\"10000000000\"` | `10000000000` |\n| `encode/numeric/main.test.ts` | optional uint32 as string | 21 | `\"1\"` | `1` |\n| `encode/numeric/main.test.ts` | uint8 as string | 27 | `\"255\"` | `255` |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/encode/numeric/src/models/models.ts`:\n```typescript\nexport interface SafeintAsStringProperty {\n  value: string;  // Should be number\n}\nexport function safeintAsStringPropertySerializer(item: SafeintAsStringProperty): any {\n  return { value: item[\"value\"] };  // No conversion\n}\nexport function safeintAsStringPropertyDeserializer(item: any): SafeintAsStringProperty {\n  return { value: item[\"value\"] };  // No conversion\n}\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/encode/numeric/mockapi.ts`:\n```\nEncode_Numeric_Property_safeintAsString: POST /encode/numeric/property/safeint with { value: \"10000000000\" }\nEncode_Numeric_Property_uint32AsStringOptional: POST /encode/numeric/property/uint32 with { value: \"1\" }\nEncode_Numeric_Property_uint8AsString: POST /encode/numeric/property/uint8 with { value: \"255\" }\n```\n\n## Root Cause\n\nIn `src/components/serialization/json-serializer.tsx`, `getSerializationExpression()` handles `@encode` for `utcDateTime` and `bytes` but has no case for integer types with `@encode(\"string\")`. Same gap in `json-deserializer.tsx`.\n\n## Recommended Fix\n\n### 1. Model type generation\nChange generated TypeScript type from `string` to `number`.\n\n### 2. Serializer (`json-serializer.tsx`)\nIn `getSerializationExpression()`, add integer+string encoding case:\n```typescript\n// When type is integer-like and encode === \"string\":\nreturn code`${accessor}.toString()`;\n```\nAlso update `needsTransformation()` to return `true`.\n\n### 3. Deserializer (`json-deserializer.tsx`)\nIn `getDeserializationExpression()`, add:\n```typescript\nreturn code`Number(${accessor})`;\n```\n\n### Reference Pattern\nLook at how `utcDateTime` + `unixTimestamp` encoding is handled \u2014 same pattern.\n\n## Key Files\n\n- `src/components/serialization/json-serializer.tsx` \u2014 `getSerializationExpression()`, `needsTransformation()`\n- `src/components/serialization/json-deserializer.tsx` \u2014 `getDeserializationExpression()`\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Numeric\"\n```"
    },
    {
      "id": "E2E-FIX-EXTENSIBLE-ENUM-ARRAY-REFKEY",
      "title": "Fix unresolved refkey for extensible enum array serializers in getTypeSignature()",
      "category": "bug-fix/emitter",
      "priority": "P0",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 12 skipped e2e tests caused by unresolved symbol references in generated extensible enum array serializers. This is a CRITICAL bug \u2014 the generated code contains literal `<Unresolved Symbol: ...>` text that causes esbuild parse failures, making the entire client unusable.\n\n## Problem\n\nThe generated `models.ts` for encode/array contains broken code:\n```typescript\nexport function commaDelimitedExtensibleEnumArrayPropertySerializer(...) {\n  return {\n    value: buildCsvCollection(<Unresolved Symbol: refkey[sarraySerializer\u2063senum]>(item[\"value\"])),\n  };\n}\n```\nThis appears 4 times (lines 173, 181, 189, 197).\n\n## Affected Tests (12)\n\nAll tests in `test/e2e/http/encode/array/main.test.ts` (entire `describe.skip` block):\n- string array: comma, space, pipe, newline delimiter (4)\n- enum array: comma, space, pipe, newline delimiter (4)\n- extensible enum array: comma, space, pipe, newline delimiter (4)\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/encode/array/mockapi.ts`:\nEach test POSTs `{ value: \"blue,red,green\" }` (or space/pipe/newline delimited) and expects same echoed back.\n\n## Root Cause\n\nIn `src/utils/refkeys.ts`, `getTypeSignature()` returns just `\"enum\"` for ALL enum types (regular and extensible). The execution flow:\n\n1. Serializer generation encounters extensible enum array property\n2. `getSerializationExpression()` checks `needsTransformation(type.valueType)` \u2192 true\n3. `valueTypeHasNamedSerializerFn(type.valueType)` \u2192 true for extensible enums\n4. Generates: `code\\`\\${arraySerializerRefkey(type.valueType)}(\\${accessor})\\``\n5. `arraySerializerRefkey()` calls `refkey(\"arraySerializer\", getTypeSignature(type.valueType))`\n6. `getTypeSignature()` returns `\"enum\"` \u2014 NOT unique per enum type\n7. No `JsonArraySerializer` component declared with matching refkey \u2192 UNRESOLVED\n\nThe specific code in `src/utils/refkeys.ts`:\n```typescript\ndefault:\n  return t.kind;  // Returns \"enum\" for ALL enum types\n```\n\n## Recommended Fix\n\nIn `src/utils/refkeys.ts`, update `getTypeSignature()` for enum case:\n```typescript\ncase \"enum\":\n  return `enum:${t.name}`;  // e.g., \"enum:ColorsExtensibleEnum\"\n```\n\nThis ensures each enum type gets a unique refkey. Then verify `JsonArraySerializer` in `src/components/serialization/json-array-record-helpers.tsx` is rendered for extensible enum types.\n\n## Key Files\n\n- `src/utils/refkeys.ts` \u2014 `getTypeSignature()` function\n- `src/components/serialization/json-array-record-helpers.tsx` \u2014 `JsonArraySerializer` rendering\n- `src/components/serialization/json-serializer.tsx` \u2014 `valueTypeHasNamedSerializerFn()`\n\n## After Fix\n\nChange `describe.skip` to `describe` in test. Regenerate test clients.\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Array\"\n```"
    },
    {
      "id": "E2E-FIX-ADDITIONAL-PROPS-DERIVED",
      "title": "Generate additionalProperties bag in derived type serializers/deserializers",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 16 skipped e2e tests caused by derived types losing their `additionalProperties` bag during serialization/deserialization.\n\n## Problem\n\nWhen a model extends a base type with additional properties (Record<string, unknown>), base type serializers/deserializers correctly use `serializeRecord()`/`deserializeRecord()`. But derived types do NOT include this logic, silently discarding additional properties.\n\n## Affected Tests (16)\n\n### Derived Unknown Additional Properties (4)\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/property/additional-properties/main.test.ts` | get derived unknown additional props | 54 |\n| `type/property/additional-properties/main.test.ts` | put derived unknown additional props | 59 |\n| `type/property/additional-properties/main.test.ts` | get derived unknown additional props (is) | 113 |\n| `type/property/additional-properties/main.test.ts` | put derived unknown additional props (is) | 118 |\n\n### Discriminated Unknown Additional Properties (4)\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/property/additional-properties/main.test.ts` | get discriminated unknown additional props | 76 |\n| `type/property/additional-properties/main.test.ts` | put discriminated unknown additional props | 81 |\n| `type/property/additional-properties/main.test.ts` | get discriminated unknown additional props (is) | 133 |\n| `type/property/additional-properties/main.test.ts` | put discriminated unknown additional props (is) | 138 |\n\n### Different Spread Types (8)\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/property/additional-properties/main.test.ts` | get/put derived different spread string | 419-424 |\n| `type/property/additional-properties/main.test.ts` | get/put derived different spread float | 437-442 |\n| `type/property/additional-properties/main.test.ts` | get/put derived different spread model | 455-460 |\n| `type/property/additional-properties/main.test.ts` | get/put derived different spread model array | 473-478 |\n\n## Current Generated Code\n\n### Base type (WORKING):\n```typescript\nexport function extendsUnknownAdditionalPropertiesDeserializer(item: any) {\n  return {\n    additionalProperties: deserializeRecord(item, undefined, [\"name\"]),\n    name: item[\"name\"],\n  };\n}\n```\n\n### Derived type (BROKEN):\n```typescript\nexport function extendsUnknownAdditionalPropertiesDerivedDeserializer(item: any) {\n  return {\n    name: item[\"name\"],\n    index: item[\"index\"],\n    age: item[\"age\"],\n    // MISSING: additionalProperties: deserializeRecord(item, undefined, [\"name\", \"index\", \"age\"])\n  };\n}\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/type/property/additional-properties/mockapi.ts`:\nTests expect round-trip of additional properties. E.g., derived type: `{ name: \"abc\", index: 1, age: 2.5, prop1: 32, prop2: true }` where prop1/prop2 are additional properties that must be preserved.\n\n## Root Cause\n\nIn the serialization/deserialization components, when generating serializers for a model type, the code checks `type.additionalProperties` but doesn't walk the inheritance chain (`type.baseModel`) to detect inherited additional properties.\n\n## Recommended Fix\n\nIn serializer/deserializer generation:\n1. Walk the inheritance chain to detect inherited additional properties\n2. Generate `serializeRecord()`/`deserializeRecord()` with combined known property names from full hierarchy\n\nFor serializers: `...serializeRecord(item[\"additionalProperties\"] ?? {})`\nFor deserializers: `additionalProperties: deserializeRecord(item, valueDeserializer, [\"name\", \"index\", \"age\"])`\n\nThe 4 discriminated union tests are fixed automatically once derived deserializers are fixed.\n\n## Key Files\n\n- `src/components/serialization/json-serializer.tsx` \u2014 model serializer generation\n- `src/components/serialization/json-deserializer.tsx` \u2014 model deserializer generation\n- Check TCGC: `type.additionalProperties` vs `type.baseModel.additionalProperties`\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"AdditionalProperties\"\n```"
    },
    {
      "id": "E2E-FIX-ADDITIONAL-PROPS-UNION",
      "title": "Generate non-discriminated union deserialization for additional properties spread types",
      "category": "bug-fix/emitter",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [
        "E2E-FIX-ADDITIONAL-PROPS-DERIVED"
      ],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 6 skipped e2e tests caused by non-discriminated union deserializers returning raw data without type discrimination.\n\n## Problem\n\nNon-discriminated union deserializers are generated as simple passthroughs (`return item`), but wire data contains union types needing proper deserialization (e.g., Date parsing).\n\n## Affected Tests (6)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/property/additional-properties/main.test.ts` | get non-discriminated union spread | 526 |\n| `type/property/additional-properties/main.test.ts` | put non-discriminated union spread | 537 |\n| `type/property/additional-properties/main.test.ts` | get non-discriminated union2 spread | 549 |\n| `type/property/additional-properties/main.test.ts` | put non-discriminated union2 spread | 564 |\n| `type/property/additional-properties/main.test.ts` | get non-discriminated union3 spread | 580 |\n| `type/property/additional-properties/main.test.ts` | put non-discriminated union3 spread | 598 |\n\n## Current Generated Code (broken)\n\n```typescript\nexport function _spreadRecordForNonDiscriminatedUnionAdditionalPropertyDeserializer(\n  item: any,\n): _SpreadRecordForNonDiscriminatedUnionAdditionalProperty {\n  return item;  // No discrimination, no deserialization!\n}\n```\n\nUnion members include:\n- `WidgetData0`: `{ kind: \"kind0\", fooProp: string }`\n- `WidgetData1`: `{ kind: \"kind1\", start: Date, end?: Date }`\n- `WidgetData2`: `{ kind: \"kind1\", start: string }` (overlapping discriminator!)\n\n## Mock Server Expectations\n\nFrom additional-properties mockapi.ts:\n- `recordUnionBody`: `{ flag: true, prop1: \"abc\", prop2: 43.125 }`\n- `recordDiscriminatedUnionBody`: `{ name: \"abc\", prop1: { kind: \"kind0\", fooProp: \"abc\" }, prop2: { kind: \"kind1\", start: \"2021-01-01T00:00:00Z\", end: \"2021-01-02T00:00:00Z\" } }`\n\n## Root Cause\n\nThe emitter doesn't generate type discrimination logic for non-discriminated unions. Members with overlapping `kind` fields need secondary property checks.\n\n## Recommended Fix\n\nGenerate union deserialization that:\n1. Uses `kind` field for initial dispatch when members have distinct values\n2. For overlapping `kind` values, use secondary property checks (e.g., presence of `end` field)\n3. Apply member-specific deserialization (e.g., ISO date parsing)\n\nThis depends on E2E-FIX-ADDITIONAL-PROPS-DERIVED being completed first.\n\n## Key Files\n\n- `src/components/serialization/json-deserializer.tsx` \u2014 union deserialization generation\n- `src/components/serialization/json-serializer.tsx` \u2014 union serialization generation\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"AdditionalProperties\"\n```"
    },
    {
      "id": "E2E-FIX-PARAM-NAME-SHADOWING",
      "title": "Fix parameter name shadowing operation function import in client class methods",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 1 skipped e2e test caused by a generated method parameter shadowing an imported operation function.\n\n## Problem\n\nThe generated `UsageClient` class has `input(input: InputRecord, ...)` where parameter `input` shadows imported function `input` from `./api/operations.js`. At runtime: `TypeError: input is not a function`.\n\n## Affected Test (1)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/model/usage/main.test.ts` | should send an input-only model | 29 |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/type/model/usage/src/usageClient.ts`:\n```typescript\nimport { input, output, inputAndOutput } from \"./api/operations.js\";\n\nexport class UsageClient {\n  input(\n    input: InputRecord,  // Parameter \"input\" shadows imported function \"input\"\n    options: InputOptionalParams = { requestOptions: {} },\n  ): Promise<void> {\n    return input(this._client, input, options);  // Calls parameter, NOT function!\n  }\n}\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/type/model/usage/mockapi.ts`:\n```\nType_Model_Usage_input: POST /type/model/usage/input with { requiredProp: \"example-value\" } \u2192 204\n```\n\n## Root Cause\n\nIn client class method generation, when method body calls `operationName(this._client, param, options)`, if any parameter has the same name as the operation function, JS scoping causes shadowing.\n\n## Recommended Fix\n\nIn client class method generation, detect parameter/operation name collision. If collision exists, rename the parameter using the model name or add a suffix. E.g., `inputRecord` instead of `input`.\n\nAlternatively, import with aliases: `import { input as inputOp } from \"./api/operations.js\"`.\n\nThe existing name policy (`src/utils/name-policy.ts`) already handles reserved word conflicts \u2014 extend for operation name conflicts.\n\n## Key Files\n\n- `src/components/client.tsx` or `src/components/classical-client.tsx` \u2014 client class method generation\n- `src/utils/name-policy.ts` \u2014 name conflict resolution\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Type.Model.Usage\"\n```"
    },
    {
      "id": "E2E-FIX-INT64-SERIALIZATION",
      "title": "Fix int64 value handling for dictionary and array put operations",
      "category": "bug-fix/emitter",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 2 skipped e2e tests related to int64 value serialization.\n\n## Problem\n\nTests use BigInt literals (`0x7fffffffffffffffn`) but generated code types int64 as `number`. JSON.stringify doesn't support BigInt.\n\n## Affected Tests (2)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/dictionary/main.test.ts` | should put a dictionary of int64 values | 47 |\n| `type/array/main.test.ts` | should put an array of int64 values | 44 |\n\n## Mock Server Expectations\n\nThe mock server uses `Number.MAX_SAFE_INTEGER` (9007199254740991), NOT BigInt. The tests should match.\n\n## Recommended Fix\n\nThis is primarily a test fix. Update test expectations to use `Number.MAX_SAFE_INTEGER` instead of BigInt literals:\n```typescript\n// Before: 0x7fffffffffffffffn\n// After: Number.MAX_SAFE_INTEGER\n```\n\nThe generated code correctly types int64 as `number` which is the JavaScript convention.\n\n## Validation\n\n```bash\npnpm test:e2e -- --grep \"Type.Dictionary|Type.Array\"\n```"
    },
    {
      "id": "E2E-FIX-NULLABLE-MODEL-ARRAY",
      "title": "Add null guard in array serializer/deserializer for nullable model elements",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 2 skipped e2e tests caused by array serializers/deserializers crashing on null elements.\n\n## Problem\n\nWhen an array contains nullable model elements (`(InnerModel | null)[]`), the `.map()` calls model serializer/deserializer on every element including `null`, causing `TypeError: Cannot read properties of null`.\n\n## Affected Tests (2)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `type/array/main.test.ts` | should get an array of nullable model values | 174 |\n| `type/array/main.test.ts` | should put an array of nullable model values | 181 |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/type/array/src/models/models.ts`:\n```typescript\nexport function innerModelArrayDeserializer(result: Array<InnerModel>): any[] {\n  return result.map((item) => { return innerModelDeserializer(item); });\n  // CRASH when item is null!\n}\n```\n\n## Mock Server Expectations\n\nFrom array mockapi.ts:\n```\nType_Array_Nullable_Model: [{property: \"hello\"}, null, {property: \"world\"}]\n```\n\n## Root Cause\n\nArray serializer/deserializer generation doesn't check for nullable element types. The TCGC type model indicates nullable via `SdkNullableType` wrapping the element type.\n\n## Recommended Fix\n\nWhen element type is nullable, generate null guard:\n```typescript\nreturn result.map((item) => item === null ? null : innerModelDeserializer(item));\n```\n\nOnly add when TCGC indicates nullable elements (`type.valueType.kind === \"nullable\"`).\n\n## Key Files\n\n- `src/components/serialization/json-serializer.tsx` \u2014 array type in `getSerializationExpression()`\n- `src/components/serialization/json-deserializer.tsx` \u2014 array type in `getDeserializationExpression()`\n- `src/components/serialization/json-array-record-helpers.tsx` \u2014 `JsonArraySerializer`/`JsonArrayDeserializer`\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Type.Array\"\n```"
    },
    {
      "id": "E2E-FIX-CUSTOM-HTTP-AUTH",
      "title": "Map non-standard HTTP auth schemes to apiKey auth in client context generation",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 2 skipped e2e tests caused by custom HTTP authentication schemes not being recognized by the runtime auth policy.\n\n## Problem\n\nThe emitter generates `{ kind: \"http\", scheme: \"sharedaccesskey\" }` for custom HTTP auth, but the runtime only recognizes `kind: \"apiKey\"` for key-based auth and `kind: \"http\"` for basic/bearer.\n\n## Affected Tests (2)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `authentication/http/custom/main.test.ts` | should authenticate with a valid custom key | 37 |\n| `authentication/http/custom/main.test.ts` | should return error for an invalid custom key | 50 |\n\n## Current Generated Code (broken)\n\nIn `test/e2e/generated/authentication/http/custom/src/customClientContext.ts`:\n```typescript\nreturn getClient(endpointUrl, {\n  ...updatedOptions,\n  credential,\n  authSchemes: [{ kind: \"http\", scheme: \"sharedaccesskey\" }]  // Not recognized\n});\n```\n\nWorking comparison (API Key auth):\n```typescript\nauthSchemes: [{ kind: \"apiKey\", apiKeyLocation: \"header\", name: \"x-ms-api-key\" }]  // Works\n```\n\n## Mock Server Expectations\n\nFrom `node_modules/@typespec/http-specs/specs/authentication/http/custom/mockapi.ts`:\n- Valid: GET `/authentication/http/custom/valid` with header `Authorization: SharedAccessKey valid-key` \u2192 204\n- Invalid: GET `/authentication/http/custom/invalid` with header `Authorization: SharedAccessKey invalid-key` \u2192 403\n\n## Root Cause\n\nIn `src/components/client-context.tsx`, `buildAuthSchemesLiteral()` has:\n```typescript\ncase \"http\":\n  return `{ kind: \"http\", scheme: \"${scheme.scheme.toLowerCase()}\" }`;\n```\nThis generates `kind: \"http\"` for ALL HTTP schemes, but custom schemes need `kind: \"apiKey\"`.\n\n## Recommended Fix\n\nUpdate `buildAuthSchemesLiteral()` in `src/components/client-context.tsx`:\n```typescript\ncase \"http\": {\n  const schemeName = (scheme.scheme ?? \"\").toLowerCase();\n  if (schemeName === \"basic\" || schemeName === \"bearer\") {\n    return `{ kind: \"http\", scheme: \"${schemeName}\" }`;\n  }\n  return `{ kind: \"apiKey\", apiKeyLocation: \"header\", name: \"Authorization\" }`;\n}\n```\n\n## Key Files\n\n- `src/components/client-context.tsx` \u2014 `buildAuthSchemesLiteral()` function\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Authentication.Http.Custom\"\n```"
    },
    {
      "id": "E2E-FIX-BYTES-BODY-ENCODING",
      "title": "Generate encoding for standalone bytes body parameters with @encode decorator",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 6 skipped e2e tests caused by missing encoding/content-type handling for standalone `@body` bytes parameters.\n\nNOTE: Bytes query (4 tests) and header (4 tests) issues are in E2E-FIX-QUERY-PARAM-ENCODING and E2E-FIX-HEADER-PARAM-ENCODING.\n\n## Problem\n\nWhen a TypeSpec operation has a bare `@body bytes` with `@encode`, the emitter passes raw `Uint8Array` directly as HTTP body without encoding. For JSON bytes, body should be base64 string. For binary, raw bytes with correct content-type.\n\n## Affected Tests (6)\n\n### Request Body (3)\n| Test file | Test name | Line | Expected |\n|-----------|-----------|------|----------|\n| `encode/bytes/main.test.ts` | default (base64) JSON body request | 127 | `application/json` body `\"dGVzdA==\"` |\n| `encode/bytes/main.test.ts` | base64 body request | 139 | `application/json` body `\"dGVzdA==\"` |\n| `encode/bytes/main.test.ts` | base64url body request | 143 | `application/json` body `\"dGVzdA\"` |\n\n### Response Body (3)\n| Test file | Test name | Line | Expected |\n|-----------|-----------|------|----------|\n| `encode/bytes/main.test.ts` | default (base64) JSON body response | 155 | Binary stream \u2192 Uint8Array |\n| `encode/bytes/main.test.ts` | octet-stream response | 160 | Binary stream \u2192 Uint8Array |\n| `encode/bytes/main.test.ts` | custom content-type (image/png) response | 165 | Binary stream \u2192 Uint8Array |\n\n## Current Generated Code (broken)\n\n```typescript\n// Request: raw Uint8Array, no encoding\nreturn context.path(path).post({ body: value });\n\n// Response (partially working):\nreturn typeof result.body === \"string\"\n  ? stringToUint8Array(result.body, \"base64\")  // Works for JSON\n  : result.body;                                 // Works for binary\n```\n\n## Mock Server Expectations\n\nFrom bytes mockapi.ts:\n- `Encode_Bytes_RequestBody_base64`: expects `application/json` with raw body `'\"dGVzdA==\"'`\n- `Encode_Bytes_RequestBody_default`: expects `application/octet-stream` with raw binary\n- `Encode_Bytes_RequestBody_customContentType`: expects `image/png` with raw binary\n\n## Root Cause\n\nThe emitter handles bytes encoding in model properties (via serializers) but not for standalone `@body bytes` parameters.\n\n## Recommended Fix\n\nIn operation `_send` function generation, when body param is `bytes`:\n1. For `base64`/`base64url` with JSON: `body: JSON.stringify(uint8ArrayToString(value, \"base64\"))`\n2. For binary content types: `body: value` with correct `contentType`\n\n## Key Files\n\n- Operation generation code in `src/components/`\n- `src/components/serialization/json-serializer.tsx` \u2014 existing bytes encoding logic\n- `test/e2e/generated/encode/bytes/src/api/requestBody/operations.ts` \u2014 current broken output\n\n## Validation\n\n```bash\npnpm build && pnpm test:e2e -- --grep \"Encode.Bytes\"\n```"
    },
    {
      "id": "E2E-FIX-EMPTY-BODY-REST-ERROR",
      "title": "Fix createRestError crash on empty-body error responses (e.g., 500 with no body)",
      "category": "bug-fix/emitter",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-investigation",
      "description": "Fix 1 skipped e2e test caused by runtime crash when handling error responses with no body.\n\n## Problem\n\nThe `createRestError(result)` call in `@typespec/ts-http-runtime@0.2.1` crashes with `TypeError: Cannot read properties of undefined (reading 'message')` when `result.body` is undefined. This happens when a server returns an error status (e.g., 500) with no response body.\n\n## Affected Tests (1)\n\n| Test file | Test name | Line |\n|-----------|-----------|------|\n| `test/e2e/http/type/enum/fixed/main.test.ts` | should put an unknown enum value and receive 500 | 39 |\n\n## Root Cause\n\nIn `createRestError(result)`, when `result.body` is undefined:\n- `const internalError = resp.body?.error ?? resp.body` \u2192 undefined\n- `internalError.message` \u2192 TypeError (accessing property of undefined)\n\nThe runtime function lacks a null check for `internalError` before accessing `.message`.\n\n## Options\n\n1. **Fix in runtime**: Add `internalError?.message` (preferred but requires upstream PR to ts-http-runtime)\n2. **Fix in emitter**: Change generated error handling to use string overload: `createRestError('Unexpected status code: ' + result.status, result)` \u2014 but this changes all scenario test expectations\n3. **Workaround**: Generate conditional: `throw result.body !== undefined ? createRestError(result) : createRestError('Unexpected status code: ' + result.status, result)`\n\n## Validation\n\nAfter fix, unskip the test and run:\n```bash\npnpm build && pnpm test:e2e -- --grep \"Type.Enum.Fixed\"\n```"
    },
    {
      "id": "E2E-FIX-EMIT-RACE",
      "title": "Fix race condition in emit-e2e.ts for nested spec directories (type/union)",
      "category": "bug-fix/e2e-infrastructure",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "automated-detection",
      "description": "Fix the flaky ENOTEMPTY race condition in eng/scripts/emit-e2e.ts when two specs share a parent-child output directory relationship.\n\n## Problem\nWhen `type/union` (output: `test/e2e/generated/type/union/`) and `type/union/discriminated` (output: `test/e2e/generated/type/union/discriminated/`) are compiled in parallel, a race condition occurs:\n1. `type/union/discriminated` creates `type/union/discriminated/` via mkdir (also creating parent `type/union/`)\n2. `type/union` tries to `rm(type/union, {recursive: true})` to clean the output directory\n3. While rm is traversing the directory tree, `type/union/discriminated`'s tsp compile creates new files inside it\n4. This causes `ENOTEMPTY: directory not empty, rmdir` error\n\nThis is non-deterministic \u2014 depends on parallel execution timing. Seen consistently in full CI runs but may pass with fewer specs or different timing.\n\n## Fix approaches\n1. **Serialize nested specs**: Detect parent-child directory relationships and ensure parent specs are compiled only after child specs complete\n2. **Clean before parallel phase**: Do all directory cleanup in a serial pre-pass before starting any parallel compilation\n3. **Retry with backoff**: Wrap the rm/mkdir in a retry loop (fragile, not recommended)\n\nApproach 1 or 2 is recommended.\n\n## Impact\nBlocks reliable CI for e2e tests (SPECTOR-9 depends on this).\n\n## Validation\nRun `pnpm emit:e2e` 5+ times in a row \u2014 all should pass without ENOTEMPTY errors."
    },
    {
      "id": "E2E-AZURE-UNRESOLVED-SYMBOL-ARM",
      "title": "Fix <Unresolved Symbol> in generated ARM resource-manager code",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nGenerated code for 6 Azure Resource Manager specs contains `<Unresolved Symbol: refkey[...]>` placeholders in TypeScript output, causing parse/transform failures. The unresolved refkey appears to be for a provisioning state serializer (`refkey[o222\u2063sserializer]`).\n\n## Root Cause\nA refkey is created for a serializer function but the corresponding declaration component is never rendered. This is a critical emitter bug \u2014 the output is broken TypeScript that cannot compile.\n\n## Affected Specs (6)\n- `azure/resource-manager/resources` \u2014 models.ts:442\n- `azure/resource-manager/method-subscription-id` \u2014 models.ts:350\n- `azure/resource-manager/multi-service` \u2014 models.ts:255\n- `azure/resource-manager/multi-service-older-versions` \u2014 models.ts:263\n- `azure/resource-manager/multi-service-shared-models` \u2014 models.ts:273\n- `azure/resource-manager/operation-templates` \u2014 api/lro/operations.ts:152\n\n## Impact\n6 e2e test files removed; specs added to `test/e2e/.testignore`.\n\n## Relevant Files\n- `src/components/` \u2014 serializer generation components\n- `test/e2e/.testignore` \u2014 specs currently skipped\n- `test/e2e/generated/azure/resource-manager/` \u2014 generated output for inspection"
    },
    {
      "id": "E2E-AZURE-BEARER-TOKEN-HTTP",
      "title": "Fix ARM e2e tests: Bearer token auth rejected over non-TLS HTTP",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nAzure Resource Manager clients use `TokenCredential` (Bearer token auth) which requires HTTPS. The e2e mock server runs on `http://localhost:3002`, causing `@azure/core-rest-pipeline` to reject requests with:\n```\nBearer token authentication is not permitted for non-TLS protected (non-https) URLs.\n```\n\n## Root Cause\nThe generated ARM clients enforce TLS for Bearer tokens (correct production behavior), but e2e tests need to bypass this for the HTTP mock server. Need either:\n- A way to pass `allowInsecureConnection: true` that propagates to the bearer token policy\n- Or a test-only credential that doesn't enforce TLS\n\n## Affected Tests (8 tests across 3 files, all skipped)\n- `test/e2e/http/azure/resource-manager/common-properties/main.test.ts` \u2014 5 tests\n- `test/e2e/http/azure/resource-manager/large-header/main.test.ts` \u2014 1 test\n- `test/e2e/http/azure/resource-manager/non-resource/main.test.ts` \u2014 2 tests\n\n## Relevant Files\n- `src/context/flavor-context.tsx` \u2014 Azure flavor configuration\n- `src/utils/external-packages.ts` \u2014 `@azure/core-rest-pipeline` package definition\n- `test/e2e/http/azure/resource-manager/*/main.test.ts` \u2014 test files with `it.skip`"
    },
    {
      "id": "E2E-AZURE-CLIENT-INIT-PARAMS",
      "title": "Fix client-initialization: constructor params not propagated to HTTP requests",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nClient-initialization specs test that parameters passed to the client constructor (header params, query params, path params, param aliases) are correctly included in subsequent HTTP requests. The generated clients fail to propagate these constructor-level parameters to the underlying HTTP calls.\n\nSymptoms:\n- Header params: `Expected test-name-value but got undefined`\n- Query params: `Expected query param format=text but got undefined`\n- Path params: `Unexpected status code: 404` (path segment missing from URL)\n- Param aliases: `Unexpected status code: 404`\n- Body params: `Body provided doesn't match expected body`\n\n## Root Cause\nThe emitter generates client constructors that accept these parameters but doesn't wire them into the request pipeline or URL template.\n\n## Affected Tests (42 tests across 3 files, all skipped)\n- `test/e2e/http/azure/client-generator-core/client-initialization/default/main.test.ts` \u2014 12 tests\n- `test/e2e/http/azure/client-generator-core/client-initialization/individually/main.test.ts` \u2014 13 tests\n- `test/e2e/http/azure/client-generator-core/client-initialization/individuallyParent/main.test.ts` \u2014 17 tests\n\n## Relevant Files\n- `src/components/client-context.tsx` \u2014 Client context generation\n- `src/components/classical-client.tsx` \u2014 Classical client class generation\n- `test/e2e/generated/azure/client-generator-core/client-initialization/*/src/` \u2014 generated output"
    },
    {
      "id": "E2E-AZURE-LRO-RESULT-UNDEFINED",
      "title": "Fix LRO pollUntilDone() returning object with undefined properties",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nFor LRO operations, `pollUntilDone()` returns an object but its properties are `undefined`. For example:\n```\nconst result = await poller.pollUntilDone();\nresult.name // undefined (expected 'madge')\nresult.data  // undefined (expected 'text')\n```\n\nThe poller completes successfully but the final result deserialization is broken \u2014 the response body is not being correctly mapped to the result type properties.\n\n## Root Cause\nLikely an issue in the generated restore-poller helpers or the polling response deserialization pipeline. The LRO operation status response body may not be correctly deserialized into the target type.\n\n## Affected Tests (2 tests across 2 files, skipped)\n- `test/e2e/http/azure/core/lro/standard/main.test.ts` \u2014 'should export with LRO' (createOrReplace and delete work)\n- `test/e2e/http/azure/core/lro/rpc/main.test.ts` \u2014 'should perform long running RPC'\n\n## Relevant Files\n- `src/components/restore-poller.tsx` \u2014 LRO restore poller generation\n- `src/components/static-helpers/polling-helpers.tsx` \u2014 Polling helper generation\n- `test/e2e/generated/azure/core/lro/standard/src/` \u2014 generated LRO code\n- `test/e2e/generated/azure/core/lro/rpc/src/` \u2014 generated LRO RPC code"
    },
    {
      "id": "E2E-AZURE-TRAITS-REPEATABILITY",
      "title": "Fix Traits: Repeatability-Request-ID header not sent for repeatable actions",
      "category": "bug-fix/emitter",
      "priority": "P2",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nThe Azure Core Traits `repeatableAction` operation expects `Repeatability-Request-ID` and `Repeatability-First-Sent` headers to be included in the request, but the generated client does not send them.\n\nError: `Repeatability-Request-ID is missing`\n\n## Root Cause\nThe emitter does not generate code to include Azure repeatability headers for operations decorated with the repeatability trait. These headers should be auto-generated (UUID for request ID, ISO timestamp for first-sent).\n\n## Affected Tests (1 test, skipped)\n- `test/e2e/http/azure/core/traits/main.test.ts` \u2014 'should perform repeatable action'\n\n## Relevant Files\n- `src/components/public-operation.tsx` \u2014 Operation generation\n- `test/e2e/generated/azure/core/traits/src/` \u2014 generated traits code"
    },
    {
      "id": "E2E-AZURE-PAGE-QUERY-PARAMS",
      "title": "Fix e2e tests: pagination operations missing extra query params",
      "category": "bug-fix/e2e-tests",
      "priority": "P2",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nTwo pagination tests fail because extra query parameters are not sent:\n- `listWithParameters`: server expects `another=Second` query param but gets `undefined`\n- `withParameterizedNextLink`: server expects `includePending=true` but gets `undefined`\n\nThis may be a test issue (wrong params passed) or an emitter issue (params not serialized into the request).\n\n## Affected Tests (2 tests, skipped)\n- `test/e2e/http/azure/core/page/main.test.ts` \u2014 'should list with parameters'\n- `test/e2e/http/azure/core/page/main.test.ts` \u2014 'should list with parameterized next link'\n\n## Investigation Needed\n- Check generated code at `test/e2e/generated/azure/core/page/src/` for method signatures\n- Compare with legacy test at `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureCorePage.spec.ts`\n- Verify if the test is passing correct params or if emitter drops them"
    },
    {
      "id": "E2E-AZURE-ACCESS-WRONG-VALUES",
      "title": "Fix e2e tests: access spec sends wrong parameter values to mock server",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nTwo access tests fail because the test sends wrong parameter values:\n- `relativeModelInOperation > operation`: server expects `name=Madge` but test sends `sample`\n- `relativeModelInOperation > discriminator`: assertion expects `sample` but server returns `Madge`\n\n## Fix\nUpdate test to use correct values matching mock server expectations.\n\n## Affected Tests (2 tests, skipped)\n- `test/e2e/http/azure/client-generator-core/access/main.test.ts`\n\n## Relevant Files\n- `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureClientGeneratorCoreAccess.spec.ts` \u2014 legacy test for reference"
    },
    {
      "id": "E2E-AZURE-FLATTEN-WRONG-VALUES",
      "title": "Fix e2e tests: flatten-property spec sends wrong expected values",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nFlatten property tests fail because the test expects `name: 'foo'` but the server returns `name: 'test'`.\n\n## Fix\nUpdate test assertions to match actual mock server response values.\n\n## Affected Tests (2 tests, skipped)\n- `test/e2e/http/azure/client-generator-core/flatten-property/main.test.ts`\n\n## Relevant Files\n- `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/modelFlatten.spec.ts` \u2014 legacy test for reference"
    },
    {
      "id": "E2E-AZURE-CLIENT-DEFAULT-VALUE",
      "title": "Fix e2e tests: client-default-value path routing and content-type issues",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nTwo client-default-value tests fail:\n- `should get path parameter`: 404 error \u2014 URL path doesn't match expected route\n- `should get header parameter`: server expects `application/json;odata.metadata=none` content-type but gets `application/json`\n\nMay be test issue (wrong URLs/headers) or emitter issue (default values not applied).\n\n## Affected Tests (2 tests, skipped)\n- `test/e2e/http/azure/client-generator-core/client-default-value/main.test.ts`\n\n## Relevant Files\n- `test/e2e/generated/azure/client-generator-core/client-default-value/src/` \u2014 generated code"
    },
    {
      "id": "E2E-AZURE-CLIENT-LOCATION-PARAMS",
      "title": "Fix e2e test: client-location move-method-parameter-to-client wrong param value",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nThe move-method-parameter-to-client test fails because the server expects `storageAccount=testaccount` but the test sends `test-account`.\n\n## Fix\nUpdate test to use the correct parameter value matching mock server expectations.\n\n## Affected Tests (1 test, skipped)\n- `test/e2e/http/azure/client-generator-core/client-location/move-method-parameter-to-client/main.test.ts`\n\n## Relevant Files\n- `test/e2e/generated/azure/client-generator-core/client-location/move-method-parameter-to-client/src/` \u2014 generated code"
    },
    {
      "id": "E2E-AZURE-ENUM-CONFLICT-BODY",
      "title": "Fix e2e test: enum-conflict secondOperations sends wrong request body",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nThe enum-conflict `secondOperations.second()` test fails with `Body provided doesn't match expected body`.\n\n## Fix\nCheck the generated API for expected body shape and update test accordingly.\n\n## Affected Tests (1 test, skipped)\n- `test/e2e/http/client/naming/enum-conflict/main.test.ts`\n\n## Relevant Files\n- `test/e2e/generated/client/naming/enum-conflict/src/` \u2014 generated code\n- `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureClientNamingEnumConflict.spec.ts` \u2014 legacy test"
    },
    {
      "id": "E2E-AZURE-OVERLOAD-ROUTING",
      "title": "Fix e2e test: client/overload listByScope route not matching (404)",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nThe overload `listByScope(scope)` test fails with 404 \u2014 the generated URL doesn't match the mock server's expected route.\n\n## Fix\nCheck generated code for correct URL pattern and update test to pass correct scope value.\n\n## Affected Tests (1 test, skipped)\n- `test/e2e/http/client/overload/main.test.ts`\n\n## Relevant Files\n- `test/e2e/generated/client/overload/src/` \u2014 generated code\n- `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureClientOverload.spec.ts` \u2014 legacy test"
    },
    {
      "id": "E2E-AZURE-RESILIENCY-PARAMS",
      "title": "Fix e2e tests: resiliency/srv-driven sends wrong parameter values",
      "category": "bug-fix/e2e-tests",
      "priority": "P3",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-azure-http-specs",
      "description": "## Problem\nThree resiliency tests fail:\n- `fromNone()`: 400 error \u2014 unexpected request\n- `fromOneRequired(parameter)`: server expects `parameter=required` but test sends `new`\n- `fromOneOptional()`: server expects `parameter=optional` but gets `undefined`\n\n## Fix\nCheck mock server expectations and update test parameter values. Also verify the API version parameter is correct.\n\n## Affected Tests (3 tests, skipped)\n- `test/e2e/http/resiliency/srv-driven/main.test.ts`\n\n## Relevant Files\n- `test/e2e/generated/resiliency/srv-driven/src/` \u2014 generated code\n- `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/resiliencySrvDrivenMain.spec.ts` \u2014 legacy test"
    },
    {
      "id": "E2E-MISSING-CONDITIONAL-REQUEST",
      "title": "Add e2e test: special-headers/conditional-request",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `special-headers/conditional-request` Spector spec. The generated client code already exists at `test/e2e/generated/special-headers/conditional-request/src/`.\n\n## What to do\n1. Create `test/e2e/http/special-headers/conditional-request/main.test.ts`\n2. Import `ConditionalRequestClient` from `../../../../generated/special-headers/conditional-request/src/index.js`\n3. Instantiate with `endpoint: \"http://localhost:3002\"` and `allowInsecureConnection: true`\n4. Write 4 test cases covering:\n   - `headIfModifiedSince()` \u2014 sends `If-Modified-Since` header\n   - `postIfUnmodifiedSince()` \u2014 sends `If-Unmodified-Since` header\n   - `headIfMatch()` \u2014 sends `If-Match` etag header\n   - `headIfNoneMatch()` \u2014 sends `If-None-Match` etag header\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/conditionalRequest.spec.ts`\n- Generated client: `test/e2e/generated/special-headers/conditional-request/src/`\n- Follow the same test pattern as `test/e2e/http/special-headers/repeatability/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-PATH-PARAMETERS",
      "title": "Add e2e test: parameters/path",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `parameters/path` Spector spec. The generated client code already exists at `test/e2e/generated/parameters/path/src/`.\n\n## What to do\n1. Create `test/e2e/http/parameters/path/main.test.ts`\n2. Import `PathClient` from `../../../generated/parameters/path/src/index.js`\n3. Instantiate with `endpoint: \"http://localhost:3002\"` and `allowInsecureConnection: true`\n4. Write 3 test cases covering:\n   - `normal(\"foo\")` \u2014 standard path parameter\n   - `optional()` \u2014 omitting optional path parameter\n   - `optional({ name: \"foo\" })` \u2014 providing optional path parameter\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/pathParameters.spec.ts`\n- Generated client: `test/e2e/generated/parameters/path/src/`\n- Follow the same test pattern as `test/e2e/http/parameters/basic/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-CLIENT-NAMING",
      "title": "Add e2e test: client/naming",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `client/naming` Spector spec (the general naming test, NOT the enum-conflict sub-test which already exists). The generated client code already exists at `test/e2e/generated/client/naming/src/`.\n\n## What to do\n1. Create `test/e2e/http/client/naming/main.test.ts`\n2. Import `NamingClient` from `../../../generated/client/naming/src/index.js`\n3. Instantiate with `endpoint: \"http://localhost:3002\"` and `allowInsecureConnection: true`\n4. Write 11 test cases covering:\n   - `client({ clientName: true })` \u2014 client property naming\n   - `language({ tsName: true })` \u2014 language-specific property naming\n   - `compatibleWithEncodedName({ clientName: true })` \u2014 encoded name compatibility\n   - `request({ clientName: \"true\" })` \u2014 request header naming\n   - `response()` \u2014 response header naming (check `default-name` header)\n   - `clientName()` \u2014 operation client naming\n   - `parameter({ clientName: \"true\" })` \u2014 parameter naming\n   - Client name model operations\n   - Union enum member name operation\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/clientNaming.spec.ts`\n- Generated client: `test/e2e/generated/client/naming/src/`\n- Follow the same test pattern as `test/e2e/http/client/naming/enum-conflict/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-PAYLOAD-XML",
      "title": "Add e2e test: payload/xml",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `payload/xml` Spector spec. The generated client code already exists at `test/e2e/generated/payload/xml/src/`.\n\n## What to do\n1. Create `test/e2e/http/payload/xml/main.test.ts`\n2. Import `XmlClient` from `../../../generated/payload/xml/src/index.js`\n3. Instantiate with `endpoint: \"http://localhost:3002\"` and `allowInsecureConnection: true`\n4. Write 18 test cases covering get/put for each sub-group:\n   - `simpleModelValue.get()` / `simpleModelValue.put({ name: \"foo\", age: 123 })`\n   - `modelWithSimpleArraysValue.get()` / `.put(...)` \u2014 arrays of strings/ints\n   - `modelWithArrayOfModelValue.get()` / `.put(...)` \u2014 arrays of model objects\n   - `modelWithOptionalFieldValue.get()` / `.put(...)` \u2014 optional field handling\n   - `modelWithAttributesValue.get()` / `.put(...)` \u2014 XML attributes\n   - `modelWithUnwrappedArrayValue.get()` / `.put(...)` \u2014 unwrapped arrays\n   - `modelWithRenamedFieldsValue.get()` / `.put(...)` \u2014 XML field renaming\n   - `modelWithRenamedArraysValue.get()` / `.put(...)` \u2014 renamed arrays\n   - `modelWithTextValue.get()` / `.put(...)` \u2014 XML text nodes\n   - `modelWithDictionaryValue.get()` / `.put(...)` \u2014 dictionary serialization\n   - `modelWithEncodedNamesValue.get()` / `.put(...)` \u2014 encoded XML names\n   - `modelWithEmptyArrayValue.get()` / `.put(...)` \u2014 empty array handling\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/payloadXml.spec.ts`\n- Generated client: `test/e2e/generated/payload/xml/src/`\n- Follow the same test pattern as `test/e2e/http/payload/media-type/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-ARM-RESOURCES",
      "title": "Add e2e test: azure/resource-manager/resources",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `azure/resource-manager/resources` Spector spec. The generated client code already exists at `test/e2e/generated/azure/resource-manager/resources/src/`.\n\n## What to do\n1. Create `test/e2e/http/azure/resource-manager/resources/main.test.ts`\n2. Import `ResourcesClient` and model types from `../../../../../generated/azure/resource-manager/resources/src/index.js`\n3. Instantiate with subscription ID `\"00000000-0000-0000-0000-000000000000\"`, `endpoint: \"http://localhost:3002\"`, and a fake `TokenCredential`\n4. Write ~24 test cases covering:\n   - **Top-level tracked resources**: get, createOrReplace, update, delete, listByResourceGroup, actionSync\n   - **Nested proxy resources**: get, createOrReplace, update, delete, listByTopLevelTrackedResource\n   - **Singleton tracked resources**: getByResourceGroup, createOrUpdate, update\n   - **Location resources**: get, createOrUpdate, update, delete, listByLocation\n   - **Extensions resources**: get, createOrUpdate, update, delete, listByScope\n   - Verify systemData fields (createdBy, createdByType, createdAt, lastModifiedBy, etc.)\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureArmResources.spec.ts`\n- Generated client: `test/e2e/generated/azure/resource-manager/resources/src/`\n- Follow the same test pattern as `test/e2e/http/azure/resource-manager/common-properties/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-ARM-OPERATION-TEMPLATES",
      "title": "Add e2e test: azure/resource-manager/operation-templates",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `azure/resource-manager/operation-templates` Spector spec. The generated client code already exists at `test/e2e/generated/azure/resource-manager/operation-templates/src/`.\n\n## What to do\n1. Create `test/e2e/http/azure/resource-manager/operation-templates/main.test.ts`\n2. Import `OperationTemplatesClient` from `../../../../../generated/azure/resource-manager/operation-templates/src/index.js`\n3. Instantiate with subscription ID `\"00000000-0000-0000-0000-000000000000\"`, `endpoint: \"http://localhost:3002\"`, and a fake `TokenCredential`\n4. Write ~10 test cases covering:\n   - `operations.list()` \u2014 list available operations\n   - `checkGlobalNameAvailability(...)` \u2014 global name availability check\n   - `checkLocalNameAvailability(...)` \u2014 local name availability check\n   - LRO operations: `lroCreate(...)`, `lroExport(...)`, `lroDelete(...)`\n   - LRO + paging: `lroPagingList(...)`\n   - Optional body operations: `optionalBodiesGet(...)`, `optionalBodiesPatch(...)`, `optionalBodiesPost(...)`\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureArmOperationTemplates.spec.ts`\n- Generated client: `test/e2e/generated/azure/resource-manager/operation-templates/src/`\n- Follow the same test pattern as `test/e2e/http/azure/resource-manager/common-properties/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-ARM-METHOD-SUBSCRIPTION-ID",
      "title": "Add e2e test: azure/resource-manager/method-subscription-id",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `azure/resource-manager/method-subscription-id` Spector spec. The generated client code already exists at `test/e2e/generated/azure/resource-manager/method-subscription-id/src/`.\n\n## What to do\n1. Create `test/e2e/http/azure/resource-manager/method-subscription-id/main.test.ts`\n2. Import `MethodSubscriptionIdClient` from `../../../../../generated/azure/resource-manager/method-subscription-id/src/index.js`\n3. Instantiate with subscription ID `\"00000000-0000-0000-0000-000000000000\"`, `endpoint: \"http://localhost:3002\"`, and a fake `TokenCredential`\n4. Write ~11 test cases covering:\n   - `operations.list()` \u2014 list available operations\n   - Subscription resources: CRUD + list for `subscriptionResource1s` and `subscriptionResource2s` (different subscription ID handling)\n   - Resource group resources: CRUD + list for `resourceGroupResource1s`\n   - Verify correct subscription ID is sent at method-level vs client-level\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/azureArmMethodSubscriptionId.spec.ts`\n- Generated client: `test/e2e/generated/azure/resource-manager/method-subscription-id/src/`\n- Follow the same test pattern as `test/e2e/http/azure/resource-manager/common-properties/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "E2E-MISSING-CLIENT-ENABLE-NAMESPACE",
      "title": "Add e2e test: client/enableModelNamespace (generate + test)",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "blocked",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `client/enableModelNamespace` Spector spec. Unlike other missing tests, the generated client code does NOT yet exist \u2014 it must be generated first.\n\n## What to do\n1. Add the `client/enableModelNamespace` spec to the e2e generation config so `tsp compile` generates a client at `test/e2e/generated/client/enableModelNamespace/`\n   - Check how other specs are registered in the e2e generation pipeline (look at `test/e2e/` config files or scripts)\n2. Run the generation step to produce the client code\n3. Create `test/e2e/http/client/enableModelNamespace/main.test.ts`\n4. Import `ClientNamespaceFirstClient` and `ClientNamespaceSecondClient` from the generated code\n5. Instantiate each with `endpoint: \"http://localhost:3002\"` and `allowInsecureConnection: true`\n6. Write 2 test cases:\n   - `firstClient.getFirst()` \u2014 expect `result.name === \"first\"`\n   - `secondClient.getSecond()` \u2014 expect `result.type === \"second\"`\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/clientEnableModeNameSpace.spec.ts`\n- Follow the same test pattern as `test/e2e/http/client/namespace/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server.",
      "blockedReason": "The enable-model-namespace emitter option is not implemented in http-client-js. This option (legacy @azure-tools/typespec-ts specific) controls whether model names include their namespace hierarchy prefix (e.g. ClientclientnamespacefirstFirstClientResult vs FirstClientResult). Without it, compiling the same client/namespace spec would produce identical output to the existing client/namespace test, making this test a duplicate. TCGC does not handle this option either. A new emitter option and model naming logic must be implemented first."
    },
    {
      "id": "E2E-MISSING-RESILIENCY-SRV-DRIVEN-OLD",
      "title": "Add e2e test: resiliency/srv-driven-old (generate + test)",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-gap-analysis",
      "description": "## Summary\nAdd the missing e2e test for the `resiliency/srv-driven-old` Spector spec. Unlike other missing tests, the generated client code does NOT yet exist \u2014 it must be generated first. The existing `resiliency/srv-driven` test only covers the v2 (main/latest) version of the service-driven resiliency spec. This task covers the v1 (old) version.\n\n## What to do\n1. Add the `resiliency/srv-driven-old` spec to the e2e generation config so `tsp compile` generates a client at `test/e2e/generated/resiliency/srv-driven-old/`\n   - Check how other specs are registered in the e2e generation pipeline\n2. Run the generation step to produce the client code\n3. Create `test/e2e/http/resiliency/srv-driven-old/main.test.ts`\n4. Import `ResiliencyServiceDrivenClient` from the generated code\n5. Write 6 test cases for v1 and v2 API versions:\n   - **v1 client** (`apiVersion: \"v1\"`):\n     - `fromNone()` \u2014 no parameters\n     - `fromOneOptional({ parameter: \"optional\" })` \u2014 optional parameter\n     - `fromOneRequired(\"required\")` \u2014 required parameter\n   - **v2 client** (`apiVersion: \"v2\"`):\n     - `fromNone({ newParameter: \"new\" })` \u2014 added optional parameter\n     - `fromOneOptional({ parameter: \"optional\", newParameter: \"new\" })` \u2014 both parameters\n     - `fromOneRequired(\"required\", { newParameter: \"new\" })` \u2014 required + new optional\n\n## Reference\n- Legacy test: `submodules/autorest.typescript/packages/typespec-ts/test/azureModularIntegration/resiliencySrvDrivenOld.spec.ts`\n- Follow the same test pattern as `test/e2e/http/resiliency/srv-driven/main.test.ts`\n\n## Validation\nRun `pnpm test:e2e` and verify the new test passes against the Spector mock server."
    },
    {
      "id": "COVERAGE-FIX-SPREAD-PARAMS",
      "title": "Fix spread parameter invalid TypeScript generation",
      "category": "bug-fix/emitter",
      "priority": "P0",
      "status": "done",
      "kind": "bug-fix",
      "dependencies": [],
      "design_review": {
        "problem": "The emitter generates invalid TypeScript for spread parameters. When an operation uses @spread with header parameters, the generated interface signatures contain hyphenated names like `x-ms-test-header` which are not valid TypeScript identifiers. This blocks 11 Spector scenarios (all Parameters_Spread_Alias and Parameters_Spread_Model tests).",
        "approaches": [
          {
            "name": "Normalize names in operation signature generation",
            "description": "Fix the operation/function declaration code in send-operation.tsx to apply camelCase normalization when generating TypeScript parameter names from TCGC SdkHttpParameter objects. The serializedName (wire name) would still be used for HTTP headers/query strings, but the TypeScript-facing name would be camelCased. This is a surgical fix in the parameter name mapping layer.",
            "pros": [
              "Minimal change \u2014 only touches parameter name generation",
              "Matches how other parameter types already work",
              "No model/interface changes needed",
              "Wire compatibility preserved automatically via serializedName"
            ],
            "cons": [
              "Needs careful handling of name collisions after normalization",
              "Must update both classic interface declarations and API layer functions"
            ]
          },
          {
            "name": "Apply name policy at the model-interface level",
            "description": "Change model-interface.tsx to run all property names through the emitter's name policy (createEmitterNamePolicy) when rendering InterfaceMember names. This catches not just spread parameters but any property with non-TS-safe names. The serializer/deserializer would map between the normalized name and the wire name.",
            "pros": [
              "Catches ALL non-TS-safe property names globally",
              "Consistent with existing name policy infrastructure",
              "Future-proof for other property name edge cases"
            ],
            "cons": [
              "Broader change surface \u2014 may affect working scenarios",
              "Need to verify all serializer/deserializer property accessors still align",
              "Risk of regressions in already-passing tests"
            ]
          }
        ],
        "chosen": "Normalize names in operation signature generation",
        "rationale": "The surgical approach in send-operation.tsx is safer because it only affects spread parameters where the bug actually manifests. The model-interface approach risks regressions in the 648 already-passing tests. TCGC already provides a `name` field on method parameters that should be the TypeScript-safe name \u2014 the bug is that spread properties use the wire `serializedName` instead of the normalized `name`. The fix is to ensure spread property accessors use the parameter's TypeScript name, not its serialized name."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nFix invalid TypeScript generation for spread parameters. The emitter generates hyphenated parameter names (e.g., `x-ms-test-header`) from HTTP header serializedNames instead of camelCase TypeScript identifiers.\n\n## Root Cause\nIn `src/components/send-operation.tsx`, the `getSpreadPropertyAccessor()` function (lines ~1134-1152) matches spread body properties using `prop.name` which is the wire name. The generated interface in the classic layer uses these raw wire names as TypeScript identifiers.\n\n## Fix\n1. In the spread parameter generation path, apply camelCase normalization to property names used in TypeScript signatures\n2. Keep `serializedName` for wire-level property keys (JSON body construction, header names)\n3. Ensure the classic interface declarations use normalized names\n\n## Scenarios Unblocked (11)\n- Parameters_Spread_Alias_spreadAsRequestBody\n- Parameters_Spread_Alias_spreadAsRequestParameter\n- Parameters_Spread_Alias_spreadParameterWithInnerAlias\n- Parameters_Spread_Alias_spreadParameterWithInnerModel\n- Parameters_Spread_Alias_spreadWithMultipleParameters\n- Parameters_Spread_Model_spreadAsRequestBody\n- Parameters_Spread_Model_spreadCompositeRequest\n- Parameters_Spread_Model_spreadCompositeRequestMix\n- Parameters_Spread_Model_spreadCompositeRequestOnlyWithBody\n- Parameters_Spread_Model_spreadCompositeRequestWithoutBody\n\n## Validation\n1. Remove `.skip` from `test/e2e/http/parameters/spread/main.test.ts`\n2. Run `pnpm test:e2e` and verify all 11 spread scenarios pass\n3. Run `pnpm test` to verify no regressions in scenario tests"
    },
    {
      "id": "COVERAGE-FIX-ARRAY-DELIMITED-BODY",
      "title": "Fix string array delimiter encoding in JSON body properties",
      "category": "bug-fix/emitter",
      "priority": "P0",
      "status": "done",
      "kind": "bug-fix",
      "dependencies": [],
      "design_review": {
        "problem": "String array properties with delimiter encoding (@encode with csv/ssv/tsv/pipes) call buildCsvCollection() in the JSON body serializer, producing a joined string like 'a,b,c'. The Spector mock API expects the delimited string in the body (this IS correct per the spec), but the e2e tests were written expecting a JSON array. The issue is a test expectation mismatch, not a serializer bug \u2014 OR the serializer should not apply delimiter encoding for body properties and the spec expects a raw array. Need to verify against the actual mock API behavior.",
        "approaches": [
          {
            "name": "Fix the e2e test expectations to match spec behavior",
            "description": "The Spector spec for Encode_Array_Property defines body properties as delimiter-encoded strings. If the generated serializer correctly produces 'a,b,c' and the mock API accepts this, the fix is to update the test expectations to send/receive delimiter-encoded strings rather than arrays. Verify by running the tests without .skip and checking what the mock API returns.",
            "pros": [
              "No emitter changes needed",
              "Aligns tests with actual spec behavior",
              "Quick fix"
            ],
            "cons": [
              "Must verify against actual mock API responses",
              "If the mock API actually expects arrays, this approach is wrong"
            ]
          },
          {
            "name": "Remove delimiter encoding from body serializer path",
            "description": "Modify the JSON serializer to NOT apply buildCsvCollection() for body properties \u2014 delimiter encoding should only apply to query/header parameters. Body properties should pass arrays as-is (JSON arrays). The serializer would check the encoding context (body vs query vs header) before applying collection format.",
            "pros": [
              "Semantically correct \u2014 JSON bodies should contain arrays, not delimiter-joined strings",
              "Query and header paths continue to work correctly",
              "Matches standard JSON serialization expectations"
            ],
            "cons": [
              "Requires modifying the serializer code path",
              "Must verify the spec actually expects JSON arrays in body (not strings)",
              "Need to check if enum arrays work differently"
            ]
          }
        ],
        "chosen": "Investigate and verify against mock API, then apply the correct fix",
        "rationale": "Both approaches could be correct depending on what the Spector mock API actually expects. The first step is to remove the .skip, run the tests, and check the actual failure. The Spector spec uses @encode(ArrayEncoding.commaDelimited) on body properties, which semantically means the wire format should be a delimiter-joined string. If the mock API expects strings, fix the tests. If it expects arrays, fix the serializer. The enum array tests passing suggests the current serializer behavior may be correct for the spec."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nFix 4 string array delimiter encoding tests for body properties (Encode_Array_Property_commaDelimited, newlineDelimited, pipeDelimited, spaceDelimited). Enum and extensible enum variants already pass.\n\n## Investigation\nThe generated serializer calls `buildCsvCollection()` for string array body properties, producing delimiter-joined strings. The enum variants also call `buildCsvCollection()` and pass. The test expectations may be wrong (expecting arrays instead of strings).\n\n## Fix\n1. Remove `.skip` from the 4 string array tests in `test/e2e/http/encode/array/main.test.ts`\n2. Run tests to see actual failure mode\n3. If mock API accepts delimiter strings \u2192 fix test expectations\n4. If mock API expects arrays \u2192 fix serializer to not apply delimiter encoding in body context\n\n## Scenarios Unblocked (4)\n- Encode_Array_Property_commaDelimited\n- Encode_Array_Property_newlineDelimited\n- Encode_Array_Property_pipeDelimited\n- Encode_Array_Property_spaceDelimited"
    },
    {
      "id": "COVERAGE-FIX-UNIX-TIMESTAMP-BODY",
      "title": "Fix unix timestamp serialization for datetime body properties",
      "category": "bug-fix/emitter",
      "priority": "P0",
      "status": "done",
      "kind": "bug-fix",
      "dependencies": [],
      "design_review": {
        "problem": "Datetime properties encoded as unixTimestamp in JSON body position produce numeric values via the JSON serializer (e.g., 1686566864), but the round-trip fails. Header and query positions work because they explicitly wrap with String(). The JSON serializer generates `((accessor).getTime() / 1000) | 0` which returns a number \u2014 this should be correct for JSON bodies (numbers are valid JSON). The actual failure mode needs investigation: is it a serialization issue (number format wrong) or a deserialization issue (response parsing fails)?",
        "approaches": [
          {
            "name": "Fix the JSON deserializer to reconstruct Date from unix timestamp numbers",
            "description": "The serializer correctly converts Date \u2192 number. The likely issue is the deserializer: it may not convert the numeric unix timestamp back to a Date object. Add a case in json-deserializer.tsx for utcDateTime with unixTimestamp encoding that creates `new Date(value * 1000)` from the numeric response value.",
            "pros": [
              "Addresses the actual failure (likely deserialization)",
              "Symmetric with serializer logic",
              "Minimal change in json-deserializer.tsx"
            ],
            "cons": [
              "Need to verify this is actually the failure point",
              "Must handle edge cases (null, undefined, arrays)"
            ]
          },
          {
            "name": "Generate both serializer and deserializer as string conversions",
            "description": "Change both serializer and deserializer to work with string representations of unix timestamps, matching the header/query behavior. Serializer would produce String(date.getTime() / 1000) and deserializer would parse Number() then construct Date.",
            "pros": [
              "Consistent with header/query paths",
              "Simpler mental model \u2014 all unix timestamps are strings"
            ],
            "cons": [
              "JSON bodies should contain numbers, not strings \u2014 this would be semantically wrong",
              "Breaks JSON schema expectations",
              "Unnecessary string conversion"
            ]
          }
        ],
        "chosen": "Fix the JSON deserializer to reconstruct Date from unix timestamp numbers",
        "rationale": "JSON bodies should contain numeric values for unix timestamps (e.g., 1686566864, not '1686566864'). The serializer is likely correct. The deserializer needs to detect the unixTimestamp encoding and convert the response number back to a Date object via `new Date(value * 1000)`. This is the standard approach used by the legacy emitter and matches JSON semantics."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nFix unix timestamp encoding/decoding for datetime properties in JSON body position. Headers and queries work; body properties fail.\n\n## Root Cause\nIn `src/components/serialization/json-serializer.tsx` (lines 356-361), the serializer generates `((accessor).getTime() / 1000) | 0` for unixTimestamp encoding. The corresponding deserializer likely doesn't reconstruct a Date from the numeric value.\n\n## Fix\n1. Check `json-deserializer.tsx` for the utcDateTime/unixTimestamp case\n2. Add deserialization: `new Date(value * 1000)` for numeric unix timestamp values\n3. Handle arrays: map each element through the same conversion\n4. Remove `.skip` from tests in `test/e2e/http/encode/datetime/main.test.ts`\n\n## Scenarios Unblocked (2)\n- Encode_Datetime_Property_unixTimestamp\n- Encode_Datetime_Property_unixTimestampArray"
    },
    {
      "id": "COVERAGE-TEST-DURATION-MS-LARGERUNIT",
      "title": "Add e2e tests for duration millisecond and LargerUnit variants",
      "category": "testing/e2e-missing",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "28 duration encoding scenarios are not-implemented because the e2e test file only covers seconds-based variants and iso8601. All millisecond variants (int32Milliseconds, floatMilliseconds, float64Milliseconds) and LargerUnit variants are missing from the test file. The generated client has all methods present \u2014 this is purely a test coverage gap.",
        "approaches": [
          {
            "name": "Add all 28 test cases to existing main.test.ts",
            "description": "Extend test/e2e/http/encode/duration/main.test.ts with test cases for each missing variant. Follow the same pattern as existing seconds-based tests. Group by position (Query, Property, Header) and add millisecond + LargerUnit tests within each group. Some property tests may need .skip if they hit the same numeric duration serialization limitation as the existing skipped property tests.",
            "pros": [
              "Simple, direct approach",
              "Follows existing test structure",
              "Easy to verify completeness"
            ],
            "cons": [
              "Large test file",
              "Some tests may need to be skipped if runtime bugs exist for millisecond property serialization"
            ]
          },
          {
            "name": "Split into separate test files per position",
            "description": "Create separate test files for query, property, and header duration tests to parallelize execution and keep files focused.",
            "pros": [
              "Smaller files",
              "Better parallel execution",
              "Easier to debug failures"
            ],
            "cons": [
              "More file management",
              "Breaks from existing single-file pattern for encode tests",
              "More boilerplate"
            ]
          }
        ],
        "chosen": "Add all 28 test cases to existing main.test.ts",
        "rationale": "The existing pattern uses a single main.test.ts per spec area. Adding tests to the existing file is consistent and simpler. The file won't be excessively large (currently ~120 lines, will grow to ~250). Property-position tests that fail can be marked .skip with a reference to the numeric duration serialization limitation."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nAdd missing e2e test cases for 28 duration encoding scenarios covering millisecond variants and LargerUnit conversions across all three positions (Header, Property, Query).\n\n## Generated Client Methods\nAll methods exist in `test/e2e/generated/encode/duration/src/` \u2014 this is test-only work.\n\n## Test Cases to Add\n\n### Header (8 tests)\n- float64Milliseconds, floatMilliseconds, floatMillisecondsLargerUnit, floatSecondsLargerUnit\n- int32Milliseconds, int32MillisecondsArray, int32MillisecondsLargerUnit, int32SecondsLargerUnit\n\n### Property (12 tests)\n- float64Milliseconds, float64Seconds, floatMilliseconds, floatMillisecondsArray\n- floatMillisecondsLargerUnit, floatSeconds, floatSecondsArray, floatSecondsLargerUnit\n- int32Milliseconds, int32MillisecondsLargerUnit, int32Seconds, int32SecondsLargerUnit\n\n### Query (8 tests)\n- float64Milliseconds, floatMilliseconds, floatMillisecondsLargerUnit, floatSecondsLargerUnit\n- int32Milliseconds, int32MillisecondsArray, int32MillisecondsLargerUnit, int32SecondsLargerUnit\n\n## Notes\n- Property-position numeric duration tests may need `.skip` if they hit the same serialization limitation as the existing 4 skipped property tests\n- LargerUnit tests verify that values expressed in larger units (e.g., hours) are correctly converted to the target unit (e.g., seconds, milliseconds)\n- Check spector mock API docs for expected values for each test"
    },
    {
      "id": "COVERAGE-TEST-PAGEABLE-GAPS",
      "title": "Add missing e2e test cases for pageable scenarios",
      "category": "testing/e2e-missing",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "7 pageable scenarios have generated client methods but no test coverage. The test file exists with 5 passing tests but doesn't cover pageSize, linkString, nestedLink, XML pagination, or nested response body continuation token variants.",
        "approaches": [
          {
            "name": "Add all 7 test cases to existing main.test.ts",
            "description": "Extend test/e2e/http/payload/pageable/main.test.ts with the missing test cases. Each test follows the same pattern: create client, call pageable method, iterate pages, verify results. XML pagination tests will verify XML deserialization works with pagination.",
            "pros": [
              "Consistent with existing file",
              "Reuses client setup",
              "Easy to compare with existing passing tests"
            ],
            "cons": [
              "None significant \u2014 this is straightforward test addition"
            ]
          },
          {
            "name": "Create separate test files per pageable variant",
            "description": "Create main.test.ts files under subdirectories (page-size/, xml-pagination/, etc.).",
            "pros": [
              "Finer-grained test isolation"
            ],
            "cons": [
              "Unnecessary complexity \u2014 all tests use the same generated client",
              "Breaks from established pattern"
            ]
          }
        ],
        "chosen": "Add all 7 test cases to existing main.test.ts",
        "rationale": "All pageable tests use the same generated PageableClient. Adding to the existing file is simpler and matches the pattern. The nested response body variants are the most interesting \u2014 they test that the paging implementation can extract items and tokens from nested response structures."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nAdd 7 missing test cases to `test/e2e/http/payload/pageable/main.test.ts`.\n\n## Test Cases to Add\n1. **serverDrivenPagination.linkString()** \u2014 pagination with string nextLink (not URL type)\n2. **serverDrivenPagination.nestedLink()** \u2014 pagination with nested link in response\n3. **pageSize.listWithPageSize()** \u2014 pagination with explicit page size parameter\n4. **pageSize.listWithoutContinuation()** \u2014 single page, no continuation\n5. **continuationToken.requestQueryNestedResponseBody()** \u2014 token in query, items/token in nested response objects\n6. **continuationToken.requestHeaderNestedResponseBody()** \u2014 token in header, items/token in nested response objects\n7. **xmlPagination.listWithNextLink()** \u2014 XML response with nextLink pagination\n8. **xmlPagination.listWithContinuation()** \u2014 XML response with continuation token\n\n## Notes\n- All methods exist in generated client\n- Follow the existing test pattern (create client, iterate pages, verify)\n- XML pagination tests verify that XML deserialization works correctly with the paging helper"
    },
    {
      "id": "COVERAGE-FIX-VISIBILITY-SERIALIZATION",
      "title": "Fix visibility-aware serialization for model properties",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug-fix",
      "dependencies": [],
      "design_review": {
        "problem": "The Type_Model_Visibility spec requires that different HTTP verbs include/exclude different properties based on lifecycle visibility decorators. For example, POST should only include @visibility(Create) properties, GET responses should only include @visibility(Read) properties, PATCH should only include @visibility(Update) properties. Currently, the emitter generates a single serializer that includes ALL properties regardless of the operation context. This means: (1) PUT sends readProp (should be excluded), (2) POST sends updateProp and deleteProp (should be excluded), (3) PATCH sends createProp and deleteProp (should be excluded).",
        "approaches": [
          {
            "name": "Generate per-operation serializers that filter by visibility",
            "description": "Generate separate serializer functions for different visibility contexts: e.g., visibilityModelCreateSerializer (only Create-visible props), visibilityModelUpdateSerializer (only Update-visible props), etc. Each operation would call the appropriate serializer based on its HTTP verb mapping. The TCGC model already provides visibility metadata per property.",
            "pros": [
              "Correct semantics \u2014 each verb sends only relevant properties",
              "Simple runtime behavior \u2014 just different serializer functions",
              "No runtime visibility checks needed"
            ],
            "cons": [
              "Multiple serializer functions per model (up to 5: create, update, read, delete, query)",
              "Increases generated code size",
              "Need to map HTTP verbs to visibility contexts"
            ]
          },
          {
            "name": "Generate a single serializer with a visibility context parameter",
            "description": "Generate one serializer function that accepts a visibility context parameter and conditionally includes/excludes properties at runtime based on the context. E.g., visibilityModelSerializer(item, context: 'create' | 'update' | 'read' | 'delete').",
            "pros": [
              "Single serializer function \u2014 less generated code",
              "Flexible \u2014 can handle any visibility context"
            ],
            "cons": [
              "Runtime branching for every property on every call",
              "More complex generated code",
              "Harder to tree-shake unused visibility contexts",
              "More complex to debug"
            ]
          },
          {
            "name": "Use TCGC's usage-based type splitting",
            "description": "TCGC already computes which properties are visible for each usage context. Use TCGC's SdkModelType.usage flags to generate separate TypeScript types (e.g., VisibilityModelInput, VisibilityModelOutput) with only the relevant properties. Each type gets its own serializer. Operations use the appropriate input/output type.",
            "pros": [
              "Type-safe \u2014 TypeScript enforces correct property sets at compile time",
              "TCGC does the heavy lifting of visibility computation",
              "Matches pattern used by other language emitters (Python, Java)"
            ],
            "cons": [
              "Generates more types \u2014 could confuse SDK users with too many model variants",
              "Complex mapping between types",
              "Significant refactor of model generation"
            ]
          }
        ],
        "chosen": "Generate per-operation serializers that filter by visibility",
        "rationale": "This approach is the most practical for the current architecture. TCGC provides visibility metadata per property (property.visibility array), so the serializer generator can check which Lifecycle values a property has and include/exclude it accordingly. Per-operation serializers are simple, don't require runtime branching, and don't require splitting model types (which would be a larger refactor). The send-operation.tsx code already knows the HTTP verb, so it can select the correct serializer. This matches the approach used by flight-instructor for similar scenarios."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nFix visibility-aware serialization so that different HTTP operations only serialize properties matching their lifecycle context.\n\n## Root Cause\nThe JSON serializer (`src/components/serialization/json-serializer.tsx`) generates a single serializer per model that includes ALL properties. It does not filter by visibility context (Read, Create, Update, Delete, Query).\n\n## Fix\n1. In json-serializer.tsx, when generating serializer for a model with visibility-decorated properties, generate separate serializer functions per visibility context\n2. In send-operation.tsx, select the appropriate serializer based on the HTTP method:\n   - GET \u2192 no body (or read-context deserializer for response)\n   - POST \u2192 create-context serializer\n   - PUT \u2192 create+update-context serializer\n   - PATCH \u2192 update-context serializer\n   - DELETE \u2192 delete-context serializer\n3. In json-deserializer.tsx, generate a read-context deserializer that only includes Read-visible properties\n\n## Scenarios Unblocked (7)\n- Type_Model_Visibility_deleteModel\n- Type_Model_Visibility_getModel\n- Type_Model_Visibility_headModel\n- Type_Model_Visibility_patchModel\n- Type_Model_Visibility_postModel\n- Type_Model_Visibility_putModel\n- Type_Model_Visibility_putReadOnlyModel\n\n## Validation\n1. Create `test/e2e/http/type/model/visibility/main.test.ts`\n2. Verify each operation sends only the expected properties\n3. Run `pnpm test` for regression check"
    },
    {
      "id": "COVERAGE-FIX-DISCRIMINATED-UNION-SERIALIZATION",
      "title": "Fix discriminated union serializer generation",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug-fix",
      "dependencies": [],
      "design_review": {
        "problem": "The emitter generates pass-through serializers for discriminated unions (just returns the input unchanged). The Spector spec defines 4 discriminated union patterns: envelope with default kind/value, envelope with custom petType/petData, no-envelope with default discriminator, no-envelope with custom discriminator. The generated serializer needs to handle discriminator-based variant selection and envelope wrapping/unwrapping.",
        "approaches": [
          {
            "name": "Generate switch-based serializers on the discriminator property",
            "description": "In json-union-serializer.tsx, detect discriminated unions (unions where variants share a common discriminator property with constant values) and generate a switch statement that serializes the correct variant. For envelope types, wrap the variant data in the envelope structure. For no-envelope types, serialize inline with the discriminator.",
            "pros": [
              "Explicit and debuggable \u2014 clear switch/case for each variant",
              "Handles both envelope and no-envelope patterns",
              "Type-safe discrimination"
            ],
            "cons": [
              "Code size grows with number of variants",
              "Must handle both serialization and deserialization directions"
            ]
          },
          {
            "name": "Use the existing union-discrimination.ts infrastructure",
            "description": "The emitter already has `union-discrimination.ts` which analyzes unions to find discriminator properties and generates discrimination logic for model hierarchies. Extend this to also handle TypeSpec union types (not just model inheritance) by reusing the same discriminator detection and adding union-specific serialization templates.",
            "pros": [
              "Reuses existing infrastructure",
              "Consistent with polymorphic model handling",
              "Less new code"
            ],
            "cons": [
              "union-discrimination.ts is designed for model inheritance, not TypeSpec unions \u2014 may need significant adaptation",
              "Risk of conflating two different concepts"
            ]
          }
        ],
        "chosen": "Generate switch-based serializers on the discriminator property",
        "rationale": "Discriminated TypeSpec unions are fundamentally different from discriminated model inheritance. The union-discrimination.ts infrastructure handles polymorphic base/derived models (where all variants extend a common base type), while these discriminated unions are standalone variants distinguished by a property. A clean switch-based approach in json-union-serializer.tsx is clearer and avoids conflating concepts. The discriminator property and variant mapping can be extracted from the TCGC SdkUnionType's variants."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nFix discriminated union serializer/deserializer generation to handle envelope and no-envelope discriminated union patterns.\n\n## Root Cause\nIn `src/components/serialization/json-union-serializer.tsx`, discriminated unions generate pass-through serializers (`return item`) instead of proper discriminator-aware serialization.\n\n## Fix\n1. Detect discriminated unions in json-union-serializer.tsx (unions with a shared constant-valued discriminator property)\n2. Generate switch-case serializers that:\n   - Read the discriminator property value\n   - Call the correct variant serializer\n   - For envelope types: wrap in `{kind: \"...\", value: serialized}` (or custom property names)\n   - For no-envelope types: include discriminator inline with serialized properties\n3. Generate corresponding deserializers that:\n   - Read discriminator from response\n   - Unwrap envelope if needed\n   - Call correct variant deserializer\n\n## Scenarios Unblocked (8)\n- Type_Union_Discriminated_Envelope_Object_Default_get/put\n- Type_Union_Discriminated_Envelope_Object_CustomProperties_get/put\n- Type_Union_Discriminated_NoEnvelope_Default_get/put\n- Type_Union_Discriminated_NoEnvelope_CustomDiscriminator_get/put\n\n## Validation\n1. Add discriminated union tests to `test/e2e/http/type/union/main.test.ts`\n2. Verify round-trip serialization for all 4 patterns\n3. Run `pnpm test` for regression check"
    },
    {
      "id": "COVERAGE-FIX-MERGE-PATCH-SERIALIZATION",
      "title": "Implement JSON merge patch serialization with null preservation",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "JSON merge patch (RFC 7386) requires that null values are preserved in the serialized body to signal property deletion. The current JSON serializer strips null/undefined values, which is correct for standard JSON but breaks merge patch semantics. The emitter has no content-type awareness in its serialization path \u2014 serializers don't know if they're being used for application/json or application/merge-patch+json.",
        "approaches": [
          {
            "name": "Generate a separate merge-patch serializer variant",
            "description": "When TCGC marks an operation's body content-type as application/merge-patch+json, generate a dedicated serializer function (e.g., resourceMergePatchSerializer) that preserves null values instead of stripping them. The operation would call this specific serializer instead of the standard one. The serializer would use explicit null checks (value === null ? null : serialize(value)) instead of the usual undefined-stripping pattern.",
            "pros": [
              "Clean separation \u2014 standard serializer unchanged",
              "No runtime content-type checks",
              "Explicit and debuggable",
              "Matches legacy emitter pattern"
            ],
            "cons": [
              "Duplicate serializer code for merge-patch models",
              "Need to detect merge-patch context from TCGC types"
            ]
          },
          {
            "name": "Add a preserveNulls parameter to existing serializers",
            "description": "Add a boolean parameter to the generated serializer function that controls null-stripping behavior. The operation code would pass preserveNulls=true when the content-type is merge-patch+json. The serializer would conditionally include or strip nulls based on this flag.",
            "pros": [
              "Single serializer function per model",
              "Less generated code"
            ],
            "cons": [
              "Runtime branching in every serializer call",
              "Doesn't handle nested models correctly (flag must propagate)",
              "More complex generated code",
              "Harder to tree-shake"
            ]
          },
          {
            "name": "Make all properties optional with explicit null type in merge-patch models",
            "description": "For merge-patch models, generate TypeScript types where all updateable properties are T | null | undefined. The serializer would then naturally include null values (since they're explicitly typed) and strip undefined values (which represent 'not set').",
            "pros": [
              "Type-safe \u2014 TypeScript enforces the null/undefined distinction",
              "Clear semantics: null = delete, undefined = skip"
            ],
            "cons": [
              "Requires different model types for merge-patch vs standard",
              "More complex type generation",
              "Users must understand null vs undefined distinction"
            ]
          }
        ],
        "chosen": "Generate a separate merge-patch serializer variant",
        "rationale": "A dedicated merge-patch serializer is the cleanest approach. TCGC's SdkHttpOperation already provides the content type, so the emitter can detect merge-patch context at code generation time. The dedicated serializer preserves null values explicitly, which is simple to implement and doesn't require runtime branching or model type changes. This matches how the legacy emitter handles merge-patch and avoids polluting the standard serializer code path."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nImplement JSON merge patch serialization that preserves null values per RFC 7386.\n\n## Root Cause\nThe JSON serializer in `src/components/serialization/json-serializer.tsx` strips null/undefined values. For `application/merge-patch+json`, null values must be preserved to signal property deletion.\n\n## Fix\n1. Detect merge-patch content type from TCGC's SdkHttpOperation\n2. Generate a dedicated merge-patch serializer that preserves null values\n3. In send-operation.tsx, use the merge-patch serializer when content-type is `application/merge-patch+json`\n4. Create `test/e2e/http/payload/json-merge-patch/main.test.ts`\n\n## Scenarios Unblocked (3)\n- Payload_JsonMergePatch_createResource\n- Payload_JsonMergePatch_updateOptionalResource\n- Payload_JsonMergePatch_updateResource\n\n## Validation\n1. Verify null values are preserved in serialized merge-patch body\n2. Verify standard JSON serialization still strips nulls\n3. Run `pnpm test` for regression check"
    },
    {
      "id": "COVERAGE-TEST-FILE-BODY",
      "title": "Add e2e tests for Type_File_Body scenarios",
      "category": "testing/e2e-missing",
      "priority": "P1",
      "status": "done",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "8 file upload/download scenarios have fully generated and working client code but no e2e test file. The emitter correctly handles binary bodies, content-type headers, and File model types. This is a test-only gap.",
        "approaches": [
          {
            "name": "Create a single main.test.ts with all 8 file body tests",
            "description": "Create test/e2e/http/type/file/main.test.ts with tests for all upload and download variants. Tests would use Uint8Array buffers as file content and verify content-type handling across the 4 content type variants (default/octet-stream, JSON, specific/image-png, multiple).",
            "pros": [
              "Matches existing test pattern",
              "All file tests in one place",
              "Simple to write"
            ],
            "cons": [
              "None significant"
            ]
          },
          {
            "name": "Create separate test files for upload and download",
            "description": "Split into upload.test.ts and download.test.ts for clarity.",
            "pros": [
              "Logical separation"
            ],
            "cons": [
              "Unnecessary \u2014 both directions use the same client and setup"
            ]
          }
        ],
        "chosen": "Create a single main.test.ts with all 8 file body tests",
        "rationale": "Standard pattern. All 8 tests use the same generated BodyClient and share the same test setup."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nCreate e2e test file for 8 Type_File_Body scenarios. Generated code is complete \u2014 this is test-only work.\n\n## Test File\nCreate `test/e2e/http/type/file/main.test.ts`\n\n## Test Cases\n1. uploadFileDefaultContentType \u2014 upload with default content type (application/octet-stream)\n2. uploadFileSpecificContentType \u2014 upload with image/png\n3. uploadFileJsonContentType \u2014 upload with application/json\n4. uploadFileMultipleContentTypes \u2014 upload with content negotiation\n5. downloadFileDefaultContentType \u2014 download with default content type\n6. downloadFileSpecificContentType \u2014 download with image/png\n7. downloadFileJsonContentType \u2014 download with application/json\n8. downloadFileMultipleContentTypes \u2014 download with content negotiation\n\n## Notes\n- Use Uint8Array for binary content\n- Verify correct Content-Type/Accept headers\n- Generated client at `test/e2e/generated/type/file/src/`"
    },
    {
      "id": "COVERAGE-TEST-AUTH-NOAUTH-UNION",
      "title": "Add e2e tests for Authentication_Noauth_Union scenarios",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "2 authentication scenarios combining NoAuth and OAuth2 union are generated but have no test file. The spec tests that a client configured with NoAuth | OAuth2 auth can make requests both without auth and with a bearer token.",
        "approaches": [
          {
            "name": "Create main.test.ts at test/e2e/http/authentication/noauth/union/",
            "description": "Create a test file that matches the generated code path. Test validNoAuth (request without auth headers, expects 204) and validToken (request with Bearer token, expects 204).",
            "pros": [
              "Matches generated code directory structure",
              "Clear and specific"
            ],
            "cons": [
              "Deep nesting (noauth/union/)"
            ]
          },
          {
            "name": "Add to existing authentication test directory",
            "description": "Add noauth union tests alongside the existing authentication/union tests.",
            "pros": [
              "Less directory nesting"
            ],
            "cons": [
              "Different generated clients \u2014 would need separate imports"
            ]
          }
        ],
        "chosen": "Create main.test.ts at test/e2e/http/authentication/noauth/union/",
        "rationale": "The generated code is at test/e2e/generated/authentication/noauth/union/, so the test file should mirror the path at test/e2e/http/authentication/noauth/union/main.test.ts."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nCreate e2e test file for 2 Authentication_Noauth_Union scenarios.\n\n## Test File\nCreate `test/e2e/http/authentication/noauth/union/main.test.ts`\n\n## Test Cases\n1. validNoAuth \u2014 send request without authentication headers, expect 204\n2. validToken \u2014 send request with Bearer token authentication, expect 204\n\n## Notes\n- Generated client at `test/e2e/generated/authentication/noauth/union/src/`\n- The NoAuth | OAuth2 union means the client should accept either no credentials or an OAuth2 token"
    },
    {
      "id": "COVERAGE-TEST-RESPONSE-STATUS-CODE-RANGE",
      "title": "Add e2e tests for Response_StatusCodeRange scenarios",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "2 status code range scenarios have generated code but no test file. The spec tests that the client correctly handles error responses for specific status codes (404) and status code ranges (494-499), deserializing the appropriate error model.",
        "approaches": [
          {
            "name": "Create main.test.ts testing error response handling",
            "description": "Create test file that exercises the generated client's error handling. Test that status code 404 returns the specific error model, and that status codes in the 494-499 range are properly handled.",
            "pros": [
              "Direct and simple",
              "Tests important error handling behavior"
            ],
            "cons": [
              "May need special error assertion patterns (expect().rejects)"
            ]
          },
          {
            "name": "Test via error interception/inspection",
            "description": "Use try/catch patterns to verify the error type and properties returned by the client for different status codes.",
            "pros": [
              "More explicit error inspection"
            ],
            "cons": [
              "More verbose test code"
            ]
          }
        ],
        "chosen": "Create main.test.ts testing error response handling",
        "rationale": "The standard vitest pattern with expect().rejects is clean and matches how other error tests work in the codebase."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nCreate e2e test file for 2 Response_StatusCodeRange scenarios.\n\n## Test File\nCreate `test/e2e/http/response/status-code-range/main.test.ts`\n\n## Test Cases\n1. errorResponseStatusCode404 \u2014 verify client throws/returns specific error model for 404\n2. errorResponseStatusCodeInRange \u2014 verify client handles 494-499 status codes with appropriate error models\n\n## Notes\n- Generated client at `test/e2e/generated/response/status-code-range/src/`\n- Tests should verify both the error type and the deserialized error body"
    },
    {
      "id": "COVERAGE-TEST-VERSIONING",
      "title": "Add e2e tests for Versioning scenarios",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "2 versioning scenarios have generated code but no test file. Versioning_Removed_modelV3_V2preview tests that a model marked @removed in v2 is correctly hidden at the v2preview version. Versioning_ReturnTypeChangedFrom_test tests that a return type change from int32 to string is handled correctly.",
        "approaches": [
          {
            "name": "Create per-scenario test files under test/e2e/http/versioning/",
            "description": "Create separate test files for each versioning spec (removed/, returnTypeChangedFrom/) matching the generated code structure. Each test would instantiate the client with the appropriate version parameter.",
            "pros": [
              "Matches generated code directory structure",
              "Independent test files",
              "Can run specific versioning tests"
            ],
            "cons": [
              "More files"
            ]
          },
          {
            "name": "Create a single versioning/main.test.ts",
            "description": "Group all versioning tests in one file with describe blocks per scenario.",
            "pros": [
              "Fewer files"
            ],
            "cons": [
              "Different generated clients for each spec \u2014 messier imports",
              "Doesn't match directory structure"
            ]
          }
        ],
        "chosen": "Create per-scenario test files under test/e2e/http/versioning/",
        "rationale": "Each versioning scenario uses a different generated client (RemovedClient, ReturnTypeChangedFromClient), so separate files with separate imports is cleaner. This matches the generated code structure."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nCreate e2e test files for 2 Versioning scenarios.\n\n## Test Files\n1. `test/e2e/http/versioning/removed/main.test.ts` \u2014 test Versioning_Removed_modelV3_V2preview\n2. `test/e2e/http/versioning/return-type-changed-from/main.test.ts` \u2014 test Versioning_ReturnTypeChangedFrom_test\n\n## Test Cases\n### Removed\n- Create client with version='v2preview'\n- Call modelV3 operation\n- Verify the response handles the removed model correctly\n\n### ReturnTypeChangedFrom\n- Create client with latest version\n- Call test operation\n- Verify return type is string (changed from int32 in v1)\n\n## Notes\n- Generated clients at `test/e2e/generated/versioning/removed/` and `test/e2e/generated/versioning/returnTypeChangedFrom/`\n- Known issue: ReturnTypeChangedFrom test may fail due to Content-Type mismatch (mock expects text/plain)"
    },
    {
      "id": "COVERAGE-TEST-MULTIPART-ANONYMOUS",
      "title": "Add e2e test for Payload_MultiPart_FormData_anonymousModel",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "The anonymousModel multipart scenario is generated but has no test case in the existing multipart test file. The spec tests that the client can handle multipart requests with inline (anonymous) model types.",
        "approaches": [
          {
            "name": "Add test case to existing multipart main.test.ts",
            "description": "Add an it() block to the existing test/e2e/http/payload/multipart/main.test.ts for the anonymousModel operation.",
            "pros": [
              "Minimal change",
              "Reuses existing client setup"
            ],
            "cons": [
              "None"
            ]
          },
          {
            "name": "Create separate test file for anonymous model",
            "description": "Create a new test file specifically for the anonymous model scenario.",
            "pros": [
              "Isolation"
            ],
            "cons": [
              "Unnecessary overhead for a single test case"
            ]
          }
        ],
        "chosen": "Add test case to existing multipart main.test.ts",
        "rationale": "The multipart test file already has 16 tests. Adding one more for anonymousModel is the simplest approach."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nAdd 1 test case for Payload_MultiPart_FormData_anonymousModel to existing multipart test file.\n\n## Fix\nAdd test in `test/e2e/http/payload/multipart/main.test.ts`:\n```typescript\nit('should send multipart request with anonymous model', async () => {\n  await client.formData.anonymousModel({\n    profileImage: createFilePartDescriptor('profileImage', jpegContents, 'image/jpeg'),\n  });\n});\n```\n\n## Notes\n- The spec defines an inline body type `{ profileImage: HttpPart<bytes> }` instead of a named model\n- Verify the generated client has the anonymousModel method"
    },
    {
      "id": "COVERAGE-TEST-XML-ERROR-VALUE",
      "title": "Add e2e test for Payload_Xml_XmlErrorValue_get",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "The XML error value scenario is generated but has no test case. The spec tests that XML error responses are correctly deserialized.",
        "approaches": [
          {
            "name": "Add test case to existing XML main.test.ts",
            "description": "Add a test case to test/e2e/http/payload/xml/main.test.ts for the XmlErrorValue_get scenario.",
            "pros": [
              "Minimal change",
              "Reuses existing client setup"
            ],
            "cons": [
              "None"
            ]
          },
          {
            "name": "Create separate test file",
            "description": "Separate test file for error value.",
            "pros": [
              "Isolation"
            ],
            "cons": [
              "Unnecessary"
            ]
          }
        ],
        "chosen": "Add test case to existing XML main.test.ts",
        "rationale": "The XML test file already tests 11 scenarios. Adding one more is straightforward."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nAdd 1 test case for Payload_Xml_XmlErrorValue_get to existing XML test file.\n\n## Fix\nAdd test in `test/e2e/http/payload/xml/main.test.ts` that exercises the XML error value deserialization.\n\n## Notes\n- Verify the generated client has the xmlErrorValue.get() method\n- Test should verify correct deserialization of XML error responses"
    },
    {
      "id": "COVERAGE-TEST-QUERY-CONSTANT",
      "title": "Add e2e test for Parameters_Query_Constant",
      "category": "testing/e2e-missing",
      "priority": "P2",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "The parameters/query spec is successfully generated but has no test file. The spec tests that constant query parameters are correctly included in requests.",
        "approaches": [
          {
            "name": "Create test/e2e/http/parameters/query/main.test.ts",
            "description": "Create a test file that exercises the constant query parameter operation. The test would verify the client automatically includes the constant query parameter in the request URL.",
            "pros": [
              "Simple and direct",
              "Matches existing pattern"
            ],
            "cons": [
              "None"
            ]
          },
          {
            "name": "Skip \u2014 constant parameters may not need explicit testing",
            "description": "Constant parameters are embedded in the generated code at compile time. They don't require user input, so they may not be meaningful to test at the e2e level.",
            "pros": [
              "No work needed"
            ],
            "cons": [
              "Missing coverage",
              "Can't verify the constant is actually sent"
            ]
          }
        ],
        "chosen": "Create test/e2e/http/parameters/query/main.test.ts",
        "rationale": "Even though constant parameters are straightforward, the test verifies the generated code actually includes them in requests. The mock server validates the constant value is present."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nCreate e2e test file for Parameters_Query_Constant_post scenario.\n\n## Test File\nCreate `test/e2e/http/parameters/query/main.test.ts`\n\n## Test Cases\n1. post \u2014 verify the client sends the constant query parameter ('constantValue') in the request URL\n\n## Notes\n- Generated client at `test/e2e/generated/parameters/query/src/`\n- The spec defines a constant string query parameter that should be automatically included"
    },
    {
      "id": "COVERAGE-FEATURE-STREAMING-JSONL",
      "title": "Implement JSONL streaming support",
      "category": "bug-fix/emitter",
      "priority": "P3",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "The emitter has no JSONL streaming support. The spec requires generating code that can send and receive newline-delimited JSON (application/jsonl) as streaming request/response bodies. Currently excluded via .testignore. This is a medium-sized feature requiring changes to content-type handling, response body generation, and possibly new runtime helpers.",
        "approaches": [
          {
            "name": "AsyncIterable-based streaming with per-line JSON parsing",
            "description": "Generate operations that return AsyncIterable<T> for streaming responses and accept AsyncIterable<T> for streaming requests. For responses, read the body as a stream, split on newlines, and parse each line as JSON. For requests, iterate the input and write each object as a JSON line. Add a static helper (jsonl-helpers.tsx) with parseJsonlStream() and writeJsonlStream() utilities.",
            "pros": [
              "Standard JavaScript streaming pattern",
              "Works in both Node.js and browser",
              "Type-safe \u2014 AsyncIterable<T> is parameterized",
              "Composable with existing pipeline"
            ],
            "cons": [
              "Requires changes to operation return type generation",
              "Need to handle backpressure and partial lines",
              "More complex than simple request/response"
            ]
          },
          {
            "name": "ReadableStream-based streaming with transform",
            "description": "Generate operations that return ReadableStream<T> using the Web Streams API. Use a TransformStream to split and parse JSONL data. This is more browser-native but less ergonomic than AsyncIterable.",
            "pros": [
              "Native Web API",
              "Good backpressure support"
            ],
            "cons": [
              "Less ergonomic \u2014 users must use .getReader()",
              "Not all Node.js versions support Web Streams natively",
              "More complex API surface"
            ]
          }
        ],
        "chosen": "AsyncIterable-based streaming with per-line JSON parsing",
        "rationale": "AsyncIterable is the standard pattern for streaming in JavaScript/TypeScript and is supported natively in all modern runtimes. It's more ergonomic (for-await-of) and matches patterns used by Azure SDK and other TypeScript client libraries. The implementation can use a ReadableStream internally but expose AsyncIterable<T> to consumers."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nImplement JSONL (application/jsonl) streaming support for request and response bodies.\n\n## Scope\nThis is a medium-sized feature (~400-500 lines of new code):\n\n1. **Content-type detection** \u2014 Detect application/jsonl in TCGC types\n2. **Operation return type** \u2014 Generate AsyncIterable<T> for streaming responses instead of T\n3. **Request body** \u2014 Generate streaming request body from AsyncIterable<T>\n4. **Static helper** \u2014 Create `src/components/static-helpers/jsonl-helpers.tsx` with parseJsonlStream() and writeJsonlStream()\n5. **Remove from .testignore** \u2014 Remove 'streaming' from test/e2e/.testignore\n6. **E2E tests** \u2014 Create `test/e2e/http/streaming/jsonl/main.test.ts`\n\n## Scenarios Unblocked (2)\n- Streaming_Jsonl_Basic_receive\n- Streaming_Jsonl_Basic_send\n\n## Notes\n- Neither the legacy emitter nor the reference emitters have streaming support\n- The ts-http-runtime package may need streaming utilities added"
    },
    {
      "id": "COVERAGE-DECISION-DOCUMENTATION",
      "title": "Decision: Documentation formatting scenarios",
      "category": "testing/e2e-missing",
      "priority": "P4",
      "status": "pending",
      "kind": "feature",
      "dependencies": [],
      "design_review": {
        "problem": "6 documentation scenarios (Lists_bulletPointsModel, Lists_bulletPointsOp, Lists_numbered, TextFormatting_boldText, TextFormatting_italicText, TextFormatting_combinedFormatting) are excluded from e2e testing via .testignore. These test JSDoc/documentation formatting in generated code, not runtime behavior. Neither the legacy nor any reference emitter tests these.",
        "approaches": [
          {
            "name": "Mark as not-applicable in coverage",
            "description": "Formally annotate these scenarios as 'not-applicable' in the spector coverage configuration so they don't count against the coverage percentage. Documentation formatting is a code-generation cosmetic concern, not a runtime behavior test.",
            "pros": [
              "Honestly reflects that these aren't client functionality tests",
              "Improves coverage percentage without false work",
              "Matches other emitters' approach"
            ],
            "cons": [
              "Documentation quality isn't verified by e2e tests",
              "If a customer cares about JSDoc formatting, no test catches regressions"
            ]
          },
          {
            "name": "Implement documentation formatting and test via scenario tests",
            "description": "Support markdown formatting (bold, italic, lists) in generated JSDoc comments. Test via scenario tests (not e2e) that verify the generated documentation output matches expected formatting. Remove 'documentation' from .testignore.",
            "pros": [
              "Complete coverage",
              "Documentation quality verified",
              "Better developer experience"
            ],
            "cons": [
              "Significant work for cosmetic feature",
              "Scenario tests are better suited than e2e for this",
              "Low ROI"
            ]
          }
        ],
        "chosen": "Mark as not-applicable in coverage",
        "rationale": "Documentation formatting is a cosmetic concern that's better tested via scenario tests (which verify generated output) than e2e tests (which verify runtime behavior). The 6 scenarios should be marked not-applicable in the spector coverage configuration. If documentation formatting support is desired, it should be tracked as a separate feature with scenario test validation, not e2e."
      },
      "source": "spector-coverage-gap",
      "description": "## Summary\nDecide how to handle 6 Documentation formatting scenarios that are currently excluded from e2e testing.\n\n## Recommendation\nMark as `not-applicable` in spector coverage configuration. These test JSDoc formatting, not runtime behavior.\n\n## Scenarios\n- Documentation_Lists_bulletPointsModel\n- Documentation_Lists_bulletPointsOp\n- Documentation_Lists_numbered\n- Documentation_TextFormatting_boldText\n- Documentation_TextFormatting_italicText\n- Documentation_TextFormatting_combinedFormatting\n\n## Action\n1. Find the spector coverage configuration mechanism for marking scenarios as not-applicable\n2. Add these 6 scenarios to the not-applicable list\n3. Verify coverage percentage recalculates correctly (should increase from 87.10% to ~87.93%)"
    },
    {
      "id": "EMITTER-FIX-FILE-BODY",
      "title": "Fix emitter to handle File body operations with raw binary instead of JSON serialization",
      "category": "bug-fix/emitter",
      "priority": "P1",
      "status": "done",
      "kind": "bug",
      "dependencies": [],
      "design_review": null,
      "source": "e2e-test-discovery",
      "description": "Fix the emitter so File body operations (Type.File.Body) use raw binary handling instead of JSON model serialization.\n\n## Problem\nThe emitter generates fileSerializer/fileDeserializer functions that treat File objects as regular JSON models (with base64-encoded contents). For File body operations, this is incorrect:\n\n### Uploads\n- **Current**: Sends JSON body `{ contentType, filename, contents: base64 }` with binary Content-Type header\n- **Expected**: Sends raw binary content (Uint8Array) as the body with the correct Content-Type header\n\n### Downloads\n- **Current**: Uses `fileDeserializer(result.body)` which accesses `result.body.contents`, but the response body IS the raw binary Buffer\n- **Expected**: Should take the raw binary response body and wrap it in a File object: `{ contents: rawBuffer, contentType: responseContentType }`\n\n## Affected scenarios (8 total, all in test/e2e/http/type/file/main.test.ts)\n1. uploadFileSpecificContentType (image/png)\n2. uploadFileJsonContentType (application/json)\n3. uploadFileMultipleContentTypes (image/png | image/jpeg)\n4. uploadFileDefaultContentType (any)\n5. downloadFileSpecificContentType (image/png)\n6. downloadFileJsonContentType (application/json)\n7. downloadFileMultipleContentTypes (image/png | image/jpeg)\n8. downloadFileDefaultContentType (any)\n\n## What needs to change\nThe emitter needs to detect when an operation uses `@bodyRoot file: Http.File<...>` and:\n- For uploads: Send `file.contents` as the raw body instead of `fileSerializer(file)`\n- For downloads: Use getBinaryResponse or equivalent to read raw binary, then construct the File object\n\n## E2E tests\nTests are already written and skipped in test/e2e/http/type/file/main.test.ts. Unskip them after fixing."
    }
  ],
  "phases_metadata": [
    {
      "phase": 10,
      "name": "Flavor Parity \u2014 Match Legacy Emitter Flavor Behavior",
      "description": "Ensure the new emitter correctly handles the 'flavor' option ('core' vs 'azure') with full parity to the legacy emitter. Core flavor should use @typespec/ts-http-runtime and NOT generate Azure-specific features (LRO, paging, logger, DefaultAzureCredential). Azure flavor should use Azure SDK packages. No 'branded' option support. Flavor must be set explicitly or not at all (defaulting to 'core').",
      "dependencies": [
        8,
        9
      ],
      "status": "pending"
    },
    {
      "phase": 13,
      "name": "Spector Integration Tests \u2014 End-to-End Validation",
      "description": "Set up infrastructure to generate client libraries from Spector specs, run them against the Spector mock server, and validate end-to-end behavior. Uses Approach C (Hybrid): separate generation script + Vitest globalSetup for server lifecycle. This replaces the legacy emitter's modularIntegration test suite.",
      "dependencies": [],
      "status": "pending"
    }
  ],
  "scenario_audit_metadata": {
    "description": "Comprehensive audit of semantic differences between NEW (http-client-js) and LEGACY (autorest.typescript) scenario test expectations. Conducted by comparing all 91 matched .md files across both test suites. The LEGACY emitter output is the source of truth - all regression tasks specify that the expected state matches the legacy test.",
    "audit_date": "2026-02-26",
    "summary": {
      "total_matched_files": 91,
      "files_only_in_new": 4,
      "files_only_in_legacy": 0,
      "identical_files": 3,
      "files_with_differences": 88,
      "total_regression_categories": 34,
      "total_intentional_changes": 3,
      "critical_regressions": 7,
      "high_regressions": 15,
      "medium_regressions": 11,
      "low_cosmetic": 1,
      "needs_review": 4
    },
    "identical_files": [
      "samples/client/apiKeyCredentialClient.md",
      "samples/client/parameterizedHost.md",
      "samples/client/renameClientName.md"
    ],
    "new_only_files": [
      "client/client.md",
      "models/basic.md",
      "models/types.md",
      "operations/basic.md"
    ],
    "file_paths": {
      "new_tests": "test/scenarios/cases",
      "legacy_tests": "submodules/autorest.typescript/packages/typespec-ts/test/modularUnit/scenarios"
    },
    "intentional_changes": [
      {
        "id": "SA-INT1",
        "title": "INTENTIONAL: Runtime Package Migration (@azure-rest/core-client \u2192 @typespec/ts-http-runtime)",
        "category": "Intentional",
        "priority": "N/A",
        "status": "INTENTIONAL",
        "description": "The new emitter targets @typespec/ts-http-runtime instead of @azure-rest/core-client. This is the intended design direction. No action needed unless Azure flavor detection requires @azure-rest/core-client.",
        "legacy_behavior": "Imports from @azure-rest/core-client and @azure/core-rest-pipeline.",
        "new_behavior": "Imports from @typespec/ts-http-runtime.",
        "affected_scenarios": [
          "Nearly all scenarios"
        ]
      },
      {
        "id": "SA-INT2",
        "title": "INTENTIONAL: Type-Only Import Modifiers (import type)",
        "category": "Intentional",
        "priority": "N/A",
        "status": "INTENTIONAL",
        "description": "The new emitter adds the type keyword to type-only imports per TypeScript best practices. This is correct behavior.",
        "legacy_behavior": "import { SomeType } from './models.js';",
        "new_behavior": "import type { SomeType } from './models.js';",
        "affected_scenarios": [
          "Nearly all scenarios"
        ]
      },
      {
        "id": "SA-INT3",
        "title": "INTENTIONAL: expandUrlTemplate Import from Runtime",
        "category": "Intentional",
        "priority": "N/A",
        "status": "INTENTIONAL",
        "description": "expandUrlTemplate utility is now imported from the runtime package instead of a static-helpers path. This is part of the runtime consolidation.",
        "legacy_behavior": "import { expandUrlTemplate } from '../static-helpers/urlTemplate.js';",
        "new_behavior": "import { expandUrlTemplate } from '@typespec/ts-http-runtime';",
        "affected_scenarios": [
          "Scenarios using URL templates"
        ]
      }
    ],
    "total_root_cause_tasks": 34
  },
  "risks": [
    {
      "id": "R1",
      "title": "Output Parity Failures",
      "impact": "high",
      "probability": "medium",
      "description": "Generated code may differ from legacy emitter in ways that break existing Azure SDK consumers.",
      "mitigation": "Run scenario tests incrementally during each phase. Accept only cosmetic differences (import ordering, whitespace)."
    },
    {
      "id": "R2",
      "title": "Alloy Version Incompatibilities",
      "impact": "medium",
      "probability": "low",
      "description": "Alloy API changes between versions may require migration work.",
      "mitigation": "Pin to @alloy-js/*@0.22.0. Update deliberately with version bumps."
    },
    {
      "id": "R3",
      "title": "TCGC API Surface Changes",
      "impact": "medium",
      "probability": "medium",
      "description": "TCGC types may evolve during development, requiring adaptation.",
      "mitigation": "Pin TCGC version. Monitor TCGC releases and adapt incrementally."
    },
    {
      "id": "R4",
      "title": "Self-Import Bugs",
      "impact": "high",
      "probability": "high",
      "description": "Separate SourceFile elements for related declarations (types + serializers) cause Alloy to generate self-imports.",
      "mitigation": "Always unify declarations and serializers in same SourceFile via ModelFiles pattern."
    },
    {
      "id": "R5",
      "title": "Static Helper Loading Complexity",
      "impact": "medium",
      "probability": "medium",
      "description": "21 helper files with varied formats need proper refkey registration and source loading.",
      "mitigation": "Start with refkey declarations. Load helper sources as-is. Test each helper independently."
    },
    {
      "id": "R6",
      "title": "Performance Regression",
      "impact": "low",
      "probability": "low",
      "description": "Alloy framework overhead may slow code generation for very large service specs.",
      "mitigation": "Benchmark against legacy emitter on large specs. Alloy is optimized for codegen."
    }
  ],
  "references": {
    "alloy_core": "submodules/alloy/packages/core",
    "alloy_typescript": "submodules/alloy/packages/typescript",
    "typespec_compiler": "submodules/typespec/packages/compiler",
    "typespec_http": "submodules/typespec/packages/http",
    "emitter_framework": "submodules/typespec/packages/emitter-framework",
    "tcgc": "submodules/typespec-azure/packages/typespec-client-generator-core",
    "reference_emitter_priority1": "submodules/flight-instructor/src/typescript",
    "reference_emitter_priority2": "submodules/typespec/packages/http-client-js",
    "legacy_emitter": "submodules/autorest.typescript/packages/typespec-ts",
    "tcgc_python_usage": "submodules/typespec/packages/http-client-python"
  }
}